# Step28-v13 v1.13 v8 第五次设计构建执行失败记录

日期：2026-08-14
状态：`EXECUTION_FAILURE_NO_DATA_OR_QUALITY_CONCLUSION`

## 运行来源与许可

第四次失败最小修复提交为 `5b02cd00af3d2ca2881b58349c61411973711f77`。运行前，v8 完整构建合同 38／38 通过，固定最终根和临时根均不存在。网页端 GPT-5.6 Sol Pro 对九个精确附件独立复算后给出阻断级 0、高级 0、中等级 0、低等级 3，并明确允许在同一既有 v8 设计权威下重建一次 1,004 世界设计级数据。

本轮使用的关键字节为：

- 候选专用模板：21,879 字节／`ff97b59f0d66d7a9d18a3b1e7d7db684e6bdcc1a7ae60ba0e6b7294528c8766a`；
- 数据集构建策略：11,821 字节／`44f84a6de307f8b03a81f8a3a5a6c3dcabb30ad429a2e0aa7839ea3486c9519a`；
- 纯自然表达渲染器：36,986 字节／`634ab5a878466186cf6603fa02aa4a145ac3bd7f495015e55d24a2812289e145`；
- 科学公共合同：23,641 字节／`870bd0ff248ca966b8c9c2a149dd778b0e35d6ee52e9a6731a701d191b580732`；
- 科学世界：53,875 字节／`81baac93a4f07d072188c187fa301391f88fd1dc7f1513c629cf2a4e8ee3b5bd`；
- 数据集构建器：41,129 字节／`23b23e6ad694f4a98ba48438b344058d866466bb28523104cdf0b169cf26ae94`；
- 构建合同测试：86,254 字节／`5b87862b70e3e3e3c774ce5270d512f9cbf79bd80e9c36654d8b53c7236093bb`。

唯一执行命令为：

```powershell
python -B scripts\step28_v13_v1_13_scientific_dataset_builder_v8.py --mode design_preflight
```

## 第五次执行失败

构建器从空商品、卖家和身份登记表开始，按固定顺序完成训练拆分序号 0 至 282，共 283 个世界。训练序号 224 已正常越过，证明第四次失败的定点修复在真实累计登记表下生效。

在训练拆分序号 283、全局位置 284，32 个预定义候选全部发生精确文档碰撞，构建器关闭失败：

```text
All 32 exact-document candidates collided
```

进程退出码为 1，总用时 880.4 秒。当前证据只证明 32 个候选均至少命中六类精确碰撞之一，尚未证明具体碰撞类别或根因与序号 224 相同；不得把本次失败武断归因于某一属性轨道。

异常事务已删除 `.design_preflight_v8_20260812.building`；最终根 `design_preflight_v8_20260812` 从未发布。退出后两类根均不存在，也没有 Python 残留进程。没有可恢复前缀、设计数据、质量结论、正式种子、正式行、模型或指标。

## 不复用与后继边界

提交 `5b02cd0` 的构建实现字节永久不得再次运行。训练序号 0 至 282 的未发布结果不得恢复、拼接或当作数据使用。

后继诊断最多只能从训练序号 0 顺序、内存重放到已经暴露的序号 283，携带真实累计商品、卖家和身份登记表；不得读取训练序号 284 以后、开发拆分或审核拆分。诊断必须先区分六类精确碰撞并记录目标 32 个候选的交集结构与不变状态，再判断是否存在新的最小、全局且标签隔离的因果修复。不得换盐、增加候选上限、删除历史排除、改变固定规模、写已知世界／商品／摘要特例，或把质量探针、标签、控制者和模型分数引入候选选择。

任何后继实现都必须形成新的来源边界，重新完成因果回归、完整合同和独立网页运行前审查。质量审计、正式 500×4、真值解封以及 M0／M1／M2／M3 训练继续禁止。
