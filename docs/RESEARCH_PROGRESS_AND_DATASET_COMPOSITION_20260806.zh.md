# 当前科研进度与中英文数据集组成

更新时间：2026-08-09

## 一、当前结论

本项目的科研主线是：先用真实英文地下市场卖家对训练英文冻结基线（M0），再构造具有新控制者、新卖家和新身份历史的中文合成数据，用同一个英文基础分数比较无适配、错配身份对照和正确身份适配，判断英文马甲识别能力能否借助中文身份历史实现跨语言迁移。

当前已经完成英文操作基线的固定，但它尚未被新收集的独立英文确认集证明为唯一最强。中文合成主线的 v1.12 曾完成正式全库测试、预锁、一次性种子仪式以及训练和开发拆分暂存，但联合质量审计发现历史商品文档碰撞，因此整个版本永久失败；种子保管区和约 1.89 吉字节失败暂存已经删除。当前后继 v1.13 已完成可见文档碰撞防线的最终设计合同和第一阶段纯原语实现，包括商品/卖家文档精确哈希、行级多重性、历史禁用登记重放、标题缺失事实重放和候选键派生；候选父结构、资料贡献血缘、身份试算、生成器接线与事务仍未实现。v1.13 的正式种子、派生能力、正式数据行、模型训练和审核结果仍全部为 0，不能声称中文合成数据集已经训练就绪，也不能声称身份迁移有效。

## 二、模型角色

- 英文冻结基线（M0）：只用真实英文标签训练，进入中文实验后完全冻结。
- 错配身份对照（M1）：在相同英文基础分数和相同样本上，使用五份整行错配的三十三维身份历史。
- 正确身份适配模型（M2）：与错配对照使用相同训练程序，只把错配身份历史换成正确对齐的身份历史。
- 直接中文监督强对照（M3）：只用合成中文训练标签直接训练，用于回答“为什么不直接训练中文模型”。

主要证据应来自正确身份适配模型相对五份错配身份对照的提升，其次才是相对英文冻结基线的提升。四类模型目前尚未在新的正式中文合成数据上产生结果。

## 三、英文真实数据

### 3.1 监督数据组成

英文来源域共有 1,321 条已审核卖家对，其中 226 条被审核为同一控制者、525 条被审核为不同控制者、570 条为不确定。不确定样本不进入二分类监督；满足主监督资格的正例为 209 条，因此最终二分类监督集为 734 条。

| 分区 | 总数 | 同一控制者正例 | 不同控制者负例 |
| --- | ---: | ---: | ---: |
| 训练集 | 401 | 116 | 285 |
| 验证集 | 152 | 42 | 110 |
| 测试集 | 181 | 51 | 130 |
| 合计 | 734 | 209 | 525 |

三个分区之间的卖家重叠和规范化别名重叠均为零。第七步（Step 7）原始表结构审计另发现一条验证负例由两条长描述续行误建而成，所以当前英文模型复核的有效边界是 401 条训练对、151 条描述性验证对和 181 条历史测试对；训练集的 401 条没有变化。

英文广义特征表中的 6,683 行是候选及特征计算范围，不是 6,683 条监督标签，也不是英文基线的训练量。当前基线实际只用上述 401 条训练对。

### 3.2 文本来源与输入

第七步第四版从两个不可变商品快照读取有效英文商品：`market_item.xlsx` 109,703 条、Agora 53 条，共 109,756 条，覆盖 853 个有效卖家。模型只读取完成身份删除后的标题和描述；市场名、卖家名、联系方式和结构化身份字段不得进入文本编码。

长文本按相同规则完整分块，不再截取单个窗口。模型先形成商品级表示，再计算卖家对的标题质心相似度、描述质心相似度、标题局部匹配、描述局部匹配及两类等权组合。

### 3.3 当前英文冻结基线

当前操作基线是 LightGBM（轻量梯度提升机）加 legacy18（十八项历史安全特征）加 LaBSE（语言无关句子嵌入模型）的完整流水线。LaBSE 只生成冻结表示，真正负责二分类的是 LightGBM；这不是对 LaBSE 进行端到端微调。

在第一组重复折分上，该流水线的分量等权平均精确率为 0.944873，卖家对等权平均精确率为 0.832358；分量等权接收者操作特征曲线下面积为 0.979793，卖家对等权接收者操作特征曲线下面积为 0.910496。更换五个新折分种子后，相应数值变为 0.934114、0.815056、0.978376 和 0.903116，并由第一名降至第二名。

这些高分受精确文本克隆捷径明显抬高：去除精确克隆后只剩 27 条正例和 259 条负例，且正例高度集中在少数连通分量，候选排序发生反转。因此该流水线只是为第二十八步（Step 28）固定的可复现操作基线，不能表述为“已由独立数据证明唯一最强”。

## 四、中文真实数据

当前中文真数据共有 893 条卖家对：

| 分区 | 总数 | 同一控制者正例 | 不同控制者负例 |
| --- | ---: | ---: | ---: |
| 训练集 | 573 | 229 | 344 |
| 验证集 | 120 | 30 | 90 |
| 测试集 | 200 | 50 | 150 |
| 合计 | 893 | 309 | 584 |

## 五、中文合成数据的当前状态

当前工作区仍没有一份可供 M0/M1/M2/M3 正式训练的全新中文合成数据集。v1.3 至 v1.11 均已永久失败并按纪律清理；旧 v1.2 只保留历史证据，训练资格已经撤销。v1.12 曾正式生成并封存训练集和开发集，各 500 个世界、189,000 对，训练集另有五份错配身份结构；但联合质量审计发现 35 个完整商品文档哈希与成功 v1.2 精确重复，且本轮内部还有 10 次完整商品文本重复。因此 v1.12 已永久失败，未发布、无训练资格、无模型结果。

最新设计预检在训练和开发各 500 个世界上逐行重算：原始分支每个拆分 189,000 对，反事实机制中性分支每个拆分 186,000 对。原始文本存在约 `0.54` 至 `0.55` 的弱字符风格预测信号；将控制者关联风格进行标签盲错排后，最大单特征对称曲线下面积为 `0.508517`，六模型最大值为 `0.504527`，9,999 次世界重抽样上界为 `0.509560`。这说明冻结攻击范围内的明显模板、标点和排版捷径已被压到接近随机，但不等于正式数据已经通过审核。

下一步是 v1.13 事后工程修复，不是直接训练模型。v1.12 的 84,000 个身份值哈希、100,936 个商品文档哈希、28,000 个卖家文档哈希、六类编号哈希和全部已消费承诺已进入不可复用归档；失败私有暂存和种子保管区已删除，共释放 1,887,149,059 字节。v1.13 必须在新种子前同时冻结商品文档与卖家五字段文档的历史、失败版本、同次运行及跨拆分去重规则，并重新执行设计预检、完整测试、源码冻结和一次性种子仪式。完整失败与清理边界见 `docs/STEP28_V13_V1_12_TERMINAL_FAILURE_AND_CLEANUP_20260809.zh.md`。

上述规则现已写成最终设计合同 `docs/STEP28_V13_V1_13_VISIBLE_DOCUMENT_COLLISION_CONTRACT_20260809.zh.md`，大小 17,811 字节，文件 SHA-256 为 `0f1906a8be46be3d3cdf16738814e0c83192e97299d162857b71ca585208eca6`。它固定候选无关的结构、身份、登记覆盖、噪声目标和卖家资料贡献血缘；只允许精确可见文档碰撞推进自然表述候选，其他闭包错误一律关闭版本；逐世界提交与拆分最终封存均禁止覆盖。网页端最终复审四级问题均为 0。该通过仅授权进入代码实现，不授权种子仪式或正式生成。

第一阶段纯实现现已完成。机器策略和脚本的 SHA-256 分别为 `2331ccbcf4c08171204f86fbe44a430780d11967b944e72996b707de2c9cca99` 与 `5cb1907ed6d15ea52cdc0b35069fdff1cc651dca2b570debbb53ee77a8f9d4d4`。只读重放得到 302,944 个商品文档禁用哈希、84,000 个卖家文档禁用哈希、999,996 个身份值禁用哈希和 37 个已消费承诺；六类编号哈希也逐项通过。网页端首次代码审查发现的两个高级、三个中级和一个低级问题均已修正或固化为回归测试，修正版获得 `V1.13 COLLISION PRIMITIVES PATCH GO`，最后一项低级测试建议也已确认关闭；当前 24 项定向测试通过。全仓 512 项中 504 项通过、7 项既有跳过，唯一失败仍是冻结的旧 Step28-v12.1 清单要求持续更新进度文档回到 2026-07-20 字节，并非 v1.13 新回归。该阶段没有生成数据，下一步只是实现候选无关父结构、资料贡献血缘和一次性身份试算。
