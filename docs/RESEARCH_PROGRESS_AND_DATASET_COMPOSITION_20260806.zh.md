# 当前科研进度与中英文数据集组成

更新时间：2026-08-10

## 一、当前结论

本项目的科研主线是：先用真实英文地下市场卖家对训练英文冻结基线（M0），再构造具有新控制者、新卖家和新身份历史的中文合成数据，用同一个英文基础分数比较无适配、错配身份对照和正确身份适配，判断英文马甲识别能力能否借助中文身份历史实现跨语言迁移。

当前已经完成英文操作基线的固定，但它尚未被新收集的独立英文确认集证明为唯一最强。中文合成主线的 v1.12 曾完成正式全库测试、预锁、一次性种子仪式以及训练和开发拆分暂存，但联合质量审计发现历史商品文档碰撞，因此整个版本永久失败；种子保管区和约 1.89 吉字节失败暂存已经删除。当前后继 v1.13 已完成可见文档碰撞防线的最终设计合同、第一阶段纯碰撞原语、第二阶段候选无关父结构与一次性身份试算、第三阶段受限候选视图和独立自然表述变化、第四阶段甲的内存精确文档碰撞候选选择，以及第四阶段乙的一世界开发烟雾事务、恢复、封存和清理实现；第四阶段乙的实现来源已由独立 Git 父锚固定。2026-08-10 又冻结了收缩后的中文合成身份迁移科研实验合同，下一任务已明确回到 500 世界、四拆分数据构建和 M0/M1/M2/M3 比较，不再继续运行环境对抗或通用安全系统设计。v1.13 的正式种子、派生能力、正式候选、正式数据行、模型训练和审核结果仍全部为 0，不能声称中文合成数据集已经训练就绪，也不能声称身份迁移有效。

## 二、模型角色

- 英文冻结基线（M0）：只用真实英文标签训练，进入中文实验后完全冻结。
- 错配身份对照（M1）：在相同英文基础分数和相同样本上，使用五份整行错配的三十三维身份历史。
- 正确身份适配模型（M2）：与错配对照使用相同训练程序，只把错配身份历史换成正确对齐的身份历史。
- 直接中文监督强对照（M3）：只用合成中文训练标签直接训练，用于回答“为什么不直接训练中文模型”。

主要证据应来自正确身份适配模型相对五份错配身份对照的提升，其次才是相对英文冻结基线的提升。四类模型目前尚未在新的正式中文合成数据上产生结果。

## 三、英文真实数据

### 3.1 监督数据组成

英文来源域共有 1,321 条已审核卖家对，其中 226 条被审核为同一控制者、525 条被审核为不同控制者、570 条为不确定。不确定样本不进入二分类监督；满足主监督资格的正例为 209 条，因此最终二分类监督集为 734 条。

| 分区 | 总数 | 同一控制者正例 | 不同控制者负例 |
| --- | ---: | ---: | ---: |
| 训练集 | 401 | 116 | 285 |
| 验证集 | 152 | 42 | 110 |
| 测试集 | 181 | 51 | 130 |
| 合计 | 734 | 209 | 525 |

三个分区之间的卖家重叠和规范化别名重叠均为零。第七步（Step 7）原始表结构审计另发现一条验证负例由两条长描述续行误建而成，所以当前英文模型复核的有效边界是 401 条训练对、151 条描述性验证对和 181 条历史测试对；训练集的 401 条没有变化。

英文广义特征表中的 6,683 行是候选及特征计算范围，不是 6,683 条监督标签，也不是英文基线的训练量。当前基线实际只用上述 401 条训练对。

### 3.2 文本来源与输入

第七步第四版从两个不可变商品快照读取有效英文商品：`market_item.xlsx` 109,703 条、Agora 53 条，共 109,756 条，覆盖 853 个有效卖家。模型只读取完成身份删除后的标题和描述；市场名、卖家名、联系方式和结构化身份字段不得进入文本编码。

长文本按相同规则完整分块，不再截取单个窗口。模型先形成商品级表示，再计算卖家对的标题质心相似度、描述质心相似度、标题局部匹配、描述局部匹配及两类等权组合。

### 3.3 当前英文冻结基线

当前操作基线是 LightGBM（轻量梯度提升机）加 legacy18（十八项历史安全特征）加 LaBSE（语言无关句子嵌入模型）的完整流水线。LaBSE 只生成冻结表示，真正负责二分类的是 LightGBM；这不是对 LaBSE 进行端到端微调。

在第一组重复折分上，该流水线的分量等权平均精确率为 0.944873，卖家对等权平均精确率为 0.832358；分量等权接收者操作特征曲线下面积为 0.979793，卖家对等权接收者操作特征曲线下面积为 0.910496。更换五个新折分种子后，相应数值变为 0.934114、0.815056、0.978376 和 0.903116，并由第一名降至第二名。

这些高分受精确文本克隆捷径明显抬高：去除精确克隆后只剩 27 条正例和 259 条负例，且正例高度集中在少数连通分量，候选排序发生反转。因此该流水线只是为第二十八步（Step 28）固定的可复现操作基线，不能表述为“已由独立数据证明唯一最强”。

## 四、中文真实数据

当前中文真数据共有 893 条卖家对：

| 分区 | 总数 | 同一控制者正例 | 不同控制者负例 |
| --- | ---: | ---: | ---: |
| 训练集 | 573 | 229 | 344 |
| 验证集 | 120 | 30 | 90 |
| 测试集 | 200 | 50 | 150 |
| 合计 | 893 | 309 | 584 |

## 五、中文合成数据的当前状态

当前工作区仍没有一份可供 M0/M1/M2/M3 正式训练的全新中文合成数据集。v1.3 至 v1.11 均已永久失败并按纪律清理；旧 v1.2 只保留历史证据，训练资格已经撤销。v1.12 曾正式生成并封存训练集和开发集，各 500 个世界、189,000 对，训练集另有五份错配身份结构；但联合质量审计发现 35 个完整商品文档哈希与成功 v1.2 精确重复，且本轮内部还有 10 次完整商品文本重复。因此 v1.12 已永久失败，未发布、无训练资格、无模型结果。

最新设计预检在训练和开发各 500 个世界上逐行重算：原始分支每个拆分 189,000 对，反事实机制中性分支每个拆分 186,000 对。原始文本存在约 `0.54` 至 `0.55` 的弱字符风格预测信号；将控制者关联风格进行标签盲错排后，最大单特征对称曲线下面积为 `0.508517`，六模型最大值为 `0.504527`，9,999 次世界重抽样上界为 `0.509560`。这说明冻结攻击范围内的明显模板、标点和排版捷径已被压到接近随机，但不等于正式数据已经通过审核。

下一步是 v1.13 事后工程修复，不是直接训练模型。v1.12 的 84,000 个身份值哈希、100,936 个商品文档哈希、28,000 个卖家文档哈希、六类编号哈希和全部已消费承诺已进入不可复用归档；失败私有暂存和种子保管区已删除，共释放 1,887,149,059 字节。v1.13 必须在新种子前同时冻结商品文档与卖家五字段文档的历史、失败版本、同次运行及跨拆分去重规则，并重新执行设计预检、完整测试、源码冻结和一次性种子仪式。完整失败与清理边界见 `docs/STEP28_V13_V1_12_TERMINAL_FAILURE_AND_CLEANUP_20260809.zh.md`。

上述规则现已写成最终设计合同 `docs/STEP28_V13_V1_13_VISIBLE_DOCUMENT_COLLISION_CONTRACT_20260809.zh.md`，大小 17,811 字节，文件 SHA-256 为 `0f1906a8be46be3d3cdf16738814e0c83192e97299d162857b71ca585208eca6`。它固定候选无关的结构、身份、登记覆盖、噪声目标和卖家资料贡献血缘；只允许精确可见文档碰撞推进自然表述候选，其他闭包错误一律关闭版本；逐世界提交与拆分最终封存均禁止覆盖。网页端最终复审四级问题均为 0。该通过仅授权进入代码实现，不授权种子仪式或正式生成。

第一阶段纯实现现已完成。机器策略和脚本的 SHA-256 分别为 `2331ccbcf4c08171204f86fbe44a430780d11967b944e72996b707de2c9cca99` 与 `5cb1907ed6d15ea52cdc0b35069fdff1cc651dca2b570debbb53ee77a8f9d4d4`。只读重放得到 302,944 个商品文档禁用哈希、84,000 个卖家文档禁用哈希、999,996 个身份值禁用哈希和 37 个已消费承诺；六类编号哈希也逐项通过。网页端首次代码审查发现的两个高级、三个中级和一个低级问题均已修正或固化为回归测试，修正版获得 `V1.13 COLLISION PRIMITIVES PATCH GO`，最后一项低级测试建议也已确认关闭；当前 24 项定向测试通过。全仓 512 项中 504 项通过、7 项既有跳过，唯一失败仍是冻结的旧 Step28-v12.1 清单要求持续更新进度文档回到 2026-07-20 字节，并非 v1.13 新回归。该阶段没有生成数据，下一步只是实现候选无关父结构、资料贡献血缘和一次性身份试算。

第二阶段代码、合同测试和网页端复审均已完成。新增父对象把 28 个卖家、378 对、商品结构、登记覆盖、噪声目标、控制者机制、身份结构和每个卖家的六项风格因子固定为候选不得改变的指纹；标题与描述原文不进入该指纹。私有资料血缘逐项记录五个冻结资料字段的支持商品、首次位置、聚合角色和输出名次，描述签名片段还记录提取序号与卖家文档频率集合摘要，并证明公开第三步资料字节完全不变。

身份试配器只接受唯一固定父代，历史身份禁用表、身份键、计数上限和本阶段固定为空的已提交集合都由内部权威来源提供；它只进入一次，不返回密钥或实时集合。随后重算生产解析、脱敏、资料及全部 378 对三十三维身份历史，并从第零计数独立重放每个身份资产的首个可接受值。第二阶段 25 项测试与第一阶段 24 项联合 49 项全部通过，网页端最终给出四级问题均为 0。全仓回归共运行 537 项，用时 1052.129 秒，其中 529 项通过、7 项既有声明跳过；唯一失败是冻结的 Step28-v12.1 同步清单仍固定旧进度文档字节，不是 v1.13 新实现失败，且没有运行错误。它仍只是单个开发烟雾世界的内存验证，不是正式数据生成。

第三阶段现已完成受限视图和自然表述变化。纯渲染器只读取匿名商品、噪声目标、安全表述库和一个 32 字节候选键，不读取标签、控制者、身份、登记覆盖、标题克隆或高语义机制；这些私有约束只由可信组装器保存和复核。可信组装器还对候选父对象与身份试配对象的全部字段计算冻结根承诺，重新派生候选键并重绘候选，阻止调用方伪造一套自洽但不同的父状态。任何未正常结束的候选调用都会使会话永久失效，不能跳过失败候选继续选择。

单个开发烟雾世界的 32 个候选均产生互异的最终自然文本和完整世界，相对第零候选有 51 至 105 个商品发生变化。网页端审查先后发现并关闭四个高级、两个中级、一个低级问题及第二轮剩余的一个高级问题，终审四级问题均为 0，并签署 `V1.13 NATURAL VARIATION FINAL GO`。本阶段 34 项测试通过，三个 v1.13 阶段联合 83 项全部通过；全仓 571 项中 563 项通过、7 项既有声明跳过，唯一失败仍是冻结的旧 Step28-v12.1 进度文档快照检查，没有 v1.13 新失败或错误。

这些结果只证明“固定结构与身份后，在不向纯渲染器泄露私有机制的情况下可确定地产生 32 个自然表述候选”这一组件链成立，不是中文合成数据集质量结论。下一步必须实现只由完整商品或卖家文档精确碰撞触发的候选重试、不可覆盖的逐世界事务和拆分封存，再重新审查与回归。当前仍没有可训练的新中文合成数据集，也不能开启种子仪式或训练 M0、M1、M2、M3。

第四阶段甲现已完成内存候选选择。候选零之前先证明两条精确标题克隆的来源标题、目标标题和描述均非空，端点没有被更早登记覆盖占用，并把真实负例标记和候选父代完整根写入私有资格证明；这些事实不进入纯渲染器视图。候选选择器没有调用方可传入的历史集合、拆分集合、密钥、候选上限或故障开关，最多按顺序查看 32 个候选。每个候选必须先完成结构、身份、解析、脱敏、资料和三十三维身份历史闭包，之后再从生产脱敏商品和最终五字段卖家资料计算文档哈希。只有八类精确文档碰撞可以继续下一个候选，其他错误立即永久终止。

开发烟雾世界在候选零即零碰撞通过，共有 105 个商品文档摘要、28 个卖家文档摘要和 84 个完整身份分配增量。接受对象会保留完整注册表增量、选择上下文和标题克隆资格；复核时既要逐字段等于本次选择器实际生成的可信完整候选，也要等于本次真实搜索历史产生的原始接受状态根，然后重新执行生产文档、哈希、碰撞和基数门。因此同步修改私有世界或拒绝计数并重算全部自摘要也不能被误判为真实结果。该对象明确不可提交，没有写盘路径。

网页端最终复审四级问题均为 0，并签署 `V1.13 CANDIDATE SELECTION IMPLEMENTATION GO`。第四阶段甲 31 项测试通过，四阶段联合 114 项全部通过，用时 238.920 秒。全仓运行 602 项，用时 1430.937 秒，其中 594 项通过、7 项既有声明跳过；唯一失败仍是冻结 Step28-v12.1 清单要求运行前 212,007 字节的进度文档回到 199,490 字节历史快照，没有 v1.13 新失败或运行错误。

在第四阶段甲完成时，这些结果仍不是正式中文合成数据集；当时下一步只能实现第四阶段乙：排他锁内重建拆分状态、锁内选择候选、不可覆盖地提交 `WORLD_ACCEPTED.json`、从连续标记恢复、确定性发布文档注册表并写入 `SPLIT_SEAL_COMPLETE.json`。在该阶段完成复审和回归前，正式种子、正式生成和 M0、M1、M2、M3 训练均不得开始。

第四阶段乙的开发烟雾实现现已完成代码复审。它只在自动删除的临时目录中处理一个钉住的审核甲世界：排他锁先于任何状态读取和候选选择，世界标记最后发布，七个最终投影与拆分封印不可覆盖，清理后仍可由固定重放验证。经直接源码守卫运行的专项攻击测试 60 项通过，另有 1 项 Windows 符号链接权限跳过；前四阶段甲 114 项最近一次全部回归通过。真实烟雾完成后没有保留字节码或临时数据。

正式入口必须使用隔离参数并经源码守卫执行。守卫不运行启动钩子或字节码，核对策略、入口、事务和测试源码，且把解释器标准库、第三方依赖、项目脚本分层。普通全仓测试发现不包含、也不认证这 60 项合同；合同文件若未经守卫注入固定模块名和专用标记，会在任何导入和测试开始前拒绝。当前 Windows 开发环境仍需重新加入用户包目录，因此第三方依赖字节和操作系统环境不在本次代码通过范围内；正式运行前必须单独冻结环境。网页端终审四级问题均为 0，并给出“实现代码通过、外部父锚待完成”。普通全仓发现另行运行 602 项、用时 1681.523 秒，594 项通过、7 项既有跳过；唯一失败仍是冻结 Step28-v12.1 进度文档历史快照检查，没有 v1.13 新失败或运行错误。

第四阶段乙仍不是中文合成数据集，也没有把不可提交的候选变成正式候选。四文件实现已由父提交 `dbafb62f91a51b057b5a4846b8028de4076c7c1c` 和独立后继回执固定；该父锚只关闭实现来源循环。下一步是设计正式多拆分权限、前序拆分封存、Linux 目录持久性与环境证明。完成这些工作并另行冻结策略之前，正式 500 世界、种子仪式、正式数据生成以及 M0、M1、M2、M3 训练均不得开始。

## 六、2026-08-10 科研主线收缩与正式实验合同

本节覆盖上文“继续设计 Linux 目录持久性与环境证明”的旧下一步，但不改写其历史发生事实。项目不再把运行环境对抗、网络攻防或通用文件系统隔离当作科研贡献。当前唯一主线是：完成正式中文合成马甲数据集，随后训练并比较 M0、五个 M1、M2、M3-base 和 M3-joint。

权威科研合同为 `docs/STEP28_V13_V1_13_SCIENTIFIC_EXPERIMENT_CONTRACT_20260810.zh.md`，大小 19,037 字节，SHA-256 为 `936ba6d52fa983307c3be654d9996814e740aea154c290c86411064d35e480f6`。网页端 GPT-5.6 Sol 极高推理经过四轮纯科研设计审查，先后关闭 M0 中文阈值污染、M1 映射选择自由度、M1/M2 数学歧义、M3 比较边界、审核标签提前消费、统计区间多义性和 372 对反事实基线错误；最终分级为阻断级、高级、中级和低级问题均 0，并明确给出“合同可进入实现”。

合同固定四个互不复用拆分，每拆分 500 个世界、每世界 28 个卖家、12 个控制者和完整 378 个卖家对，共 189,000 对；每世界 20 个正例、358 个负例。M0 的模型、预处理和英文阈值完全冻结；五个 M1 由事前唯一、无标签的一因子错配算法生成；M2 使用正确三十三维身份历史；M3-base/M3-joint 是直接中文监督强对照。所有模型在开发集、审核集甲、审核集乙必须分别报告 AP（平均精确率）与梯形 PR-AUC（精确率—召回率曲线下面积），并报告 MRR（平均倒数排名）、MAP（平均准确率均值）、Recall@1/3/5/10、NDCG@1/3/5/10 及合同中的其余分类指标，任何一项不得漏报。

当前授权只允许把现有一世界原语扩展成 500 世界乘四拆分的构建器、合并器和科研质量审计，不授权直接宣称数据已完成，也不授权在质量门通过前训练。正式种子、正式数据行、M0 分数、M1/M2/M3 模型和正式指标仍全部为 0。紧接着的实现工作必须直接服务中文合成数据构建；若某项工作不能说明其与数据质量、标签隔离、模型比较或固定评估的直接关系，应停止而不是扩展旁支。
