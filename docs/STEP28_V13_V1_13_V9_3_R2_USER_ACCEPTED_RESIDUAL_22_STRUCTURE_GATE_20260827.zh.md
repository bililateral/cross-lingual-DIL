# Step28-v13 v1.13 V9.3-R2 用户接受 22 点残差与结构捷径门合同

日期：2026-08-27  
状态：`DATASET_INVALIDATED_PREBUILD_STRUCTURE_GATE_NO_METHOD_ROOT_NO_TRUTH_NO_TRAINING`

## 1. 后继理由

V9.3-R1 的唯一运行在开发拆分残差 `22` 处因不能继续严格下降而失败关闭；没有发布计划或数据。用户随后明确要求停止优化该残差并继续科研主线。本合同把这一决定透明记录为看见开发终态后的版本化放宽，不追认 R1，也不声称 22 点满足理论精确配平。

允许的唯一科研解释仍是：公开完整残差，并以早于本次放宽冻结的有限结构探针检验它是否形成超过门限的可学习结构捷径。结构门失败即关闭 R2；结构门通过也不能表述为“不存在捷径”或“实现精确平衡”。

## 2. 唯一变化与保持不变的部分

R2 相对 R1 只改变一项：版本中立残差检查点边界从 `20` 放宽为 `22`。训练固定轨迹仍应终止在 `20`；开发固定轨迹应重现 `22`。公开种子、55,000,000 次搜索、候选生成、粗修复、输入文件、5,324 个统计单元、计划合法性、完整披露格式、结构矩阵、四个探针模型、阈值和 9,999 次世界重抽样全部保持不变。

不得为了通过结构门换种子、增删特征、改变模型、调阈值、重排世界、修改标签或返回修复计划。确定性重放只用于重新物化 R1 原子失败时未保留的内存终态，不是重新选择结果。

## 3. 版本与唯一输出

- 计划版本：`2026-08-27-step28-v13-v1-13-registered-negative-plan-v9-3-r2-user-accepted-residual-22`；
- 计划状态：`PASS_DETERMINISTIC_USER_ACCEPTED_RESIDUAL_22_PENDING_STRUCTURE_GATE`；
- 唯一计划根：`reports/step28_v13_v1_13_balanced_schedule_v9_3/registered_negative_bounded_preflight_r2_20260827`；
- 唯一结构门根：`reports/step28_v13_v1_13_balanced_schedule_v9_3/registered_negative_structure_gate_r2_20260827`；
- 方法政策、随机权威、方法资格根和质量审计必须使用 R2 独立版本及路径，不得复用 R1 名称或状态。

五文件计划根仍必须包含训练／开发计划、两份完整残差披露和一个构建回执。每份披露必须列出全部 5,324 个单元，不得只保留总残差。重新打开后必须从零重建并验证训练 `20／20／20`、开发 `22／22／22`；任何值不一致立即失败。

## 4. 结构门和主线边界

计划根发布后，先冻结方法政策，再创建一次性 R2 随机权威并运行一次前置结构门。硬门保持：单特征最大对称 ROC-AUC 不超过 `0.52`，家族最大对称 ROC-AUC 不超过 `0.53`，家族最大 AP 提升不超过 `0.01`，世界重抽样的 ROC-AUC 与 AP 提升 95% 上界分别不超过 `0.53` 和 `0.015`。

只有结构门全部通过，才允许构建 1,004 世界方法资格数据并执行完整质量审计。完整审计仍须重算同一结构探针，并继续检查代码零出现、私有坐标非干预、碰撞、拆分隔离、反事实文本和全部冻结证据门。审核甲／乙真值在规定阶段以前保持封存；M0／M1／M2／M3 在方法资格质量通过以前继续禁止。

## 5. 失败纪律

R2 的确定性终态、机械验证、政策冻结、随机权威、结构门、数据构建或完整质量审计任一步失败，先记录运行编号、阶段、根因、关键摘要和结论边界，再删除失败载荷、临时目录及仅服务失败版本的代码。不得再次提高残差边界或用新的公开种子重试。

## 6. 实现审查记录

网页端 GPT-5.6 Sol Pro 首轮读取 R1／R2 文档、构造器、R2 发布器和专项测试后，正确确认构造器除 `20→22` 外的算法字节未变，也确认发布器分别要求训练 `20／20／20`、开发 `22／22／22`；但因本地首条消息漏传已修改的计划验证器，它错误读取会话旧附件并报告“仍复用 R1 版本／状态”。本地实际导入值与正在运行的发布器均证明计划版本和状态已经是 R2，因此该项未作为代码缺陷采纳；专项测试随后增加两个 R2 合同字面量断言，避免只做内部自引用。

补传实际计划验证器和更新测试后，网页端明确撤销上述阻断，给出阻断／高级／中级问题 `0／0／0`，末行为“可继续R2结构门准备”。最终窄范围回复正文为 692 个 UTF-8 字节，SHA-256 为 `73c294ac917d6eed1863f56ce4cb6b6404a2be0de1c0a7e04991486d4c631c37`；会话为 `https://chatgpt.com/c/6a8f98b3-1704-83eb-aed3-335400327c15`。该回复只是实现审查证据，不替代本地重放、政策冻结或正式结构门结果。

计划发布后的集成审查又发现两项正式冻结前错误。第一，完整质量审计器把 `private_coordinate_nonintervention` 直接写为常量真值，不能作为可失败的登记观察；现已改为逐拆分验证原始／风格错排商品、卖家画像和三十三维身份特征的精确模式、行连接、私有字段及私有标识符零泄漏。第二，前置结构门的规范自摘要采用“删除 `canonical_self_sha256` 字段后散列”，而方法根构建器和质量审计器曾用“字段保留但置空后散列”；两者不等价，会机械拒绝合法的通过结果。两处现使用专用结构门摘要函数，其他对象原有置空规范不变，并有反例测试钉住。

网页端对修复字节复核后指出，私有标识符正则的两端边界会漏掉 `prefix_sel_<64位十六进制>` 和 `sel_<64位十六进制>_suffix`。该意见成立，现已删除两端边界并增加两个包裹反例。网页端同时建议卖家画像增加 `world_uid`；本地核对冻结模式确认画像连接字段精确只有 `seller_uid`，画像卖家全集又必须等于 `sellers.jsonl` 的唯一 `seller_uid→world_uid` 映射域，额外 `world_uid` 反而属于模式漂移。补充精确模式反例后，网页端撤销该意见，最终给出阻断／高级／中级 `0／0／0`，末行为“修复后可冻结R2政策”。

## 7. R2 计划重放结果

唯一确定性重放正常退出并原子发布精确五文件计划根。训练拆分独立全量重建为一范数违例／平方目标／非零单元数 `20／20／20`，安排摘要 `f9e7947a930ea72be0ca55576a0855a8c0940b175e8faa8fc0712bd5d5ec5971`，5,324 单元摘要 `c44b1f13b622fae0f6bfc85a95f0d30f81524c7d72500c92e288c0c8aabf2a86`。开发拆分重现 `29→25→24→23→22` 后在 R2 边界停止，独立重建为 `22／22／22`，安排摘要 `3106b7203613d8d38f184579430eaa7e354e5c336e891f7dfcfbdd6bcf780782`，5,324 单元摘要 `fd47893367df27cbc78b978b81120e07b2786ba1680ec5ab14d8e1a14863d606`。没有进入 R1 失败的第 5 轮。

构建回执规范自摘要为 `08e49176f9c4f8e3480378578881e55d3248c2e0743198c7ecfd442875eab77f`，文件 SHA-256 为 `60f86e54093198c8d7650a3ff1f212aa3a0ca5db3c9ca990b2b95a9fc64bcec3`。其余四文件的字节数／SHA-256 为：训练计划 `909,028／6c5a6d4462b894c13f6c5579ff90614ea273f5a0d49ebf181ae9e7f316d503be`，训练披露 `759,714／32500b7b87cf543282ffc8045df8b11659733a204255f0993f2d14c6d2dce585`，开发计划 `909,034／87218b8f83e292977995fb64ca2741e7cf2c9d94a2e01327f4416f20a68992da`，开发披露 `758,345／74c5bf818015cbc6ff1d513bf8a2aaca9403111354ed9ebdea0838832a5333ce`。结构门入口再次重开五文件、重算两个拆分和计划对，结果一致；49 项相邻合同回归通过。

政策冻结前全仓回归共运行 846 项，用时 1,692.258 秒：832 项实际通过、14 项按既定边界跳过、0 失败、0 错误。回归后 49 文件路径列表摘要仍为 `efd87c51567d3cde6f5d3b86edeeb97da8897447362c962d97ea1307101bccce`，完整钉住记录摘要为 `a4bbdcff35a53f800b837cb4601947aed26dc1a96b3b07b520b1102016f67764`；测试缓存和临时目录已清理。该计划仍只具有“等待结构门”的抽象资格，不是方法资格数据或训练结果。

上述集成修复后的最终相邻回归运行 81 项：76 项通过、5 项按既定边界跳过、0 失败。最终字节全仓回归运行 849 项，用时 1,554.363 秒：835 项实际通过、14 项按既定边界跳过、0 失败、0 错误。冻结前只读重算仍为 49 个钉住文件，路径列表规范摘要保持 `efd87c51567d3cde6f5d3b86edeeb97da8897447362c962d97ea1307101bccce`，总字节 `4,954,664`，完整钉住记录规范摘要更新为 `5fb1cc65c7499bfe8d39bda0e7a8d171e7952789f7ea4b701bcfcb5cc36e87dc`；内存模拟的冻结政策规范自摘要为 `9088de1437e795bf348191da810adc98286756bd965964101e94535774ecece2` 并通过正式验证器。此处仍只是运行前证据；政策、随机权威和结构门尚未实际创建。

## 8. 正式前置结构门失效结论

政策随后按上述摘要正式冻结，一次性随机权威创建后只输出承诺、不输出十四个私钥值。唯一结构门正常完成训练／开发各 500 世界矩阵冻结、四个固定模型和 9,999 次世界重抽样，但五项硬门全部失败。卖家槽视图接近随机；噪声可见视图的最大单特征对称曲线下面积为 `0.768156`，四模型最大值为 `0.836099`，最大平均精确率提升为 `0.114283`，两项百分之九十五上界分别为 `0.837003` 和 `0.116693`，均远超冻结门限。

因此 R2 在文本和方法资格根产生以前即取得有效 `DATASET_INVALIDATED_PREBUILD_STRUCTURE_GATE`。结果文件为 83,554 字节／`e06225ba78e20003bbd4fca25fc6be659fa6560aa1405a5017e078e9b0f6c6d4`，规范自摘要 `573f29a2bb5d7202803f2a6c5f29cb18bc2d0c90a50cc5e887f5c242a44f100f`。审核甲乙真值读取、自然文本、方法根、训练和模型结果仍全部为零。详细记录见 `docs/STEP28_V13_V1_13_V9_3_R2_PREBUILD_STRUCTURE_GATE_DATASET_INVALIDATION_20260827.zh.md`。
