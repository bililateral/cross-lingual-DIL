# Step28-v13 v1.12 可见文本捷径预检结果

> **历史关闭声明（2026-08-30）**：该预检通过不构成数据质量通过。V1.12 后续因历史商品文档精确重复永久失败，专属运行代码已清理；不得从本文恢复运行资格。

日期：2026-08-09
运行编号：`v13_training_ready_v1_12_cleanroom_20260803`

## 1. 结论

本轮无正式密钥设计预检通过，规范状态为：

`PASS_DESIGN_TEXT_SHORTCUT_PREFLIGHT_NO_FORMAL_AUTHORIZATION`

它证明当前 v1.12 设计在冻结的攻击范围内，已把“同一控制者共享模板、标点和排版风格”造成的明显可见文本捷径压到接近随机水平，并且生产解析器、身份删除、卖家画像、拆分隔离和逐世界重算链条能够闭合。

它不证明正式中文合成数据集已经生成，不证明身份迁移模型有效，也不授权正式密钥、正式数据、审核真值解封或模型训练。正式数据行仍为 0。

## 2. 规范运行与证据完整性

第一次运行在两个登记门通过后、原始训练世界进度 `100/500` 时被外部会话中断。该次没有最终回执，也没有持久化世界或矩阵。中断边界由 `external_interruption_receipt.json` 固定；按事先审核通过的一次性精确替代纪律，本轮使用相同运行编号、源码、策略、合同、设计主密钥、阈值、参数和运行环境从第零阶段完整重算。替代资格在启动时即已消耗，v1.12 不得再运行第三次。

最终回执：

- 路径：`reports/step28_synthetic_chinese_dataset/design_preflights/v1_12_cleanroom_20260803/text_shortcut_preflight_receipt.json`
- 文件大小：105,744 字节
- 文件 SHA-256：`e371a460d65560f06e071a6961bf67c22d1f39a5e5e3a37431af5b1c88dd152e`
- 规范自哈希：`ffe86c9ad07f77609c3463e831108e73504493b591c73815359c0419e58c4a20`
- 来源文件闭包规范哈希：`75c3e940a873a2674300337d0d3f1042fa41626e7e0aa2e97f640744175da2a3`

最终来源闭包与中断前冻结闭包完全一致，因此本轮没有根据第一次运行的中间结果修改实现。

## 3. 登记门结果

六个阶段全部通过：反事实快速/完整路径一致、原始快速/完整路径一致、错排关系空检验、可见文本捷径攻击、原始文本描述性隔离，以及逐世界设计审计。

错排关系空检验中，开发集十项关系的最大直接对称接收者操作特征曲线下面积为 `0.504168`，低于 `0.52`；9,999 次世界重抽样的族最大值百分之九十五上界为 `0.508505`，低于 `0.53`。重抽样抽签哈希为 `dfad1a38ab9ead6befc977af24e545d49e1de8e49cf4f633c12c21f3cfc059dd`。

反事实机制中性文本的开发集结果为：

| 检查项 | 实际值 | 冻结上限 |
| --- | ---: | ---: |
| 最大单特征对称曲线下面积 | 0.508517 | 0.52 |
| 六个攻击模型最大对称曲线下面积 | 0.504527 | 0.53 |
| 最大平均精确率提升 | 0.000113 | 0.01 |
| 重抽样曲线下面积上界 | 0.509560 | 0.53 |
| 重抽样平均精确率提升上界 | 0.001052 | 0.015 |

可见文本重抽样抽签哈希为 `755f6dfb7a5c1d12842e9702b7a3949ebcd0339fa10f716e3fa0dee7e7a49a51`。重抽样次数为 9,999，模型没有在每次重抽样内重新拟合，符合冻结合同。

三个特征视图各自完成五折世界隔离：每折用 400 个训练世界、148,800 对拟合，在另 100 个训练世界、37,200 对上形成折外分数；开发集只用于硬门。所有逻辑回归均在 2 次迭代内收敛，无收敛警告，归一化梯度远低于冻结容差。

## 4. 原始文本的科研解释

原始描述性分支每个拆分包含 500 个世界、189,000 对，其中正例 10,000、精确克隆负例 1,000、高语义相似负例 2,000、其他负例 176,000。训练集最强字符三元组描述特征的曲线下面积为 `0.548025`，开发集为 `0.544081`；开发集最高平均精确率约 `0.061770`，随机基线为 `0.052910`。

这支持一个有限结论：原始合成文本确有弱而稳定的可见文本及风格相关信号；控制者盲的风格错排后，冻结攻击范围内的信号降到近随机。不得扩大为“全部作者信号都由风格造成”，也不得外推为真实中文地下市场没有作者风格信号，因为两分支的候选对集合并不完全相同，且没有登记原始与反事实差值的正式统计检验。

## 5. 逐行和隔离审计

训练与开发各审计 500 个世界。每个拆分的反事实机制中性候选为 186,000 对、正例 10,000；原始候选为 189,000 对；卖家画像为 14,000 个。训练和开发实际身份删除商品分别为 50,656 和 50,197 条。

世界编号、卖家编号、商品编号、规范卖家对编号、控制者编号、商品文档哈希、卖家文档哈希和五字段卖家记录哈希的跨拆分交集全部为 0；可见禁止残留为 0。原始设计世界、标签矩阵和攻击矩阵均未持久化。

## 6. 测试、外部复核与下一步

本轮针对性回归为 39 项通过、0 失败。完整仓库共发现 464 项：456 项通过、7 项按既有声明跳过、1 项 Step28-v12.1 guarded sync manifest 历史测试失败。该失败只因旧清单固定了 2026-07-20 的动态进度文档字节；预运行豁免回执已精确绑定旧清单、当前文档及测试字节。它不是本轮实现回归，不得改写旧历史清单或回滚当前进度文档来制造全绿。

网页端 GPT-5.6 Sol xhigh 对最终回执逐项独立复核，结论为阻断级 0、高级 0、中级 0、低级 0，并给出 `RESULT RECEIPT GO`。该外部意见是附加复核，不替代本地哈希和数值验证。

下一步只能是依据本结果讨论并冻结后续正式授权闭包。正式密钥仪式、四拆分正式生成、审核真值解封及 M0/M1/M2/M3 训练均需另行明确授权；本结果本身不能启动这些动作。
