# 2026-08-30 工作区清理与当前状态审计

状态：`CLEANUP_APPLIED_CURRENT_REGRESSION_PASSED`

## 当前科研边界

正式中文合成数据集已经存在。V9.4.1 尝试一发布训练、开发、审核甲、审核乙各 500 个世界，共 2,000 个世界、56,000 个卖家、198,000 件商品和 756,000 个卖家对。正式质量结果为 `PASSED_FORMAL_500X4_ROOT_QUALITY_TRAINING_QUALIFIED`，结果文件 SHA-256 为 `6b3ffe53e5404b918890bcc9bd433932fda91aa9e7a7ac2ee6b7529f310a5780`。

这只表示数据通过训练资格门，不表示已经获准训练。当前机器边界仍是 `training_qualified=true`、`m0_m1_m2_m3_training_authorized=false`。在无标签 Linux 兼容性重放、四拆分公开投影、训练合同冻结和一次性授权完成前，不得读取训练／开发标签来拟合模型，也不得打开审核甲乙真值。

## 本次确定删除

### V1.12 封闭运行代码

V1.12 已因完整可见商品文档历史精确重复而永久失败，且终止文档明确规定不得再次调用其正式执行器。本次删除与当前 V9.4.1 无运行依赖的 27 个专属文件：

- `schema/` 下 4 份 V1.12 策略／预锁；
- `scripts/` 下 18 份 V1.12 生成、审计、授权和清理程序；
- `tests/` 下 5 份 V1.12 合同测试，共 107 个测试用例。

删除前总大小为 859,526 字节。按路径、大小和逐文件 SHA-256 排序后的规范清单摘要为 `222082f11ba2fc84843a247818053bc93920a15452c7b66be80190198a177e82`。精确字节由 Git 历史保留；失败终态、清理回执、终止谱系和哈希排除归档继续保留。

### 早期设计预检矩阵复核后保留

曾尝试删除 `reports/step28_synthetic_chinese_dataset/design_preflights/` 下 21 个 `*.checkpoint.shortcut_inputs.npz`。清理后完整回归在 `TrainingReadyBuilderContracts` 的类级准备阶段正确失败：当前 `training_ready` 活体合同仍会从登记回执按摘要读取 `shortcut_arrays`，缺失时在标签读取前拒绝。该删除因此在提交前被判定为错误清理，21 个文件已从 Git 精确恢复。

恢复后总大小为 73,979,098 字节，规范清单摘要仍为 `1db70493d06120f327797e17021256d41ea1043c427a9a27baac81a10e1bc04e`。它们虽然不是正式数据或模型，却仍是活体合同的固定输入，必须保留。第一次清理回归的该项错误不构成科研实验结果，最终回归必须在恢复后从头运行。

## 明确保留

- V9.4.1 正式四拆分公开／私有根及质量结果；
- V9.4 方法资格根和质量结果；
- 当前冻结 M0 及其 Step7-v4.1／v4.2 证据；
- 成功 v1.2 历史根，因为当前碰撞排除仍依赖其历史字节；
- V1.12 的 50,991,342 字节哈希排除归档，因为 V9.4.1 仍用它拒绝历史碰撞；
- Step28-v12 修正复现实验及 v12.1 分离应用；v6-v11 小型／历史输入是该结果的冻结复现依赖；
- 所有历史结论文档、小型终态、消费回执、清理回执和不可逆排除摘要；
- 被现行政策按精确 SHA-256 钉住的历史草案或共享实现。
- 当前 `training_ready` 合同仍按摘要读取的 21 个设计预检矩阵检查点。

历史文档不会因结论过时而删除；它们必须标注由更新状态覆盖。`docs/CURRENT_EXPERIMENT_DESIGN.md` 和 `docs/PROJECT_PROGRESS.md` 的文件开头将补充 2026-08-30 权威状态，避免读者误把 8 月 1 日／7 月 20 日的段落当成当前状态。

## 删除后验证

删除后已建立新的第二版当前回归合同，不改写 2026-08-30 已冻结的第一版回归字节。V9.1 的 34 项历史冻结回归继续引用原提交隔离结果。清理后当前工作树共发现 919 项，精确移出 34 项历史测试后计划 885 项；既有类级封闭边界逐项登记 20 项未启动测试，实际启动 865 项，其中 858 项正常通过、7 项逐测试跳过，另有 1 条类级准备跳过事件，失败和错误均为 0。运行用时 2,186.983 秒，结果绑定提交 `cabcf106579d64b8ca6020bf0678ac8285e9a13e`。

第二版结果文件为 `reports/step28_regression_partition/v2_post_cleanup_20260830/current_active_post_cleanup_regression.json`，共 6,171 字节，SHA-256 为 `e05ebc8a318c83d255c243543eeea56074f77e8fd02190b7e01929a63abebf80`，规范自哈希为 `7d96acbfba9278839d1dc16c80078f8af70f0e9755c195eee6d61d8375e182ff`。结果仍明确保持训练和审核真值授权为假。
