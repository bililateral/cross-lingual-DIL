# Step28-v13 v1.12 外部中断与一次性等价替代运行记录

> **历史关闭声明（2026-08-30）**：本文只记录 V1.12 当时的中断处置，不授权任何当前运行。V1.12 后来永久质量失败，专属运行代码已清理；完整终态见 `docs/STEP28_V13_V1_12_TERMINAL_FAILURE_AND_CLEANUP_20260809.zh.md`。

## 事件边界

运行编号 `v13_training_ready_v1_12_cleanroom_20260803` 的首次 500＋500 设计级文本捷径预检于 2026 年 8 月 8 日启动。Codex 会话被外部终止时，运行进程同时退出；程序没有捕获到科研异常，也没有生成最终回执。

标准输出最后进入 `original_train_500_worlds`，原始训练世界完成 100/500。按冻结运行器的控制流，这只能发生在错排零假设门和可见文本攻击门均返回通过之后；日志没有暴露具体指标。标准错误只有任务并行库探测物理核心失败后回退逻辑核心的警告，没有回溯。中断后不存在 Python 进程、世界、矩阵、检查点或最终回执。

## 证据与结论边界

小型中断回执位于 `reports/step28_synthetic_chinese_dataset/design_preflights/v1_12_cleanroom_20260803/external_interruption_receipt.json`，规范自散列为 `503291c8749de2a4b8e2668f45dad6de7e0607c760fedebb0d7ae0fed5d396a5`。它绑定两份临时日志的大小和散列、全部冻结源闭包、运行环境以及正式授权全关闭状态。

本事件分类为外部基础设施中断，不是科研门失败，也不是 Step28 成功。不得把“两个门沿控制流通过”写成完整预检结论，因为原始世界、跨拆分隔离和逐世界最终审计没有完成。

## 一次性替代纪律

网页端独立审查结论为 `EXACT-REPLACEMENT-RERUN GO`。只允许一次完全等价的替代运行：同一运行编号、最终回执路径、代码、策略、合同、设计主草案、设计主密钥、数据、公开域、阈值、参数和运行环境；必须从阶段零完整重算，禁止检查点恢复和复用任何中间结果。

替代资格在替代进程启动时立即消耗。若替代运行再次中断或失败，v1.12 不得进行第三次尝试；必须记录失败边界、清除失败产物，并在新版本重新冻结后才能继续。

中断回执验证通过后，原标准输出和标准错误临时日志必须删除。后续只保留本记录和小型中断回执，不保留中间世界、矩阵、缓存或临时工作区。正式密钥、正式数据生成、模型训练和审核真值解封仍全部未授权。
