# Step28-v13 v1.13 V9.4 构建后质量审计尝试一失效记录

日期：2026-08-28
机器曾写状态：`PASSED_METHOD_ROOT_QUALITY`
纠正后的科研状态：`AUDITOR_DESIGN_INCOMPLETE_NO_DATASET_CONCLUSION`

## 1. 失败阶段与根因

尝试一在 `counterfactual_text_shortcut_gate` 阶段只训练了一个基于完整卖家文本字符三元组余弦的逻辑回归。它没有执行冻结质量审计 C 修订件要求的七个视图、两类模型、全部 346 个单特征和 9,999 次家族最大值世界重抽样。因此其 `training_qualified=true` 与“通过”状态越过了权威合同，必须撤销。

这不是数据硬门失败，也不能证明数据合格。V9.4 方法资格数据根保持 `BUILT_NOT_TRAINING_QUALIFIED`，必须由新审计尝试在新代码、新结果路径下完整复核。

## 2. 已形成但不得晋级的诊断

该不完整结果记录了：控制者盲风格错排后的单一探针对称接收者操作特征曲线下面积约 `0.500139`、平均精确率提升约 `0.000142`；原始文本单一描述探针接收者操作特征曲线下面积约 `0.606579`；三十三维身份历史正向诊断接收者操作特征曲线下面积约 `0.905213`、平均精确率约 `0.812991`。这些数值只可说明已执行的局部诊断，没有数据资格效力。

原结果文件为 4,817 字节，SHA-256 为 `e334ef0a1efef616f7861996c7be7c5d8d9723b495eed0800eb7b0a3aa2d69df`，其规范自哈希为 `c084f1748409e22c760adaae5a0caaf47c5e5607dbff56dd1a88f90c9716bb84`，绑定根清单规范自哈希 `558b8e80f3af2911a1d79334a416a7083b287df929c7241aca9fb84fe2eb7a37`。

## 3. 结论边界与清理

- 不得引用尝试一作为质量通过、训练资格或模型结果；
- 不得复用其结果目录；
- 数据根没有被证明失败，故不得删除或重生成；
- 失效结果载荷和仅实现该不完整审计的脚本从工作树删除，精确摘要和本记录保留；
- 后继尝试必须先冻结完整七视图十四模型口径，再读取训练／开发标签，审核甲乙真值继续保持未读。
