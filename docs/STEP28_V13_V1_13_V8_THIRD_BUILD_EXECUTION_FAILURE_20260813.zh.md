# Step28-v13 v1.13 v8 第三次设计构建执行失败记录

日期：2026-08-13
状态：`EXECUTION_FAILURE_NO_DATA_OR_QUALITY_CONCLUSION`

## 运行权威与许可

运行绑定提交 `38b4672843e070ac0fa9a25e331cd911093edcc9`，本地与远端一致。
网页端外审对十个精确附件给出阻断级 0、高级 0，并以“允许同一权威重建v8设计级数据”
收尾。首次调用因本地执行工具误设一秒外层超时，在构建器建立临时事务根之前被终止；
没有世界事件、文件、候选、行或残留进程。补充外审据此只允许重新启动一次。两份审查
原文保存在 `reports/step28_v13_v1_13_scientific_builder/external_review/`。

## 第三次执行失败

重新启动使用与获准字节完全相同的命令：

```powershell
python -B scripts\step28_v13_v1_13_scientific_dataset_builder_v8.py --mode design_preflight
```

构建器顺序完成训练世界 0 至 158，共 159 个世界。在训练世界 159、全局构建位置
160 处，32 个候选全部发生精确文档碰撞并关闭失败：

```text
All 32 exact-document candidates collided
```

临时根 `.design_preflight_v8_20260812.building` 已由异常路径删除，最终根
`design_preflight_v8_20260812` 从未发布。没有设计数据、质量结果、正式种子、
模型或指标；本次不是数据质量失败，不能据此判断中文合成数据是否合格。

提交 `38b4672` 的构建实现字节不得再次运行。后继诊断只能顺序重放已经暴露的训练
世界 0 至 159，不得读取世界 160 以后或其他拆分。不得换盐、改变规模、增加候选上限、
删除历史排除、把近似文本当作碰撞、按已知世界／商品／摘要写生产特例，或让标签、
控制者、模型分数和质量探针进入候选生成。

## 已暴露前缀的精确诊断

诊断只从训练世界 0 顺序重放到已经暴露的训练世界 159，使用真实累计内存登记表，
没有读取世界 160 以后、开发集或审核拆分，也没有写出数据文件。进入目标世界前，159
个已接受世界的候选序号分布为 `0:144, 1:6, 2:5, 3:2, 4:1, 7:1`；商品、卖家、
身份登记数分别为 16,034／4,452／13,356，规范摘要分别为：

- 商品：`c3a3ffe3187837c3984205d8c2a4d2e67820d43b598efb9a2db16de80c2f2162`
- 卖家：`4d7c6daac239a6b28813f58ac142cae4ca974033af98db0c9fc717c6d72f549c`
- 身份：`ddfecefdf1157678a5b4136ec04944ba8faba14c8a840c730d08c4b3983484af`

目标世界 32 个候选全部且仅命中 `historical_item_document`。碰撞商品在所有候选中
固定为软件工具类“文件整理工具”；配送、服务、标题模板、描述模板、基础描述和噪声均
只有一种状态，标题有两份精确字节，属性只有“标准版／组合版”两种。历史碰撞并集为
两份文档、共同交集为空，并集规范摘要为
`ec9ba637bdb97d31083a89df00ea1f8004b97ae299a10ac75b51deffd5869943`。
32 个整世界自然输出仍有 29 种，说明失败来自该目标商品的二状态闭包，并非整个候选
世界没有变化。

两条扩展路线被生产硬门正确拒绝，且从未进入世界：把“标准版／组合版／轻量版／更新版”
合成四元轨道会破坏跨风格精确相等关系；加入“可选配色”会改变可见字符结构。

## 最小后继修复边界

允许进入实现的候选只把原安全库单例“多规格”追加到原二元轨道，形成同一“版本／规格
内容属性槽位”的三状态可接受替代轨道：`标准版 → 组合版 → 多规格`。三者不是严格同义词，
不得如此表述。候选键、候选上限、独立消息域、摘要截取、轨道原有顺序前缀、其他候选域、
共享随机流、历史排除和全部生产硬门均不得改变。该全局双射不得读取世界序号、商品、碰撞
摘要、登记表、控制者、标签、模型或质量探针。

只在内存中改变该轨道成员并从训练世界 0 重放的可行性探针显示：训练世界 159 在候选 5
接受，候选 0 至 4 累计命中历史商品 5 次、当前数据商品 2 次，其他四类为 0；接受后 160
世界的商品／卖家／身份登记数为 16,138／4,480／13,440，规范摘要分别为
`ba90909a83566e1b31d5d49fcd8df28e881d2b2686f1a8b9861cb7486fdb3af2`、
`e4a6a0818162c9fa6f83343a34a07043098fdba9af24e90894b9d669744c285d`、
`e00b11e464e4638401353cc2a9e8bd240f1ebca75d21df38fe5d05f47424428a`。
这些数值只是候选证据，必须由冻结生产实现重新计算后才能成为测试金标准。

冻结候选随后完成成对因果回归。旧二态机制和新三态机制分别从空当前登记表开始，使用
同一权威、同一训练世界 0 至 159 顺序独立重放；没有复用另一机制的前缀登记表，也没有
手工注入两份目标摘要。旧二态精确复现上述 32 候选耗尽。新三态进入世界 159 前的登记
数仍为 16,034／4,452／13,356，但商品和卖家摘要分别变为
`c4d7fd0ec524824144f3e1e99cb5120bbb10bab50673e1d322379787f6e704ab`、
`ebe213a24c5fbf4b18261eb3e99d1278dda4b82a9afa611d571e938a12f3ebc5`；身份摘要仍为
`ddfecefdf1157678a5b4136ec04944ba8faba14c8a840c730d08c4b3983484af`。这说明前三类数量
相同，合法可见文档选择已变化，但身份结构没有变化。候选 0、3、4 各只命中历史商品；
候选 1、2 同时命中历史商品和当前数据商品；候选 5 无碰撞并接受。接受后的三类数量和
摘要与前述探针完全一致。成对回归 1／1 通过，用时 1,048.914 秒。

网页端纯科研设计审查给出阻断级 0、高级 0，最后一行“允许进入三状态属性轨道实现”。
回执为 20,198 字节／`6425b1e8f026a9988129d141fe692b0173e66573160deca5f3f6296bd61fa40d`，
保存在 `reports/step28_v13_v1_13_scientific_builder/external_review/`。该许可仅允许修改实现、
策略、测试和文档；不授权重新构建 1,004 世界。后继还须完成整个构建合同和相邻合同回归，
冻结精确字节并取得新的运行前审查。质量审计、正式 500×4、真值解封和 M0／M1／M2／M3
训练继续禁止。
