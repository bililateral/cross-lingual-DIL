# Step28-v13 v1.13 v8 第四次构建失败最小修复与重建许可

日期：2026-08-14
状态：`REVIEWED_GO_FOR_ONE_DESIGN_PREFLIGHT_REBUILD_ONLY`

## 修复边界

第四次设计构建在训练拆分序号 224 处失败：既有二状态属性轨道只能产生“轻量版”或“更新版”，32 个候选均只命中历史商品精确文档排除。后继修复只在候选专用模板末尾新增“通用版”，把该轨道扩为（轻量版，更新版，通用版）。基础模板、基础属性抽样、基础父世界、候选密钥域、候选次序、32 候选上限、历史排除和随机权威均未改变。

候选专用模板为 21,879 字节，SHA-256 为 `ff97b59f0d66d7a9d18a3b1e7d7db684e6bdcc1a7ae60ba0e6b7294528c8766a`。删除属性数组末尾的“通用版”后，其规范 JSON 与 21,860 字节的基础模板完全相同。候选生产链只在候选扩展精确等于（通用版）时加载该模板，并先核验固定路径、SHA-256 和规范差异；基础父世界仍使用原模板。

## 因果回归与本地验证

- 旧状态从空登记表顺序重放训练序号 0 至 224，精确复现序号 224 的 32 候选耗尽；前 224 个世界的候选分布、商品／卖家／身份登记数量及摘要均固定。
- 新状态从另一组空登记表顺序重放相同 225 个世界，序号 224 在候选 0 无碰撞接受；8 条可见商品文本及 8 条私有渲染语法树均实际含“通用版”。
- 旧新共同前缀的结构父投影序列和 33 项身份历史特征序列逐项相等。
- 训练序号 0 至 159 的上一轮因果回归仍闭合。
- v8 完整构建合同 38／38 通过，用时 2,553.143 秒；两次四拆分小型烟雾构建内容一致。没有产生或保留设计发布根、正式种子、正式行、模型或指标。

## 独立网页审查

网页端 GPT-5.6 Sol Pro 对九个精确附件复算后给出：阻断级 0、高级 0、中等级 0、低等级 3，并以“允许在同一既有 v8 设计权威下重建 1004 世界设计级数据”收尾。完整回执：

`reports/step28_v13_v1_13_scientific_builder/external_review/step28_v13_v1_13_v8_fourth_repair_external_review_go_20260814.json`

回执为 20,983 字节，SHA-256 为 `a2b0767bd5c689594e1be362d94550ad8a18378759eed3e1f5198fb847741307`，对话地址为 `https://chatgpt.com/c/6a7e4662-0450-83ea-bfc6-50d623bdb570`。

三项低等级建议是：补充第二条三状态轨道的全上下文机器穷举；把序号 224 的旧态逐候选碰撞向量写入合同；把生产特例自动扫描扩展到科学世界、公共合同和数据集构建器。独立审查确认这些是测试钉住增强，不是当前生产缺陷。为保持本次许可绑定的精确字节，重建前不修改；必须在正式 500×4 冻结前完成并重新审查。

## 唯一许可与结论边界

只允许以当前精确策略和六项实现钉住，从空登记表按训练、开发、审核甲、审核乙顺序重建一次固定 500／500／2／2、共 1,004 世界的设计级数据。不得复用第四次失败的未发布前缀，不得改变输出根、候选上限、历史排除、属性轨道或随机权威。

构建成功仍只能得到 `PASS_DESIGN_BUILD_NOT_TRAINING_QUALIFIED`。本许可不等于数据质量通过，不授权正式 500×4、真值解封、M0／M1／M2／M3 训练或任何正式指标。
