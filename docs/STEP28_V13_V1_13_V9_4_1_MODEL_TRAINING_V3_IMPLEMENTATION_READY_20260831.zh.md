# Step28-v13 v1.13 V9.4.1 模型训练 V3 实现就绪记录

日期：2026-08-31

## 结论

M0、五个 M1、M2、M3-base、M3-joint 的训练数值核心、确认评价器、盲评阶段协议和完整指标已经按 V3 合同实现。当前状态仅为
`IMPLEMENTATION_AND_CURRENT_REGRESSION_READY`：没有执行四拆分公开投影，没有读取训练／开发监督，没有训练模型，没有生成正式审核预测，审核甲乙真值读取均为零。

## 冻结实现

- 合同：6,480 字节，SHA-256 `4d1a34dd19c145add2a63e01c76596d142d2538c3bc1ec5f9f1ce69dc3fa8934`；
- 机器政策：7,878 字节，SHA-256 `5ae871ec70b80429bf20e7d3c2bf90ed42e75e8285bebdbf2f7fdac62e6a58da`，规范自哈希 `5e26b6c5fd6fea068c5a766ddb94302248f4db4d3ec3ea8d180f60334fa8b4bb`；
- 训练核心：50,815 字节，SHA-256 `e483ce1977cc8df595f806751fbe791dac1609bbe30e59441ab323eec50270d5`；
- 确认评价器：18,845 字节，SHA-256 `5d6075a81dd6593622fded88eff3b4ae932df8df006912636155aabffabf7556`；
- 盲评协议：5,103 字节，SHA-256 `08fcf15fd8af3b8dd44c6878373492ffd614eaf03e9b21a44ed4561a65d1782a`；
- V3 合同测试：20,202 字节，SHA-256 `2f02e1b7c8eb10b65ecb1a407aaf00907755acdd3fcfedc2b32e5821d99128aa`。

实现强制五个 M1 与 M2 共用正确训练折估计的无标签变换和同一二范数惩罚选择；M1 只在拟合行错配身份历史，折外、开发和审核推理均使用正确可见行。M3 固定八组轻量梯度提升机参数，以五折合并折外平均精确率选型。审核阈值必须逐模型精确等于开发冻结阈值。

## 指标与盲序

逐模型报告平均精确率、梯形精确率召回率曲线下面积、接收者操作特征曲线下面积、精确率、召回率、F1、特异度、平衡准确率、马修斯相关系数、布里尔分数、对数损失、假阳性率百分之一处召回率、原始与世界等权混淆矩阵，以及平均平均精确率、平均倒数排名、Recall@1/3/5/10 和 NDCG@1/3/5/10。9,999 次区间使用同一批完整世界重抽样，禁止逐行抽样或先平均五份 M1 概率。

阶段顺序已统一为：冻结实现；当前回归、提交、推送和独立复核；一次性公开投影；之后才可能单独授权训练／开发监督；先冻结审核甲盲预测再开甲；甲通过后才冻结乙盲预测再开乙。当前所有正式权限均为假。

## 验证结果

V3 直接测试 19／19、相邻英语与公开投影合同 89／89、真实轻量梯度提升机八网格五折小型集成、解析梯度中央差分和指标随机对照均通过。网页端 GPT-5.6 Sol Xhigh 对初版六附件给出阻断／严重／一般 `0／0／0`；阶段顺序修订后又独立复算新合同、政策、规范自哈希和训练核心，仍为 `0／0／0`。网页端未收到更新后的测试附件，因此测试结论只以本地机器结果为准，不扩大其审查范围。网页审查后仅机械删除了合同日期行末两个空格；随后的合同、政策、核心与测试摘要均已在本地重新闭合，不把网页端的旧字节摘要误报为终版审查。

最终当前有效回归发现 971 项，历史／前代分区 37 项，当前调度 934 项；20 项属于冻结类级未启动边界，实际运行 914 项，正常成功 907 项、登记跳过事件 8 条、失败 0、错误 0、意外成功 0。结果文件 7,030 字节，SHA-256 `4909db2966acc4ada53aee8179d44c9889c93ffa320bf06b56c435a5a2a98271`，规范自哈希 `a24ff250410e60915edb409496db4dab61f447c5939cd8121853625dff25dd4b`，用时 2,166.094 秒。

回归结果中的仓库提交是本轮父提交 `6dbae22d7929aa5a3638d78c79daa0e14538c6f0`，不得误述为新实现提交。后继公开投影授权必须在本记录随新提交推送后，重新钉住该精确提交、上述实现文件及其摘要。

## 下一步唯一允许动作

提交并推送本里程碑后，只可另行构造并审查“一次性四拆分公开投影授权”。公开投影仍不得读取任何标签、控制者、成员关系、检索相关性或审核真值。公开矩阵冻结并复核以前，不得申请训练权限；任何模型结果目前都不存在。
