# Step28-v13 v1.13 v8 第四次设计构建执行失败记录

日期：2026-08-13
状态：`EXECUTION_FAILURE_NO_DATA_OR_QUALITY_CONCLUSION`

## 精确运行边界

三状态属性修复提交 `90b0391d80d1784f70394ba7128cb2f4a696f1c5` 的本地构建合同
36／36、相邻四组合同 114／114，共 150／150 通过。网页端 GPT-5.6 Sol Pro 对九个
精确附件独立复算哈希后给出阻断级 0、高级 0，并以“允许同一权威重建v8设计级数据”
收尾。审查回执保存在
`reports/step28_v13_v1_13_scientific_builder/external_review/`，大小 17,140 字节，
SHA-256 为 `bced20308688cd434f3ed32ddd12fdc6483fb5383988575818bbee0a11be4a32`。

运行前临时检出上述精确提交；最终根和临时根均不存在，入口策略、固定规模、单次权威和
渲染器绑定合同 3／3 通过。唯一执行命令为：

```powershell
python -B scripts\step28_v13_v1_13_scientific_dataset_builder_v8.py --mode design_preflight
```

策略文件为 10,814 字节／`d9b87361d853dbca41491660cb9442f97e3cbf5665b5a6ab54ca4194ce237ccf`，
规范自哈希为 `dd7a5b0ebe3790b42b88b3d459af725cb14e217699eaa3c0ef5d5dd87b49d1e4`。

## 第四次执行失败

构建器顺序完成训练世界 0 至 223，共 224 个世界。在训练世界 224、全局构建位置 225
处，32 个候选全部发生精确文档碰撞并关闭失败：

```text
All 32 exact-document candidates collided
```

进程退出码为 1，用时 791.8 秒。异常路径删除了临时根
`.design_preflight_v8_20260812.building`，最终根 `design_preflight_v8_20260812` 从未发布；
退出后两者均不存在。外层执行工具返回失败后仍发现一个与本轮启动时间一致的 Python
进程（进程号 14136）；检查时它没有对应的临时根或最终根，随后只终止该精确进程，复核
确认进程和两类输出根均不存在。没有恢复、拼接或重跑构建，也没有设计数据、质量结果、
正式种子、正式行、模型或指标。本次是构建执行失败，不是数据质量失败。

## 不复用与后继边界

提交 `90b0391` 的构建实现字节永久不得再次运行。不得把已完成的 224 个世界作为可恢复
数据；它们没有发布且已经由事务清理。不得换盐、改变固定规模、增加候选上限、删除历史
排除、放宽精确碰撞、加入人工文本标记，或按已知世界、商品、摘要写生产特例。

后继诊断只能从训练世界 0 顺序重放到已经暴露的世界 224，携带真实累计商品、卖家和身份
登记表；不得读取训练世界 225 以后、开发拆分或审核拆分。先记录目标 32 个候选的六类
精确碰撞、交集摘要和可见字段状态，再判断根因。任何后继实现都必须保持标签、控制者、
模型分数和质量探针隔离，并重新完成因果回归、完整合同与独立运行前审查。质量审计、正式
500×4、真值解封和 M0／M1／M2／M3 训练继续禁止。
