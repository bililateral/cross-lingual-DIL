# Step28-v13 v1.13 V9.3-R1 有界残差披露与结构捷径门修订

日期：2026-08-27  
状态：`PERMANENTLY_CLOSED_DEVELOPMENT_RESIDUAL_22_NO_PLAN_NO_STRUCTURE_RESULT_NO_METHOD_ROOT_NO_TRUTH_NO_TRAINING`

## 1. 修订目的与历史边界

V9.3 原合同要求训练和开发各自的 5,324 个登记负例计数单元全部落入理论最均衡整数区间。训练固定轨迹能够经全量重建得到 `45→38→37→36→28→24→20`，但 V10 至 V17 的多种独热、交换闭包和显式候选列表示均未形成严格零违例计划。V17 只证明“永久累积完整补偿批次”的表示在冻结预算内未找到零解，不证明合同不可满足。

本修订永久停止继续求解 `20→0`。旧 V9.3 严格路线、V10 至 V17 的失败事实和“不具有计划／数据资格”的结论保持不变；不得把 20 点解释为旧合同通过、理论配平完成或残差无害。本修订是新的版本化后继协议，不覆盖旧合同，不复用旧状态名、输出路径或失败版本代码。

科研主线不变：先构建并审计 1,004 世界方法资格数据；只有完整质量通过后才可进入正式数据，更不得提前训练 M0／M1／M2／M3。

## 2. 为什么允许止损

这 20 个单位偏差不是错标签、身份关系错误或文本错误。它们只描述 500 个训练世界、6,000 个登记难例端点在卖家槽、噪声槽、方向、角色和控制者规模统计上的少量非理想暴露。它们可能形成弱结构捷径，但不会改变“同控制者为正、异控制者为负”的标签公式，也不会改变身份历史、作者风格或文本机制。

理论精确配平是模型无关保证；冻结结构探针只是有限模型族的否证检验，二者不能等价。本修订的允许结论只能是：

> 在一次冻结的确定性终态上，预登记结构探针未检出超过冻结门限的可学习捷径。

禁止写成“实现了理论配平”“残差无害”或“不存在结构捷径”。

## 3. 版本、路径和一次性边界

- 计划版本：`2026-08-27-step28-v13-v1-13-registered-negative-plan-v9-3-r1-bounded-residual`；
- 唯一计划根：`reports/step28_v13_v1_13_balanced_schedule_v9_3/registered_negative_bounded_preflight_r1_20260827`；
- 唯一结构门结果：`reports/step28_v13_v1_13_balanced_schedule_v9_3/registered_negative_structure_gate_r1_20260827`；
- 旧 `PASS_ABSTRACT_REGISTERED_NEGATIVE_PLAN_ONLY_NOT_DATA_OR_TRAINING_QUALIFIED` 状态不得用于本修订；
- 新计划状态只能是 `PASS_DETERMINISTIC_BOUNDED_RESIDUAL_PLAN_PENDING_STRUCTURE_GATE`；
- 结构门全部通过后只能升级为 `PASS_PREBUILD_STRUCTURE_GATE_NOT_METHOD_ROOT_OR_TRAINING_QUALIFIED`。

训练或开发任一计划生成失败、机械硬门失败或结构门失败，本修订立即永久关闭。不得换公开种子、追加迭代、改变候选、删除特征、改模型、调阈值或重抽样后重试。

## 4. 冻结的确定性终态

训练和开发继续使用既有公开设计常数 `281320260826` 和 `281320260828`、既有训练／开发配平计划、联合噪声签名 V2 及其钉住摘要。不得读取自然文本、标签文件、身份真值或模型结果。

终态算法固定为既有、已经重放验证的两阶段轨迹：

1. 精确执行 40,000,000 次退火和 15,000,000 次冷精修；
2. 进入既有粗修复器；每次提交只允许独立重建后严格降低完整平方目标和一范数违例；
3. 在既有、早于本修订设置的 `LOCAL_REPAIR_COARSE_MILP_THRESHOLD=20` 阶段边界首次满足时停止，不进入 V15 至 V17 残差定价；
4. 该数值只是固定旧算法阶段的输出边界，不是科研通过阈值。训练已知终态为 20；开发终态尚未知，禁止据其结果改变算法；
5. 若在既有最多 128 个粗修复轮次内不能到达该阶段边界，或任一求解器／重建机械失败，本修订关闭且无数据结论。

计划必须先落盘、重新打开、在新的搜索实例中清空 11 组数组并全量重建，然后才能冻结。结构矩阵或标签不得在计划冻结前创建。

## 5. 公开残差披露

训练和开发分别发布：

- 500×12 个逻辑卖家槽端点；
- 两份输入文件、构造器、验证器和发布器的路径、字节数与 SHA-256；
- 全部 5,324 个单元的有序记录：家族、索引、当前值、下界、上界、带符号偏差和绝对违例；
- 平方目标、一范数违例、非零单元数、逐家族分解和规范自哈希；
- 500 个世界的机械合法性汇总；
- 训练／开发计划字节不相同和全局重标拒绝证据。

不得只公布总残差或只公布失败家族。实际残差不设置根据训练 20 点新选择的通过阈值；它是完整诊断，不是资格结论。

## 6. 不得降级的机械硬门

每个世界仍必须严格满足：6 个登记负例、12 个端点互异、每对异控制者、卖家槽与噪声槽合法、四个具名角色合格谓词、语义资产精确重放和逻辑商品分层循环。计划版本、输入摘要、自哈希、顺序、拆分和世界数必须闭合。

`clone_source_eligible`、`clone_target_eligible`、`semantic_left_eligible` 和 `semantic_right_eligible` 必须物化为同一份 `role_eligible[world, position, seller_slot]` 权威，供搜索、计划验证和后续世界构建共同读取。即使当前 V2 使四个谓词都为真，也必须用角色不对称反例证明不会混用。

## 7. 前置结构捷径硬门

在任何自然文本、私有真值或方法资格根产生前，先冻结训练／开发的卖家槽矩阵和噪声槽／可见结构矩阵。列名、公式、维数、类型、顺序、空值约定、归一化、模型超参数、种子、训练拟合／开发过门角色、最大值聚合和 9,999 次世界重抽样规则必须沿用原 V9.3 合同及机器政策，不得依据实际非零单元增删列。

必须事前登记 11 个计数家族到结构矩阵列族的覆盖映射。计划、5,324 项计数、资格位图、矩阵和摘要全部冻结后，单一验证器才可各物化一次训练与开发抽象标签。计划不得在看见任何结构分数后返回修改。

四个模型继续分别运行，不拼接后隐藏来源：卖家槽矩阵与噪声槽／可见结构矩阵，各用固定二范数逻辑回归和深度 2 浅树。冻结硬门保持不变：

- 单特征最大对称 ROC-AUC 不超过 0.52；
- 家族最大对称 ROC-AUC 不超过 0.53；
- 家族最大 AP 提升不超过 0.01；
- 9,999 次世界重抽样的家族最大 ROC-AUC 与 AP 提升 95% 上界分别不超过 0.53 和 0.015。

任一机械门或统计门失败即关闭本修订。后续完整质量审计对同一结构门的重算只是独立实现复核，不是第二次统计机会；其余代码零出现、私有坐标非干预、碰撞、拆分隔离、反事实文本和完整证据门全部保留。

## 8. 标签、审核和训练边界

计划生成只使用抽象控制者关系来保证登记对为负例，不读取标签文件。训练／开发标签只可在计划、计数、矩阵和资格位图冻结后由窄验证器各物化一次。审核甲、审核乙的监督真值、控制者关系、qrels 和逐行标签继续保持未打开；只允许不需要真值的结构闭合。

本修订、抽象计划和前置结构门都不是方法资格数据、正式数据或训练资格。M0／M1／M2／M3 继续禁止，直到 1,004 世界方法资格根取得完整质量通过终态。

## 9. 运行前验证与失败纪律

运行前至少覆盖：固定输入／路径漂移、训练开发不同种子、重新打开全量重建、任一端点重复、同控制者对、角色谓词不对称、5,324 单元顺序／值／摘要漂移、结构矩阵缺列／多列／重排、标签提前读取、结构门失败后计划不可修改，以及旧 V10 至 V17 路径和状态不可复用。

失败运行先在交接和进度文档记录运行编号、阶段、根因、结论边界、关键摘要和不可复用承诺，再删除失败载荷、缓存、临时目录和只服务失败运行的代码。不得保留大型失败数据。

## 10. 审查记录

网页端 GPT-5.6 Sol Pro 在读取旧 V9.3 合同、当前构造器、验证器、训练配平计划和联合签名 V2 后，先提出世界块分解的最后求解方案；本地因实现量、训练／开发双拆分成本和有限预算不保证成功而未采纳。随后网页端仅审查本止损路线，结论为“有条件可接受”：它明确指出这是已知训练残差后的后验修订，但原有结构矩阵、模型、阈值和重抽样门均未根据 20 新设，因此只要版本隔离、固定终止、完整披露、标签时序和一次失败即关闭全部落实，就不构成循环论证。

该回复为 3,789 个 UTF-8 字节，SHA-256 为 `8b5e1554d7643d3a015e3457b20c8df98d6383492994d6cbdec7e74c47fa9e75`；会话为 `https://chatgpt.com/c/6a8f98b3-1704-83eb-aed3-335400327c15`。网页意见不是运行许可，本地仍须逐项实现和验证。

实现级审查随后指出三项真实阻断：结构门没有绑定完整五文件计划根及披露回执、探针结果模式没有对四个模型和 9,999 次重抽样逐项失败关闭、计数家族到有限结构投影的映射仍由运行时生成。本地已分别改为五文件逐字节绑定、精确结果模式验证和预登记投影摘要，并增加标签能力一次性消费、矩阵实际字节重算及运行后输入重新校验。一次后续回复因未收到更新后的计划验证器附件而误判本地接口仍旧；该意见经实际文件和运行中训练拆分重放交叉核对后没有作为代码缺陷采纳。

补齐实际计划验证器、结构门和聚焦测试附件后的窄范围依赖闭合审查结论为“阻断：0；高级：0；中级：0”，末行为“可进入政策冻结集成”。该回复正文为 146 个 UTF-8 字节，SHA-256 为 `6768600bd4aa1fd63be0c6d8e25be6e5eab27e954239b7c24899855b67b85ebb`。这只允许继续本地政策集成和测试，不替代计划生成、冻结政策、正式结构门或数据质量结论。

## 11. 唯一运行失败与关闭边界

唯一运行于 2026-08-27 10:54:48（亚洲／上海）启动。训练拆分按冻结轨迹到达一范数违例／平方目标／非零单元数 `20／20／20`，但该内存结果不是独立发布物。开发拆分完成 55,000,000 次固定搜索后为 `29`，粗修复依次得到 `29→25→24→23→22`；第 5 轮返回 `22→22`，未严格改善，触发 `RegisteredNegativeConstructionError`。失败阶段为开发拆分粗修复，根因是冻结 R1 实现只允许在不超过 20 时返回，而当前确定性候选求解在 22 处没有下降。

构建器按原子失败纪律删除了全部部分状态：正式五文件计划根、`.building`、结构门结果、方法资格根、文本、真值和模型均不存在，运行进程已经退出。因此 R1 没有计划资格、结构资格、数据结论或训练资格，不得重跑、修复或改口为通过。

失败时四个关键源文件的字节数／SHA-256 分别为：R1 发布器 `23,360／70d873bf9a74001baf3fb801db0bb2ddf1614eadb3cc054b08e40a5330dd979f`，残差审计器 `14,005／2d9d2af9018537d1cc7f16f81cf5e74d4b173cb9ff0ec9c23df01eb947cc1db9`，构造器 `92,176／2ba221ddf02ba7a66c21463cf09d6bf92a9c95ab3a48c7c8f8c1798b3c3a0192`，计划验证器 `47,988／a8e89807a5c423559704d8525cbf51c302717c3b84fc7b6a85c746700c5533b6`。用户随后明确决定不再优化 22 点残差，允许建立透明的新版本后继；该决定不追认 R1，也不能隐藏 22 点是看见结果后的放宽。
