# Step 28 V9.4.1 旧英文初始化微调 V1 结果与关闭记录

## 运行边界

运行编号为 `v9_4_1_english_initialized_labse_finetune_v1_20260902`。它先用旧 Step 7 的 401 条英文训练对端到端更新 LaBSE，再以相同中文 500 世界训练日程比较通用初始化与英文监督初始化。审核甲、审核乙标签和检索真值读取均为 0。

## 结果

英文训练拟合平均精确率为 `0.967273`，但这不是隔离验证。中文开发集上，通用初始化与英文初始化的平均精确率分别为 `0.117627` 和 `0.117037`，主差值为 `-0.000590`；梯形精确率—召回率曲线下面积差为 `-0.000623`。英文初始化只在接收者操作特征曲线下面积、平均准确率均值和平均倒数排名上出现 `+0.004258／+0.003092／+0.002927` 的微小单次差异，未通过预设的主要平均精确率继续门，不能解释为稳定迁移收益。

## 关闭原因与结论边界

本轮主指标已经给出阴性结果。随后对旧 401 对的源数据审查又发现，大量正例依赖完全相同标题、描述或模型可见身份串；旧验证集被反复查看，旧测试标签汇总也在 2026-09-03 探查中被误读。因此本轮最多记录为“旧污染英文监督没有产生主要中文开发增益”，不得作为干净英文身份迁移的正证据或最终负证据，也不得用其高英文训练拟合证明身份能力。

同步结果共 53 个文件、5,717,936,008 字节。`evaluation.json` 的 SHA-256 为 `707d509eb7013eb489d35b4da70faf5b2a525a20e186f0406dd392598db7de1b`，`manifest.json` 为 `c2cb2aeb191ec8ce0296fc2e8bd70637f5d950f4298a8dc3c6802d0ebf4b4f25`。完整大型结果目录已在本记录落盘后删除；不得恢复模型权重、概率或分类头用于后继实验。后继只使用从冻结原始英文来源重新构建并通过审计的 Step 7 V5 数据。
