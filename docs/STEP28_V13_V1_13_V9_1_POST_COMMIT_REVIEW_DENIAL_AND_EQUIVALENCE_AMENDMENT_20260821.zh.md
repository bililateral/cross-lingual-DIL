# Step28-v13 v1.13 V9.1 提交后复审否决与等价合同修订

日期：2026-08-21

状态：`IMPLEMENTATION_BLOCKED_NO_BUILD_AUTHORIZATION`

## 1. 被审字节与终态

被审提交为 `9ab0723bd3221744fe09fc66dec68cb55815c9b5`，树为 `ab0e36e99eb29e5a1f707090bbc2428a2ba449d1`。网页端 GPT-5.6 Sol Pro 在 `https://chatgpt.com/c/6a884949-f3bc-83ed-ad9a-6af761715399` 完整读取 15 个附件并审查 41 分 27 秒。回复正文为 10,534 个字符、19,289 个 UTF-8 字节，SHA-256 为 `cd11c2818632651b167ca33df73ddb540729e6bf3ca0a440f0e47c9bbaaa8023`；问题分级为阻断 1、高 0、中 1、低 0，最后一行精确为“不允许运行一次V9.1同一权威1004世界设计构建”。

本轮没有创建构建回执，没有构建世界，没有读取任何标签或审核真值，也没有生成模型或指标。提交 `9ab0723` 永久不得作为 V9.1 构建字节运行。

## 2. 接受的阻断项

原修复合同把每个结构行允许变化的对象写成三个顶层字段，但实际修复包含四个持久化 JSON 位置：

1. `/full_profile_sha256`；
2. `/masked_profile_sha256`；
3. `/neutral_profile_sha256`；
4. `/neutral_receipt/neutral_profile_sha256`。

当前物化器已把第四个位置改为实际写盘投影摘要，而等价验证器只从不变量投影中删除前三个顶层字段。因此第四个位置仍被要求等于失效 V9 的宽画像摘要，真实构建会确定性失败。这与最初“承诺对象和写盘对象不一致”属于同类对象边界错误。

## 3. 精确修订

本文件只修订 `STEP28_V13_V1_13_V9_1_MODEL_VIEW_COMMITMENT_REPAIR_CONTRACT_20260821.zh.md` 中“三个字段”对持久化路径数量的错误描述，其余同一随机权威、68 个不变文件、世界／标签／文本／阈值不变和禁止训练边界继续有效。

四个路径只承载三个语义摘要；中和摘要在外层和内层各登记一次。新紧凑承诺必须使用 JSON Pointer 形式逐路径登记，递归删除上述四个且仅四个路径后计算结构不变量流。外层三摘要和内层中和摘要必须分别保存旧值流承诺；新构建中内外中和摘要必须相等，并共同等于从实际写盘中和卖家画像逐世界重算的摘要。

结构行顶层 `version` 和 `neutral_receipt.version` 不属于允许变化路径。只读 Git LFS 历史对象确认二者旧值均为 `2026-08-14-step28-v13-v1-13-quality-channel-materializer-v9`。V9.1 源码版本只进入新政策、源码钉、拆分／根清单和等价回执；四个结构 JSONL 继续持久化旧精确版本值。任一版本变化必须关闭失败。

新紧凑承诺还必须保存每行递归映射键序列的流摘要，确保旧新结构模式和键序不变；世界 UID 行序摘要继续保留。不得恢复或运行旧 V9 代码，只允许从 Git LFS 历史对象只读提取四份旧结构文件并生成更精确的哈希承诺。

## 4. 必补直接测试

中等级问题是 52 项测试没有直接调用 `_validate_v9_1_persisted_equivalence`。修复后必须用四拆分小夹具直接覆盖：

1. 正常四路径机械修复通过；
2. 内外中和摘要不等失败；
3. 任一第五路径变化失败；
4. 顶层或内层版本变化失败；
5. 世界行序变化失败；
6. 68 个不变文件任一记录变化失败；
7. 实际写盘三种画像任一模型可见字段变化失败；
8. 允许路径集合被增删或改名失败；
9. 递归映射键序列变化失败。

只有补强承诺、代码、政策钉和测试，重新完成聚焦与全仓回归、形成新提交／树并通过新的提交后网页审查，才可另行申请一次 V9.1 同一权威设计构建。该许可仍不包含质量审计、正式生成、真值解封或 M0／M1／M2／M3 训练。
