# Step28-v13 v1.13 V9.4 正式四组五百世界尝试一失败记录

日期：2026-08-29  
状态：`POST_CONSUMPTION_MECHANICAL_FAILURE_NO_DATASET_CONCLUSION`

## 1. 运行边界

唯一正式命令基于提交 `ebbd31de0b8358796dde886e53b80169f3b59c35`、树 `d78e39e1fdf4b63372467311f9b5073d53c4d990` 执行。目标是训练、开发、审核甲、审核乙各 500 世界；审核真值未打开，模型训练未授权。

一次性权限成功签发后只消费一次。训练拆分完成 315 个世界后，在生成训练世界序号 315 时，严格身份值碰撞门抛出 `Formal500x4BuildError`。没有跳过世界、换种子、挑候选或原路径重跑。

## 2. 根因与科研结论边界

异常消息摘要为 `0e3393ad3b704c88e85bc3c13a290ed310fd760fab4b74892d6418e5ed8e920f`，对应“历史或本轮身份值碰撞”。同一已消费权限的纯内存确定性复现进一步区分出：

- 碰撞来源：成功 v1.2／失败 v1.12 历史身份排除库；
- 身份类型：电话号码；
- 本轮内部碰撞数：0；
- 方法资格根身份碰撞：否；
- 失败前完整世界数：315。

根因是正式实现沿用了方法资格根的单资产“散列后对类型域取模”分配。电话号码域只有 `10^9` 个值；该规则只检查单世界内部重复，没有在渲染前对全局历史排除库作确定性避让。后置碰撞门工作正确，但因此使已消费正式运行机械失败。

本次没有形成正式数据集、数据质量结论或模型结果。失败不能解释为合成方法、身份机制、捷径质量或跨语言模型效果失败。

## 3. 关键回执与摘要

- 授权文件：2,294 字节，SHA-256 `a2a0546b1470650665187605bcdfb80b8e1bc4dfd48632739d2570daf1d6d874`，规范自摘要 `0782dca58699e7bc2bb6e5f2e8f8d33f77ff9a01677f679fde38af1d4f2eddf4`；
- 签发占用回执：1,140 字节，SHA-256 `e853726b7fc21165aac7343c74cccffeafb8d36a028839dd448cb0641f8a746e`；
- 消费回执：783 字节，SHA-256 `8eb12d033b6f7c343b5e1bd54fc241f4c069360bff9ded083609f42e09d3dc73`，规范自摘要 `078c3552f1772c5d6b2e581bf1b30ac055e67cae43d65246cde225e4da71e221`；
- 失败终态：1,395 字节，SHA-256 `8804602b760db89b67aee3658966725e875ee8fc93d27e1a51d0405cac670aa1`，规范自摘要 `5208a7bdeba84a2e08d264b385de640ed1e1463c75b5bc5d5e3064365d02f0e3`；
- 冻结政策 SHA-256：`197015f97fd2928d0b3c8715c08e8181d83956f2b92f0e2e74c33f005704ba9b`。

## 4. 清理与不可复用承诺

程序先写失败终态，再删除公开／私有临时根和未完成最终根。四个数据路径均不存在；没有保留 315 世界载荷。六个 32 字节原始私有权限已删除，只保留授权承诺、签发占用、消费和失败回执。

尝试一的提交、政策、权限、授权、回执、输出路径和完成的 315 世界永久不得复用或重跑。后继必须使用新版本、新实现路径、新政策、新权限目录、新授权和新输出路径；其禁用承诺集合必须包含尝试一的六项权限承诺。

## 5. 后继最小修复

后继身份分配必须在任何可见文本渲染以前运行。对每个新身份资产，以全新身份权限、身份类型、资产标识和固定递增计数器生成确定性候选，选择第一个既不命中历史／方法资格根排除集合、也不命中本轮已分配集合的值。该过程不得读取标签、控制者角色、模型分数或文本质量，不得据此重抽世界或权限；候选计数、拒绝来源聚合和最终全局零交集必须进入私有审计与公开聚合回执。
