# Step28-v13 v1.13 V9.4.1 完整英文兼容性重放 v2 修订合同

日期：2026-08-30  
状态：`IMPLEMENTED_NO_GPU_RESULT_NO_TRAINING_AUTHORIZATION`

## 1. 修订范围

本合同只替换
`STEP28_V13_V1_13_V9_4_1_MODEL_EXPERIMENT_IMPLEMENTATION_CONTRACT_20260830.zh.md`
第 4 节中“抽取 49 块的最小兼容性夹具按十二位小数一致”这一前置门。原合同的
正式中文数据、M0、五个 M1、M2、M3、折、权重、阈值、指标、盲预测顺序和真值
权限均不改变。

旧 v1 夹具改变了原 Step7 的批次总体和补齐上下文，却要求跨工作负载十二位逐字
一致，不能据此判断模型兼容性。其运行边界由
`STEP28_V13_V1_13_V9_4_1_COMPATIBILITY_FIXTURE_V1_INVALIDATION_20260830.zh.md`
单独记录；v1 不得放行训练。

## 2. v2 冻结输入与登记运行环境

机器政策为
`schema/step28_v13_v1_13_v9_4_1_full_english_compatibility_policy_v2.json`。
它只允许读取原 Step7 的无标签、无证据值图形处理器载荷：33,434 个去重文本、
41,808 个共享块、853 个不透明卖家、38,618 条卖家—文本连接、733 个不透明卖家
对、原 LaBSE 完整分数文件、共享块清单和原 LaBSE 运行清单。不得读取英文标签、
证据类型、中文 identity33、控制者、成员关系、检索相关性或审核真值。

运行前必须与原 Step7 运行清单登记的下列关键项一致：Python 3.10.19、NumPy 2.2.6、PyTorch
2.9.1+cu130、CUDA 13.0、cuDNN 9.13.0、Transformers 4.46.3、
sentence-transformers 5.6.0、NVIDIA GeForce RTX 5090、计算能力 12.0；关闭
TF32，启用确定性算法，批量大小固定为 24。四个共同分块模型目录仍必须通过完整
内容摘要检查，只编码 LaBSE 权重。原运行清单没有记录驱动和实际 cuBLAS／
cuBLASLt 库版本，故不得声称完整物理软件栈已经逐项钉住；这些未登记差异最终由
完整嵌入矩阵的精确字节摘要门覆盖，也只能在失败时作为尚未定位的机械差异来源。

## 3. 唯一有效的兼容性门

v2 必须按原去重文本顺序从头调用冻结 Step7 分块器，并要求重建的全部共享块与
原 41,808 行逐字段相等。随后按原文本块顺序和原批量大小生成连续单精度
`41808×768` 嵌入矩阵。

同时满足以下两项才通过：

1. 嵌入矩阵原始连续字节 SHA-256 精确等于
   `635b932cb6e50e9d2e917f627ecb89222f20a6f62fc31c0bfed13bf158fae978`；
2. 由该矩阵重新计算的 733 对、十二项 LaBSE 主／频次审计聚合分数文件逐字节
   等于原文件，SHA-256 为
   `9597497613493d04f4cc2b9dbb663341e6756677fa60c89e199fdae52937d282`。

不得降低小数位、增加事后容差、重选文本或卖家对。输入或运行环境漂移属于机械
预检失败；完整工作负载的矩阵或分数不一致属于兼容性重放失败。两者均不授权训练，
失败的临时矩阵和观察分数不得保留。

## 4. 输出、执行和后继边界

唯一入口为：

```bash
bash scripts/run_step28_v13_v1_13_v9_4_1_full_english_compatibility_v2_linux_20260830.sh
```

成功输出根只能包含观察分数文件和自哈希成功清单：

`reports/step28_model_experiment/v9_4_1_implementation_v2_20260830/full_english_compatibility_linux_replay`

正式 `run` 只允许在该输出根不存在时执行；若输出根已经存在必须拒绝，不能把已有
文件当成本次图形处理器重放。`validate-output` 只复核已同步成功产物的字节和语义
完整性，必须明确报告本命令没有执行图形处理器重放，不得签发新的实验通过结论。

兼容性通过只证明当前 Linux 能逐字重放冻结英文 LaBSE 特征生成链，不训练任何
模型，也不自动授权 M0／M1／M2／M3。结果同步回 Windows、独立核对且训练实现
合同全部闭合后，才可另行签发一次性模型阶段授权；审核甲乙真值继续封存。
