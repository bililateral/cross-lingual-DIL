# Step28-v13 v1.13 V9.3-R2 前置结构捷径门失效结论

日期：2026-08-27  
状态：`DATASET_INVALIDATED_PREBUILD_STRUCTURE_GATE`

## 1. 运行边界

本轮只运行冻结政策下的一次前置结构捷径门。运行根为 `reports/step28_v13_v1_13_balanced_schedule_v9_3/registered_negative_structure_gate_r2_20260827`，失败阶段为 `prebuild_structure_gate_hard_gates`。方法政策规范自摘要为 `9088de1437e795bf348191da810adc98286756bd965964101e94535774ecece2`，文件摘要为 `ad692d8a14cea17d610e22dc358ac41bb6af0dc7891386494908fa66d1a3f275`；一次性随机权威规范自摘要为 `db6330bcd85f7d26e435d7aee4a2ace886576df07b949ca687bb889be40aebb7`，文件摘要为 `b097086f5566c1e32eb1d95415dde4e6eb3994a953d23ada6e6da7cdc2e2647d`，十四个私钥值从未打印或写入公开结果。

输入仍是 R2 五文件计划：训练 `20／20／20`、开发 `22／22／22`，构建回执规范自摘要 `08e49176f9c4f8e3480378578881e55d3248c2e0743198c7ecfd442875eab77f`。训练和开发各冻结 500 世界、189,000 对；正例各 10,000，对应基线平均精确率 `0.0529100529`。审核甲／乙真值读取次数为零。

## 2. 五项硬门全部失败

| 硬门 | 门限 | 观察值 |
|---|---:|---:|
| 最大单特征对称接收者操作特征曲线下面积 | 0.52 | 0.768156 |
| 四模型最大对称接收者操作特征曲线下面积 | 0.53 | 0.836099 |
| 四模型最大平均精确率提升 | 0.01 | 0.114283 |
| 9,999 次世界重抽样曲线下面积百分之九十五上界 | 0.53 | 0.837003 |
| 9,999 次世界重抽样平均精确率提升百分之九十五上界 | 0.015 | 0.116693 |

卖家槽视图没有检出捷径：逻辑回归和深度二浅树的对称曲线下面积为 `0.500311`、`0.500227`。噪声可见视图则非常强：逻辑回归为 `0.835981`，平均精确率 `0.167193`；浅树为 `0.836099`，平均精确率 `0.158460`。最大单特征值也来自噪声可见视图，卖家槽单特征最大值仅 `0.500161`。

## 3. 科研解释与不得越界的结论

有效结论是：冻结的噪声可见结构在训练到开发的固定迁移中仍携带稳定、可学习的标签关联，强度远高于全部预登记门限。现有“控制者分组＋噪声槽映射＋登记负例残差”联合设计没有实现模型所需的标签独立性，因此 R2 在自然文本和方法资格根产生以前即失效。

不能把失败单独归因于开发残差 `22`，也不能事后选择获胜列、删除不利探针、改变阈值或只保留卖家槽视图。正式结果只发布预登记汇总，没有发布获胜单列名称；不得为解释方便重新运行同一门做后验特征搜索。后继若存在，必须重新设计标签与噪声联合层，而不是隐藏噪声特征或继续放宽残差。

## 4. 结论边界

本轮没有生成自然文本、1,004 世界方法资格根、正式中文训练数据、审核真值、模型或训练指标。`natural_text_generated=false`、`method_root_generated=false`、`m0_m1_m2_m3_training_authorized=false`。结果文件为 83,554 字节，文件摘要 `e06225ba78e20003bbd4fca25fc6be659fa6560aa1405a5017e078e9b0f6c6d4`，规范自摘要 `573f29a2bb5d7202803f2a6c5f29cb18bc2d0c90a50cc5e887f5c242a44f100f`。

## 5. 不可复用与清理纪律

R2 政策、随机权威、五文件计划、结构门结果和 R2 专用入口永久不得重跑、覆盖或用于训练。先由本文和同步交接记录关键摘要、失败阶段、根因及结论边界；随后删除随机权威、失败结果载荷、R2 计划载荷、缓存、临时目录和仅服务 R2 的代码。Git 历史只保留小型、可审计的失败边界，不在当前工作树保留失败数据。
