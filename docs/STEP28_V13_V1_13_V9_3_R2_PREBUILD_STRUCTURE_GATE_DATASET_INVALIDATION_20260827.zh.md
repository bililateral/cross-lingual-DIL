# Step28-v13 v1.13 V9.3-R2 前置结构捷径门失效结论

日期：2026-08-27  
状态：`AUDITOR_DESIGN_INVALID_NO_DATASET_CONCLUSION`

## 1. 运行边界

本轮只运行冻结政策下的一次前置结构捷径门。运行根为 `reports/step28_v13_v1_13_balanced_schedule_v9_3/registered_negative_structure_gate_r2_20260827`，失败阶段为 `prebuild_structure_gate_hard_gates`。方法政策规范自摘要为 `9088de1437e795bf348191da810adc98286756bd965964101e94535774ecece2`，文件摘要为 `ad692d8a14cea17d610e22dc358ac41bb6af0dc7891386494908fa66d1a3f275`；一次性随机权威规范自摘要为 `db6330bcd85f7d26e435d7aee4a2ace886576df07b949ca687bb889be40aebb7`，文件摘要为 `b097086f5566c1e32eb1d95415dde4e6eb3994a953d23ada6e6da7cdc2e2647d`，十四个私钥值从未打印或写入公开结果。

输入仍是 R2 五文件计划：训练 `20／20／20`、开发 `22／22／22`，构建回执规范自摘要 `08e49176f9c4f8e3480378578881e55d3248c2e0743198c7ecfd442875eab77f`。训练和开发各冻结 500 世界、189,000 对；正例各 10,000，对应基线平均精确率 `0.0529100529`。审核甲／乙真值读取次数为零。

## 2. 机器五项硬门全部失败

| 硬门 | 门限 | 观察值 |
|---|---:|---:|
| 最大单特征对称接收者操作特征曲线下面积 | 0.52 | 0.768156 |
| 四模型最大对称接收者操作特征曲线下面积 | 0.53 | 0.836099 |
| 四模型最大平均精确率提升 | 0.01 | 0.114283 |
| 9,999 次世界重抽样曲线下面积百分之九十五上界 | 0.53 | 0.837003 |
| 9,999 次世界重抽样平均精确率提升百分之九十五上界 | 0.015 | 0.116693 |

卖家槽视图的逻辑回归和深度二浅树对称曲线下面积为 `0.500311`、`0.500227`。被命名为“噪声可见”的视图则返回逻辑回归 `0.835981`、平均精确率 `0.167193`，浅树 `0.836099`、平均精确率 `0.158460`；最大单特征值为 `0.768156`。这些数值是机器输出，但下节证明该视图不是干净的模型可见表面，不能作为数据集捷径结论。

## 3. 审计器真值污染与正确结论

结果发布后对冻结代码作只读追踪，确认“噪声可见”矩阵错误混入两组模型不可见的私有真值派生字段：

1. `controller_size_min/max/diff/sum` 直接由私有 `controller_membership` 计算；而本任务标签正是两个卖家是否属于同一控制者，控制者规模属于标签生成机制，不是模型可见噪声；
2. `registered_treatment`、登记端点计数、克隆／语义端点计数、登记商品序号和四类角色计数直接由私有 `override_audit` 计算；这些字段标识预先指定为负例的处理角色，也不进入 M0／M1／M2／M3 模型表面。

所谓“先冻结矩阵、后读取标签”不能消除该污染，因为矩阵已从决定标签的私有控制者归属和登记负例计划派生。最大单特征 `0.768156` 与模型族约 `0.836` 因而至少部分、且很可能主要来自审计器的真值泄漏。正式结果没有发布获胜列名称，本轮也不重新运行同一门做后验列搜索；无需知道具体获胜列即可判定输入表面违反冻结的“模型可见捷径”科学定义。

正确结论是：R2 的前置结构门实验设计失效，没有形成有效数据集质量结论。它既不能证明数据存在模型可见捷径，也不能证明数据通过质量门；开发残差 `22` 不是已证实根因。R2 仍永久关闭，因为冻结审计合同已经污染，不能通过事后删列把同一运行追认为通过。

## 4. 结论边界

本轮没有生成自然文本、1,004 世界方法资格根、正式中文训练数据、审核真值、模型或训练指标。`natural_text_generated=false`、`method_root_generated=false`、`m0_m1_m2_m3_training_authorized=false`。结果文件原为 83,554 字节，文件摘要 `e06225ba78e20003bbd4fca25fc6be659fa6560aa1405a5017e078e9b0f6c6d4`，规范自摘要 `573f29a2bb5d7202803f2a6c5f29cb18bc2d0c90a50cc5e887f5c242a44f100f`；其中机器状态 `DATASET_INVALIDATED_PREBUILD_STRUCTURE_GATE` 只记录冻结程序走到的分支，不能升级为有效科研结论。

## 5. 不可复用与清理纪律

R2 政策、随机权威、五文件计划、结构门结果和 R2 专用入口永久不得重跑、覆盖或用于训练。精确失效字节已由 Git 提交 `9a742a1` 保存。随后从当前工作树删除 11 个 V9.3 报告载荷、1 份冻结政策、20 个 V9.3 实验脚本、8 个对应测试以及忽略区内的一次性随机权威；40 个受版本控制路径合计删除 4,489,136 字节，十四个私钥值未进入 Git。本文、R1／R2 边界文档、总进度和交接继续保留。当前工作树不保留失败数据或 R2 活入口。

删除后全仓回归运行 768 项、用时 1,810.335 秒：759 项实际通过、9 项按既定历史边界跳过、0 失败、0 错误。回归产生的唯一 `scripts/__pycache__` 已删除；未发现测试临时报告、`.pytest_cache`、`.building` 或 V9.3 活路径。

后继尚未授权。下一版必须用新版本和新路径，先把结构探针输入严格限制为真实模型可见字段或其无真值上界代理，明确禁止控制者规模、登记处理、角色、机制和任何标签派生字段；再独立冻结并运行捷径门。不得复用 R2 随机权威、矩阵承诺、模型分数或输出路径，也不得把本轮聚合结果用于挑列后重跑同一门。
