# Step28-v13 v1.13 V9.1 冻结质量审计实现合同

日期：2026-08-22

状态：`IMPLEMENTATION_ONLY_NO_QUALITY_RUN_NO_FORMAL_GENERATION_NO_TRAINING`

## 1. 当前输入与唯一目标

V9.1 同一权威设计根已发布为 `reports/step28_v13_v1_13_scientific_builder/design_preflight_v9_1_20260821`。根清单文件为 7,049 字节、SHA-256 `36657ec5a287b5ecc7be71117764d94a78d1c926e9227ec933fe7fa7630c4f18`、规范自哈希 `f10086faa5f68b08a4d25a6e49943fb18ede0858ca50bad711d7bb2f4d94200f`。V9.1 等价重放规范自哈希为 `b5a19ae5c2f5c3694368ad1eacb01e559a6819727a48b2f64ca27910ae30c92b`。

结果记录经网页端 GPT-5.6 Sol Pro 在 `https://chatgpt.com/c/6a8922d2-c458-83eb-b2b1-6ec69eeb9266` 复算勘误，回复为 2,515 个 UTF-8 字节、SHA-256 `04235064ad1cbb5a45e7d2316270081f0aacbd61da6f11649d9d80443ca3d76f`，最终一行“当前结果记录可以作为V9.1质量审计实现阶段输入”。这不是质量审计运行许可。

本阶段唯一目标是实现一次性执行接线，使既有冻结质量方法能够审核这一精确 V9.1 根。不得修改或重建数据根，不得改动冻结科学政策、特征、模型、阈值、重抽样、世界、标签或候选。

## 2. 冻结科学基线

下列现有模块继续保持字节不变：质量通道政策及验证器、基础质量运行器、三种文本视图、结构聚合器、探针准备器、探针验证器、训练／开发真值能力和相应合同测试。冻结政策的 `quality_audit_run=false` 与 `metric_generation=false` 必须保持；不得制造授权位为真的伪政策，也不得运行时替换验证函数。

质量探针仍只在合成训练拆分拟合、合成开发拆分过门。它们不是 M0／M1／M2／M3，不产生下游模型效果结论。审核甲／乙真值在本阶段保持零打开、零读取、零物化。

## 3. 新增执行字节

只允许新增四类内容：

1. V9.1 覆盖政策：精确绑定根和四份拆分清单、冻结政策与基础源码、新适配器、合同测试、唯一结果路径及本合同，并定义无参数入口的名称和参数数量。为避免哈希环，入口精确字节不写回覆盖政策；提交后的外部回执必须再绑定入口文件、覆盖政策、适配器和 Git 提交／树的精确字节。
2. 一次性执行适配器：接受已消费外部回执形成的不可变上下文；结构门直接调用冻结 `_aggregate` 核心，数值门只调用冻结数值辅助函数，不复制结构算法或更改科学参数。
3. 无参数入口：验证当前 Git 提交／树和全部文件钉，消费一次性外部回执，调用适配器并排他发布唯一终态。
4. 合同测试：覆盖消费、绑定、真实生产接线、标签隔离、失败分类和事务发布。

V9.1 质量审计尝试编号固定为 1；它与根清单中的设计构建 `attempt_index=2` 是不同概念，禁止混用。待用回执固定为 `private_custody/step28_v13_v1_13_v9_1_quality_audit_authorization.json`；结果固定为 `reports/step28_v13_v1_13_quality_audit/v9_1_design_preflight_attempt1_20260822/quality_audit_receipt.json`。旧 V9 回执、已消费回执、结果和执行字节均不得被解释为 V9.1 授权。

## 4. 一次性能力与标签边界

仓库代码不得生成外部回执。只有最终精确字节提交、推送并经新的网页端提交后复审后，才可在仓库外创建回执。回执只允许 `quality_audit_run=true` 和 `quality_metric_generation=true`；正式种子、正式 500×4、审核甲乙真值、下游模型训练及下游模型指标全部为假。

入口必须在读取任何设计视图或真值前原子消费回执，并从已消费路径重新读取、重验大小、文件摘要、规范自哈希、复审网址／摘要／最终行、Git 提交／树、覆盖政策、适配器、入口、根、结果路径和能力全集。回执复用、路径替换、根替换、旧尝试替换或能力扩张一律关闭失败。

正式顺序固定为：消费回执；闭合根清单和 77 文件物理全集；闭合构建与冻结质量政策；加载四拆分标签无关输入；闭合卖家／商品／对／查询全集和实际六视图结构；冻结全部矩阵与资格位图；仅打开训练与开发真值一次；再次闭合标签无关对象；拟合冻结质量探针；发布不含行级标签或预测的终态。

## 5. 必须直接覆盖的测试

- 无待用回执、待用路径错误、旧 V9 回执、已有结果或临时结果必须在首个设计视图读取前拒绝。
- 仓库相对根清单路径、数据根和 `RootManifestPin(path="root_manifest.json")` 必须真实组合闭合；不得再次混淆两种路径语义。
- 结构桥必须真实进入冻结 `_aggregate`，并证明实际 V9.1 三种卖家画像摘要通过；任一实际画像字段或四条结构承诺被篡改必须失败。
- 训练／开发真值只能在所有矩阵和资格承诺冻结后各读取一次；审核甲乙真值读取始终为零；真值打开后任何矩阵、资格、政策、能力或根钉变化必须失败。
- 数据结构／承诺错误归为 `DATASET_INVALIDATED`；回执、路径、运行库、文件打开和发布故障归为 `AUDITOR_EXECUTION_FAILED_NO_DATASET_CONCLUSION`。
- 消费后成功、消费后异常、已有相同终态、已有不同终态和终态无法发布均必须有确定性行为；不得恢复待用回执或允许重试。

测试只能使用小型临时夹具或只读当前根，不得创建正式回执，不得完整运行 1,004 世界质量审计，不得输出行级标签或预测。

## 6. 候选实现复审与定点修正

网页端 GPT-5.6 Sol Pro 在同一会话中对首版候选实现完成复审。回复为 23,362 个 UTF-8 字节，SHA-256 为 `7ea8b1d3165caefe8b4c230255cf2f538db893849d4d89d1cf14170dd868e94d`，最终一行为“候选实现不能进入本地回归与提交前修正阶段”。该结论不是运行许可。

复审后接受并落实的修正包括：适配器复用入口的完整回执模式并再次核对 Git、入口、冻结源码、根和四拆分；最终结果采用精确模式、逐层自哈希和证据计数验证，最小 `PASS` 与未登记载荷必须失败；数值等价测试改用冻结合法夹具，不再制造伪授权政策或替换验证函数；新增只读真实根测试，实际完成 44 个标签无关文件、六类视图、画像摘要、冻结结构聚合与加载结构绑定，并在矩阵和真值阶段前停止；严格区分布尔值与整数尝试编号；入口重算 72／77 文件和字节总数；审查网址使用精确会话格式；两探针家族后再次闭合矩阵、资格、政策和已消费执行上下文；消费后文件缺失类机械故障不得宣称数据失效；包装层无法取得真值运行计数时记录“未到达”或“异常后不可得”，不得伪报零。

复审建议的持久化多阶段锁状态机没有纳入科研实现：正式无参数入口仍是唯一获准的生产入口，负责原子消费、唯一结果目录和消费后唯一终态；适配器是内部科学执行模块，不是第二个公开入口。为关闭实际绕行，适配器现在必须通过与入口相同的完整回执、Git 和文件绑定验证。不得把本边界扩张为进程锁、网络或攻防系统。

首版修正后的合同测试方法数为 44。2026-08-23 文档同步前后的两轮聚焦回归分别为 44／44（42.380 秒）和 44／44（42.942 秒）；其中包含真实 V9.1 四拆分标签无关输入、六类视图、画像摘要、冻结结构聚合与加载结构绑定的只读集成验证，并在矩阵和真值阶段前停止。全仓回归运行 812 项，用时 1,587.193 秒：803 项实际通过、9 项按既有历史记录跳过、0 失败、0 错误。记录该结果后只修改本合同、最新交接和相应哈希钉，不修改科学逻辑；哈希链随后由第三轮 44／44（40.564 秒）闭合。在提交推送并取得新的提交后网页复审前，质量审计仍未授权。

提交前修订候选又在同一网页会话完成精确附件复审。网页正文提取为 15,713 个 UTF-8 字节、SHA-256 `69038a2e55c05026f6d3a4e536d18d478f838cebe79c21c9802526a7060144be`，问题分级为阻断 0、高 1、中 0、低 0，最后一行“修订候选不能提交并进入提交后复审”。该高级问题成立：两个入口近同时读取同一待用回执时，未取得原子改名所有权的调用可能依据“已消费目标存在、待用源消失”误认自己拥有终态发布权，抢先写入错误失败终态。修复不引入锁或持久状态机，只让回执消费函数在原子改名后立即返回、把已消费字节重验留给既有验证器，并以调用内布尔值记录本次是否取得改名所有权；只有真实消费者可发布消费后终态。新增第 45 项定向反例，证明竞争失败者不能创建结果目录或调用终态发布器。修复后聚焦和全仓回归仍须重跑。

## 7. 阶段门与清理

实现后必须先跑最新 45 项聚焦合同和全仓回归，删除字节码、临时夹具、测试回执与测试结果，再进行修复后提交前网页复审。复审通过后才可提交推送，并把精确提交后文件、根／拆分清单、当前紧凑等价承诺及其四路径修订件交网页端再次复审。只有提交后回复最后一行精确为“允许运行一次V9.1冻结质量审计”，才可创建一次外部回执并执行无参数入口。

即使质量审计通过，也只说明设计根满足冻结质量门。正式中文合成数据生成、审核甲乙真值解封及 M0／M1／M2／M3 训练仍须另行授权。任何不可恢复失败必须先记录运行编号、阶段、根因、结论边界和关键摘要，再删除失败载荷与专用代码，只保留小型回执和不可逆排除证据。
