# 跨语言地下市场马甲识别科研项目最新交接

更新时间：2026-08-26

当前分支：`method/step27-english-pretrained-synthetic-adaptation`

V9.1 质量失效关闭提交：`51eb92c`。本文后续仅同步交接状态，接手时应以 `git rev-parse HEAD` 取得精确当前提交。

本文取代 `AI_RESEARCH_HANDOFF_20260719.zh.md` 作为当前接手入口。旧交接、失败记录和历史实验文档继续保留，但不得把其中 Step27 或已关闭版本当成当前主线。

## 1. 当前科研主线

研究目标是把只用真实英文马甲监督学到的识别能力迁移到中文卖家身份链接任务，并判断新增中文身份历史是否提供超出“仅做中文拟合”的真实增量。输入是两个卖家的可见资料与身份历史，输出二者是否由同一控制者经营的概率。

当前主实验比较：

- M0：冻结的英文来源基线；
- 五个 M1：使用相同数值但故意错配身份历史的阴性对照；
- M2：在冻结 M0 基础上，用正确对齐的中文合成身份历史训练残差迁移模块；
- M3-base：仅用中文合成监督，从头训练与 M0 相同的 24 项基础输入；
- M3-joint：在 M3-base 基础上加入正确的三十三维身份历史。

主要因果比较是 `M2-五个M1均值`，不是单独比较 M2 与 M0。即使合成实验全部通过，也只能证明冻结生成机制内的身份信息可学习，不能替代未来真实中文独立身份标签验证。

## 2. 英文与中文数据边界

### 英文来源

M0 固定为 `LightGBM + legacy18 + LaBSE`：先计算 18 项冻结卖家对特征，再由完整商品文本分块得到 6 项 LaBSE 相似度，最后由英文 LightGBM 输出基础概率。冻结模型为：

`reports/step7_v4_1_style_free_classifier_selection/v1_20260724/selected_models/lightgbm__legacy18_labse.joblib`

模型文件 SHA-256 为 `aae7f0e520c28006471cf1e1e518d6b0d383e6d14810a2c4aca4d28aa11d91dd`。来源训练边界为 401 个真实英文卖家对（116 正、285 负）。历史验证数据已被反复查看，只能作开发诊断，不能再承担独立认证。

### 中文目标域

真实中文快照只向合成器提供训练侧、去标签的汇总风格和商品数量分布；不复制真实文本，不读取真实身份关系，也不参与合成标签。合成标签仅由私有控制者归属定义：同一控制者为正例，不同控制者为负例。

最终目标数据为训练、开发、审核甲、审核乙各 500 个世界。每个世界有 28 个卖家、12 个控制者和全部 378 个无序卖家对，其中固定 20 个正例、358 个负例。正式目标合计 2,000 个世界、56,000 个卖家和 756,000 个卖家对；审核甲乙真值在盲预测冻结前不得打开。

V9.1 曾构建训练 500、开发 500、审核甲 2、审核乙 2，共 1,004 个设计世界、28,112 个卖家、379,512 对（20,080 正、359,432 负）。它不是正式训练数据，并已因至少两个冻结文本质量门失败而永久失去质量资格；大型载荷须删除，只保留小型清单和失败谱系。

## 3. 当前有效结果

V1.3 至 V1.12 均已永久关闭。V1.12 曾生成 378,000 行，但因可见文档碰撞失败；大型载荷与专用代码已删除，只保留失败边界和不可逆排除摘要。

V9 第二次质量审计发现 1,004 个世界的三类落盘卖家画像承诺全部错误，共 3,012 项不匹配。V9 根已删除。V9.1 只机械修复四个持久化路径，并保持同一随机权威、68 个文件逐字节不变、版本和其他结构不变。

V9.1 历史设计构建根：

`reports/step28_v13_v1_13_scientific_builder/design_preflight_v9_1_20260821`

- 状态：`PASS_DESIGN_BUILD_NOT_TRAINING_QUALIFIED`；
- 构建时为 77 个文件、1,464,158,039 字节；质量失效清理后只保留 5 份清单、30,711 字节；
- 根清单 SHA-256：`36657ec5a287b5ecc7be71117764d94a78d1c926e9227ec933fe7fa7630c4f18`；
- 根清单规范自哈希：`f10086faa5f68b08a4d25a6e49943fb18ede0858ca50bad711d7bb2f4d94200f`；
- V9.1 等价重放规范自哈希：`b5a19ae5c2f5c3694368ad1eacb01e559a6819727a48b2f64ca27910ae30c92b`。

该结果只记录设计构建与机械等价曾经闭合。随后冻结质量审计严格证明至少两个文本质量门失败，因而该根不能用于训练，也不能通过包装层修复恢复资格。

## 4. V9.1 冻结质量审计尝试 1 包装失败且质量失效

质量审计实现以提交 `8f935adae741dce83f505edb3ef2ef9384d998ae`、树 `108fd7fe3b31bc591458b5aa18e092c048190693` 推送。提交后网页复审正文为 15,906 个 UTF-8 字节／`bfcc614a147a2aca658e2bbeec02df4575c36977884452b2a17dcf8f6b35ef52`，最后一行精确为“允许运行一次V9.1冻结质量审计”。一次性回执为 4,519 字节／`29221d5e1dd4b9113efc333e1df8086285fbc70b7c0109add3ce38abe63e6d96`，规范自哈希 `5c11e4199a527a53442683271e7085ddf36bf410d39bc9961cab34319111175c`，现已消费且永久不得复用。

唯一运行约 7,237.18 秒后发布 5,336 字节终态，文件 SHA-256 为 `f190ae560b46fe65bc0b2fb43a2c5297f9a15eb9846a87db9f497dcf9135ae53`，规范自哈希为 `c215b4a69200445159a32ccf9533e9a816bd0ea955f3597a18bf72c63e048f00`。状态为 `AUDITOR_EXECUTION_FAILED_NO_DATASET_CONCLUSION`，失败阶段为 `quality_audit_result_validation`，重试未授权。

异常消息摘要 `b3a6c52513dd6a009621fa8635795b4de92399a941dcc49c532a0f96e851d48c` 精确对应 `text gate evidence drift`。适配器按冻结计算顺序记录失败门，外层验证器却先按名称排序再逐项比较。本轮文本侧至少两个冻结质量门被触发，同一集合因顺序不同被包装器错误拒绝，完整指标没有持久化。

机器终态仍必须原样解释为包装层 `AUDITOR_EXECUTION_FAILED_NO_DATASET_CONCLUSION`；但冻结适配器在任一文本门失败时已把监督回执和总审计回执置为 `DATASET_INVALIDATED`。因此科研结论是：V9.1 根至少失败两个文本质量门，永久失去质量资格。具体门名、观测值、超限幅度和编码／槽位家族结论不可恢复，禁止猜测。

本次异常只能发生在完整审计体返回以后，因此训练／开发监督路径和两家族计算均已到达；终态未保留精确访问计数。审核甲乙仍未获能力授权，审计体返回路径要求其访问计数为零。网页端修正审查正文为 14,086 个 UTF-8 字节／`3c31ba7be7253a206d92bc23d25596996ec2f5da0da9c73d3d4d1397b663a067`，本地复核接受其数据失效边界。修正版复审正文为 9,282 个 UTF-8 字节／`458ed47014c62b734c78096b694d5a127b933046467fa0b47459fbf873963f69`，分级 0／0／0／0，最终行“V9.1修正归因与清理边界可以执行”。详见 `docs/STEP28_V13_V1_13_V9_1_QUALITY_AUDIT_ATTEMPT1_EXECUTION_FAILURE_20260823.zh.md`。

清理后原根路径只剩根清单和训练／开发／审核甲／审核乙四份拆分清单，共 5 个文件／30,711 字节，五份文件摘要均未改变；72 个登记数据文件共 1,464,127,328 字节已删除，`observed`／`private` 目录均不存在。本次失败专用覆盖政策、适配器、无参数入口和合同测试也已删除。小型终态、已消费回执、实施合同和冻结 V9 方法基线仍在原路径。

## 5. 质量门和训练授权

质量审计必须验证：

- 逐世界 28 节点、378 对和控制者结构闭合；
- 四拆分隔离、文档碰撞和清单完整性；
- 编号、行序、商品数量、字段缺失等禁止元数据不能预测标签；
- 控制者盲风格置换后，完整文本、主题文本和模板表面文本探针不能超过冻结捷径阈值；
- 代码通道敏感性、画像摘要和身份机制承诺闭合；
- 训练与开发真值只能在矩阵和资格位图冻结后各打开一次；审核甲乙真值读取必须为零。

本次 V9.1 审计没有持久化具体质量指标，但已经严格证明至少两个文本门失败。同一 V9.1 根不得重跑或恢复资格。新的 V9.2 必须使用新合同、新版本根、新尝试、新结果路径和独立资格边界；失败门集合须采用唯一规范表示，包装异常仍须保存紧凑聚合证据。即使未来新版本质量审计通过，也不等于正式数据已生成；仍须另行冻结和授权正式四拆分生成，随后才能计算 M0 并训练 M1、M2、M3。

V9.2 科研协调合同已经冻结为 `docs/STEP28_V13_V1_13_V9_2_SCIENTIFIC_RECONCILIATION_CONTRACT_20260823.zh.md`，17,420 字节，SHA-256 `bb1a98044f91e3a9d915b842b231fd4822d305cc3121852a8f889130cc54edfe`。它修复了一个上位合同矛盾：原科学合同允许模型学习用词、标点、长度、空白和排版等作者风格，近随机文本硬门应作用于控制者盲风格错排后的反事实文本；V9 却把保留原作者分配的完整／代码遮蔽／代码中和三表面直接送入近随机门，错误地把合法作者风格也纳入否决对象。

V9.2 采用最小语义修复：反事实完整表面固定为七视图、两模型，共 14 个文本硬门模型；原作者三表面的 42 个模型只作描述性作者风格和编码通道诊断；公共代码／私有槽位四模型继续作为独立硬门。设计根每世界在 `_build_private_truth` 前物化原作者三表面六份商品／画像输入及一套反事实完整商品／画像，共八份模型输入；另以版本化 `channel_structure_audit` 保存映射和不变量证据，不建立反事实遮蔽／中和表面。

网页端 GPT-5.6 Sol Pro 对唯一附件名 `V9_2_CONTRACT_REV2_17420_BYTES_bb1a9804.md` 实际复算了 17,420 字节和上述 SHA-256。最终回复按浏览器 `innerText` 提取为 7,872 个 UTF-8 字节，SHA-256 `e25bbe75d822dcb289d29c271e078492e66c9b90cad508b22c22d27fd9ff639d`，分级阻断／高级／中级／低级均为 0，最后一行“V9.2合同可进入实现”；会话为 `https://chatgpt.com/c/6a8aba2f-1618-83eb-8a3a-110cdf90d0c9`。该结论只允许实现、测试、回归、提交和运行前复核，不授权随机权威仪式、1,004 世界构建、质量运行、正式数据、真值解封或训练。

### V9.2 当前实现状态（2026-08-24）

V9.2 初版实现以提交 `6eb0655db883a2f7478e0123fa770858e54b54b9`、树 `02cc57e340574521fb8b47824e29cef127fa5dce` 推送。提交后网页审查正文为 31,857 个 UTF-8 字节／`e8cbdb61119f4489250a66b86a2c26266dd2d7267bec5411510126153c3f7766`，分级为阻断 2、高级 3、中级 4、低级 0，最终行“V9.2实现不可进入随机权威仪式前审查”。该否决已被接受，初版提交不得作为随机权威或构建依据。

本轮针对可证实问题完成实现修复：最后一个代码／槽位模型家族结束后再次核对全部冻结矩阵、资格掩码、政策和输入字节；构建器在临时根校验、改名后还会在最终路径完整重放校验；正式结构汇总和数值验证只接受由已消费一次性回执签发的窄能力对象，不再接受普通字典；每拆分 20 个清单载荷在解析前全部重算原始字节摘要和物理行数，训练／开发真值仍各只语义读取一次，审核甲乙真值只作原始物理绑定而不解析；根清单的策略、构建源码、构建回执、随机权威、规模和密钥承诺全部闭合。

失败语义也已收紧：无法完成全部计算的格式、顺序、输入输出或机械异常一律只能得到“审计器执行失败、无数据集结论”；可以计算的真值数量、映射、重放、自哈希、能力、承诺和落盘摘要偏差必须继续计算全部 97 项门并形成数据失效证据。完整失效证据即使主结果或外包装发布失败也保持失效结论，并排他保存机器终态；`cleanup_required` 只表示需要清理，不会绕过“先记录失败边界、再精确删除”的科研纪律。网页建议的自动删除失效根未采用，因为它会破坏现有失败记录顺序和人工确认边界。

第一轮修复已以提交 `2a4eedf8700efea76e81cf0bea2ea12c8866988c`、树 `e8bffe5461283957eaa483294d17f58bffc476d9` 推送。提交归档为 156,615 字节／`9c04e7fbc1b877feb0116c2f6eded2826096e98a760d1408a287a14437f297a4`。网页端最终审查原文按浏览器 `innerText` 提取为 6,563 个 UTF-8 字节／`85db3189fdffcdc507a23caa2546bce8350d1819e8e2eede5b71f871eebd6ac3`，分级为阻断 0、高级 0、中级 1、低级 0，最后一行“V9.2修复可进入随机权威仪式前审查”；会话为 `https://chatgpt.com/c/6a8b91f1-4fc0-83eb-a95f-4fcf721132d7`。唯一中级项是内部能力工厂可进一步限制为正式已消费回执路径；网页审查明确认定它不是当前唯一正式入口可触发的科研缺陷。

本地接受该项为最小接口闭合：正式质量能力只能由固定的 V9.2 已消费授权路径签发，并增加默认路径一致性和任意伪造 `.consumed.json` 拒绝测试。一次中间复核曾误把“审核真值不得解析”扩大为“真值文件不得做原始字节核验”，但上位质量修订合同明确要求审核私有文件仍完成纯字节大小、SHA-256 和行数核验；该过度修改已在提交前撤销。当前继续保持每拆分 20 个物理载荷全部原始摘要与行框架绑定，审核甲乙不解析字段、不建立真值行对象且监督真值能力不挂载。

当前机器策略仍为 10,523 字节，文件 SHA-256 `987fc203835057fb51d226296b801dad40accf12fa12a9bf6a05e5d3c8c61eff`，规范自哈希 `082e68c3154841cc089f3db164e9b358ab16767feb2845476c3989d417d3eebc`；13 份运行源码和 5 份测试共 18 个来源逐字节复核无漂移。收紧后的聚焦回归为 43／43 通过，用时 63.414 秒；相邻 V9／V9.2 合同回归为 190／190 通过，用时 382.376 秒；最终全仓回归运行 811 项、用时 1,949.393 秒：802 项实际通过、9 项历史既定跳过、0 失败、0 错误。当前没有创建随机权威、V9.2 世界、设计根、质量结果、正式行、真值结果或模型；随机权威仪式、1,004 世界构建、质量运行、正式生成和训练仍全部禁止。详细记录见 `docs/STEP28_V13_V1_13_V9_2_POST_COMMIT_REVIEW_REMEDIATION_20260824.zh.md`。

固定路径闭合已以提交 `c3449a3b334a95beb83652c309afd0e85ef5af73`、树 `7940d97b6e881eca93c9910290958cb8a56dd89f` 推送；本地与远端分支头一致。精确提交归档含 27 个非目录文件，为 158,268 字节／`aa5f57163c61e487f75146c50bac374f1cb24c62f7877f6d24e9e815386b0787`，逐文件与提交字节一致。网页端最终复审原文按浏览器 `innerText` 提取为 12,042 个 UTF-8 字节／`16bb09c9732ed83733da6703c80e56ed95a7e90d05524bdacd6531019f2c716d`，分级阻断／高级／中级／低级均为 0，真实科研缺陷和未闭合可选加固均为 0，最后一行“V9.2最终修复可进入随机权威仪式前审查”；会话为 `https://chatgpt.com/c/6a8baec5-d980-83eb-9aef-b6c8a9877962`。它独立确认归档、策略自哈希、全部合同／父策略／来源钉住、97 门注册表、固定消费路径、测试替换作用域、20 载荷纯字节核验、冻结时序、42／14／4 模型角色和完整失效证据未回归；选择性归档不能独立证明 Git 对象库或重跑完整回归，该限制由本地 Git 与上述回归证据补足。该结论仍只是进入随机权威仪式前审查，不是随机权威、设计构建或质量运行许可。

### V9.2 随机权威与方法资格根结果（2026-08-24）

纯文档闭合提交 `70397a1a53d539cba4d8b36fe7c863f451f71ffa`、树 `080a3968f290a2fbf12f999ff300cb1b49d67ca0` 成为唯一运行基线。随机权威仪式网页回复为 10,226 个 UTF-8 字节／`4d2859bd0bc3e40f6d678f9d2f9ce278d7630302d8116c2441a4529e5319ad2e`，四级问题均为 0，最后一行“允许生成并冻结一次V9.2全新随机权威”。仪式使用操作系统密码学随机源生成 14 个互异的 32 字节值，未打印、上传或写入 Git。权威文件为 1,805 字节／`0fc61873e58686f3cfbcb41f5f11ebe15344bba8190c4c6b55eb0da543b6ffd4`，规范自哈希 `e143cb1806923d17b61e8343559f8dc21bd5743bde7548db856b6c5bac5d3ee9`，权威束承诺 `00e59f584bcc7bc776de4e3b8855f2e78f3b0b6f94ed67aed4e1b37a49787def`。

构建运行前网页回复为 12,383 个 UTF-8 字节／`5fdbb0223f9fa4b422523e8d933db3d5e5198e4121b5ef18444d225856fae969`，四级问题均为 0，最后一行“允许运行一次V9.2方法资格根1004世界构建”。1,458 字节构建回执摘要为 `890df2d3b6860a0a94b8efba514a6c084bbecb83d741dd7354a385e4706bc7c6`。两份授权都在首个数据字节前消费；唯一构建正常退出，没有重试、重抽或补跑。

新根位于 `reports/step28_v13_v1_13_scientific_builder/design_preflight_v9_2_20260824/method_qualification_1004`，含训练／开发／审核甲／审核乙 500／500／2／2 个世界，共 28,112 个卖家、379,512 对，正对 20,080、负对 359,432。85 个文件共 1,616,648,450 字节；根清单为 4,431 字节／`4419439d9791081a11294160f83118c8d73fa8cdb34c2654bd9d02dddd82bb82`，规范自哈希 `4c14f5e936a17068d91688e9d410d11acdb2675d13b05fda435db7eff789b3dd`。独立重读全部文件后，文件集合、大小、SHA-256、行数、根／拆分自哈希、引用、数量、注册表和全局编号汇总全部闭合，错误数为 0。所有世界均使用候选 0，六类碰撞拒绝计数均为 0；无 `.building`、缓存或浏览器临时目录。

加入真实方法资格根后的提交前全仓回归运行 811 项、用时 1,890.671 秒：802 项实际通过、9 项历史既定跳过、0 失败、0 错误。回归后未发现测试夹具、`.building`、字节码、测试缓存或浏览器临时目录，根清单摘要保持不变。

方法资格根和同步文档已以提交 `1b71eaca56b492d8a6dbb2cff65e01c9ef42901d`、树 `b8135277752f7468624015984dae85ad9a957c06` 推送；80／80 个 Git LFS 对象上传完成，远端分支头与本地提交一致，工作区和 LFS 状态干净。

根仍固定为 `PASS_DESIGN_BUILD_NOT_TRAINING_QUALIFIED` 且 `scientific_use_forbidden=true`；正式种子未创建、正式行数为 0、训练未开始。本结果没有质量指标、正式数据、审核甲乙监督评估、模型或模型指标。完整构建结果见 `docs/STEP28_V13_V1_13_V9_2_METHOD_QUALIFICATION_BUILD_RESULT_20260824.zh.md`。

### V9.2 质量审计尝试 1 执行失败（2026-08-24）

方法资格根提交后的网页审查正文为 12,735 个 UTF-8 字节／`439ca71b92be3abd78fda75b9a6ec6fd15384336d05869bd84e7493b35dab882`，338 行，四级问题 0／0／0／0，最后一行精确为“允许运行一次V9.2方法资格根质量审计”；会话为 `https://chatgpt.com/c/6a8c05e4-7c28-83ed-8c89-39a9c8ecc6c6`。审查没有冒充逐行读取未上传的 1.62 GB 数据，也没有把构建成功解释为质量成功。

外部一次性质量回执为 1,695 字节／`2df0585a081d3cb37b59792919bc30003d30f3cfb0aa300eb8febfbacfd91b3c`，规范自哈希 `ffe54ceb7f1cc36e321d265b008cdf1307781e72cf16ec774cdb084f8e6b6908`。它绑定提交 `22f5c0df60a8d8208670c354fafad0f55765d8ad`、树 `b845db65534981afe0217454f24b1756c4b1ecbf`、质量策略、根清单、网页回复和唯一输出路径；回执在首个审计动作前消费，永久不得复用。

唯一质量审计运行约 10.6 秒后在 `public_uid_and_structure_closure` 阶段发布 `AUDITOR_EXECUTION_FAILED_NO_DATASET_CONCLUSION`。699 字节终态的文件 SHA-256 为 `b3b8abea330a76e781c2e1f1066b730f35e65bbb57d5b880cff21c11f8905526`，规范自哈希 `404f979309b3b39cb8c50371e4c287f8fbd2f2dd5c09555aeb079eb6b94e7b8c`。没有形成 `complete_quality_evidence.json`，没有返回逐行标签或预测，没有生成正式数据或启动训练。

根因已经精确闭合：V9.2 运行器的 `_validate_public_closure()` 调用冻结的 `_validate_endpoints()` 时漏传必需的仅关键字参数 `expected_pairs_per_world`。确切 `TypeError` 文本摘要为 `d2d9ccddff1db2e3081b7154eb119b335c261a8a2f09160250140693bca47496`，与终态一致。失败发生在任何监督真值打开之前；训练、开发真值均未读取，审核甲乙监督能力未挂载。811 项回归没有让真实根经过该生产调用路径，因此没有拦住这个低级接线错误。

本次是审计器机械失败，不是数据质量失效。成功方法资格根仍是只读科研输入，但继续禁止科研使用和训练；本次提交、网页许可、已消费回执和结果路径永久不得重跑或复用。详细边界见 `docs/STEP28_V13_V1_13_V9_2_QUALITY_AUDIT_ATTEMPT1_EXECUTION_FAILURE_20260824.zh.md`。

### V9.2 质量审计尝试 2 执行失败（2026-08-24）

尝试 2 薄包装器在提交 `e69ee68efdd2086aa9f57bfb4cfcb87964bb449c`、树 `b960600d8819c0099b29da19550025e12476b543` 固定并推送。新增测试 5／5、相邻合同 32／32、全仓 816 项均通过；全仓为 807 项实际通过、9 项历史跳过、0 失败、0 错误，用时 1,684.723 秒。网页代码审查附件为 61,661 字节／`c90ac33e671ece210e86218fadf2114738952e0e9d7a69965c735e4669a9cb49`，回复为 12,232 个 UTF-8 字节／`559795b9c2ce1e5846e5512d9bfc938626dd921193bc947776c150ee1d418506`，四级问题均为 0；同会话单次运行确认为 751 个 UTF-8 字节／`6b28acb83c7cdfa8cbd89320ba9f5951c305cc1ee20c8d949748fda491317b72`。

1,704 字节尝试 2 回执的 SHA-256 为 `f0f5b26db8bc694c5d2bf3816ba2b436822d4c60d6e8c90988c8228c9e7ad3f5`，规范自哈希 `9e611f9345eb64fdf862d4c6c76ad97ec7231f5c867f68b78f81996bbc19692b`，现已消费。唯一运行约 20 分钟后发布 733 字节终态：SHA-256 `fb03de2be889fff1f81107b3149dfa5f714872742ebc8d6ecbbdd95795754d3d`，规范自哈希 `9918a5ea2d3c05143635102529b6c341a4562cf3d80238435c6c43b5a45ef738`，状态仍为 `AUDITOR_EXECUTION_FAILED_NO_DATASET_CONCLUSION`，阶段 `freeze_28_text_2_code_slot_and_2_masks`，异常类型 `QualityProbePreparationError`。

异常消息摘要 `b8c4d7775d6c840479f67a485a294a070ce0bf174262ca8bfb1e56a933b5018a` 精确对应“解码世界序号与冻结世界权威不一致”。根因是运行器用拆分偏移加拆分内序号猜测全局世界序号，但商品代码使用的是世界在确定性打乱分配前的真实全局创建序号。用冻结标识密钥重建全部世界标识后，根的 1,004 个世界集合严格闭合，而训练 500、开发 500、审核甲 2、审核乙 2 个当前猜测序号全部错误。

尝试 2 已通过尝试 1 的漏参位置，但仍未读取监督真值、未形成完整质量证据或数据结论。尝试 2 提交、网页许可、回执和结果路径永久不可复用；专用代码、测试和临时文件按失败纪律删除。正确后继只能从冻结世界标识重建真实全局序号，不得改根或从拆分位置猜测。完整记录见 `docs/STEP28_V13_V1_13_V9_2_QUALITY_AUDIT_ATTEMPT2_EXECUTION_FAILURE_20260824.zh.md`。

### V9.2 质量审计尝试 3 完整计算并使数据集失效（2026-08-25）

尝试 3 以提交 `26a57685ef864701575831393044c39098e2ad2c`、树 `d21ccbe7c305503abafa10ef564eeeabfde4f93a` 固定两项接线修复：继续补入冻结每世界 378 对，并由冻结标识密钥重建 0 至 1003 的世界标识到拆分前全局创建序号的一一映射。真实根无标签重放确认四拆分 1,004 个世界完全闭合；训练与开发共 28,000 个卖家、100,275 个商品代码的世界序号、卖家槽和商品槽错误数均为 0。8 项尝试 3 合同、53 项相邻合同以及 819 项全仓回归全部通过；全仓为 810 项实际通过、9 项历史跳过、0 失败、0 错误，用时 1,931.870 秒。

网页端代码审查回复为 12,477 个 UTF-8 字节／`6cda8c04be24603cea8739f8e9c6b7a78fc6b11e8b2669d7ffbf7654feee5667`，四级问题均为 0；固定提交后的单次许可为 539 个 UTF-8 字节／`1db5fee101d6677d9767b90d61ac27a4a734404a8739cfaf5b051c214f06a376`，末行“允许运行一次V9.2方法资格根质量审计”。1,704 字节回执 SHA-256 为 `5f7ce2f58cb2876519cfdbb95f549d0eb7d586c7d388af9e9b8460ef950bb7f7`，规范自哈希 `70ca40d94b66a19e5a5f5508e49bcd0bff4ae71e36f689e553ef8ff1c1b917a9`，已经消费。

唯一运行约 2 小时 27 分钟后正常退出，完成全部 97 项登记观测并发布 `DATASET_INVALIDATED`。反事实文本 5 项硬门全部通过，结构 45 项硬门全部通过，原作者文本 42 项仅作描述性结果；公开代码／私有创建槽位 5 项硬门全部失败。最大单特征对称曲线下面积为 0.586433（上限 0.52），最大模型族对称曲线下面积为 0.653042（上限 0.53），最大模型族平均精确率增量为 0.033127（上限 0.01），两项百分之九十五自助抽样上界也失败。

这是非文本生成元数据可预测标签的实质捷径，不是包装异常。完整证据没有返回最强列名、逐行标签或逐行预测，因此根因只能严格定位到公开代码／私有槽位特征族，不得在本轮能力消费后擅自重开真值细分。审核甲乙真值打开次数为 0，正式数据未生成，训练未开始。完整证据为 122,519 字节／`62687a99cf41e426102c457a6b52402014c37e3e67302f1263acdf281b29301a`，规范自哈希 `c9b2e3fe562c40e810c88337236731d845c4b8677613c50354c01e90ee96ba6f`；机器终态为 1,463 字节／`e7b74cab383bf6871db24adc83a88f03b1b786653cbc944a7f021fde2ab4328e`，规范自哈希 `61bc6602cab61b489ec35d7b685babbdaf5687b2f37272dc5214d7060fecd01b`。

V9.2 方法根因此永久失去训练资格。其提交、入口、许可、回执和结果路径均不可复用；不得重跑、改阈值或删门恢复。冻结验证器是分别训练两个公开代码模型和两个私有槽位模型后取最大值，并未拼接两个视图；完整证据未公布获胜视图或列名。只读源码追踪另确认精确标题克隆允许携带人工商品码，且槽位／商品数量只做了概率独立而非固定样本的确定性配平，这两条风险必须同时消除，但不得冒充本轮未公布的获胜列。

失效清理已经完成：删除 80 个载荷／1,616,618,475 字节及 20 个 V9.2 专属策略、脚本和测试文件；原根仅余 5 份清单／29,975 字节，载荷文件和 `scripts`／`tests`／`schema` 中的 `v9_2` 活文件均为 0。删除后全仓回归运行 768 项、用时 1,696.850 秒，759 项实际通过、9 项历史跳过、0 失败、0 错误；缓存和临时目录已清零。正式 500×4 数据、审核甲乙监督评估和 M0／M1／M2／M3 仍全部禁止。详细记录见 `docs/STEP28_V13_V1_13_V9_2_QUALITY_AUDIT_ATTEMPT3_DATASET_INVALIDATION_20260825.zh.md`。

### V9.3 结构配平修复当前状态（2026-08-26）

V9.3 当前仍处于无文本、无真值、无模型的抽象设计预检阶段。训练／开发各 500 世界的独立配平计划 `design_preflight_v2_20260825` 已通过卖家槽、噪声槽、无序对、三元组暴露、拆分距离和全局重标拒绝审计；训练／开发实际文件 SHA-256 分别为 `c4a058fb97e351b033d2f4c595985251cf790b1693123bdec01fc64bcc18a4c2` 与 `d29197a5c052e6a450ea838291f740fbc39e434dd754d2bd3c91dd2bf5ba4087`。旧值经文件、构建回执和验证器交叉核对确认只是文档记录错误，不是文件漂移。联合噪声签名已由真实中文训练侧去标签汇总独立重放闭合，回执自摘要为 `a98cb5718fdbcd528274789e908d635fe3322ccb620f9e58d23d545a4e4def71`。

登记负例联合计划的前七次开发轨迹均未形成严格零违例计划：前三次退火分别停在 40、29、32；第四次缺少可审计求解诊断；第五次 11,110 个候选的通用整数规划超时；第六次把违例从 45 降到 20 后在压缩候选池超时；第七次从 45 直接求解 11,110 个候选，900 秒返回 `UNKNOWN`。这些结果只证明旧搜索和候选表示不足，未证明 5,324 项合同不可满足。失败输出、临时日志和专用路径已按文档记录后删除。

第八次后继实现撤销了未经运行的“两步候选合并”猜测，并于 2026-08-26 唯一运行。训练拆分严格重放到 20 点残差后，191、192、320、500 世界原始端点模型四次均返回 `UNKNOWN`；完整模型含 2,148,000 个布尔选项和 5,324 个计数单元，1,807.686382 秒内仅产生 2,495 个分支、0 个冲突。违例摘要为 `19dbedf0d57277c88f16d4c94f0a0efe1b337dc898461239edf2a7ab7fa01d79`，固定世界选择顺序摘要为 `662b0a49f6beecf20f25760896649366d2d6eb6cb403922324b071a548546d1d`。这证明当前独热展开表示和传播不足，不证明合同不可满足；不得靠延长同一模型时限、放宽配额或把 20 点近似解发布为计划。

开发拆分未启动，V10 输出目录、`.building`、计划、回执、文本、真值和模型均为零。第八次路径和同一 2,148,000 变量独热模型永久不可复用。下一步只能保持公开设计常数、数据、5,324 项约束和严格零违例门不变，建立等价的紧凑分解求解；训练和开发均取得独立全量重建后的严格零违例计划以前，不得进入文本构建、方法资格根或训练。

V11 已在运行前冻结为紧凑局部残差求解：每个超额／不足单元只确定性选择 `max(4, 2×偏差量)` 个贡献或支持世界，不再把 170 个超额贡献世界全部强制入模；每批先计算外部冻结贡献，并安全删除任何单独超过残差上界的端点选项。唯一运行于 2026-08-26 13:24 启动，在训练固定重放第 49,000,000 步、目标 52 后主动终止；没有启动粗修复、局部整数规划或开发拆分，没有输出目录、计划、回执、文本、真值或模型。

终止根因是上游联合噪声签名 V1 的训练就绪边界错误，而非 5,324 项登记负例合同不可满足。V1 把 17 个无任何非空描述的真实训练侧卖家折算成 1 个 `description_present_mask=00` 噪声槽，但有效机器政策要求每个合成卖家至少 2 条非空描述，身份槽也只能写入非空描述。只读重算表明 676 个来源卖家中有 648 个同时满足至少 1 个非空标题和 2 个非空描述，28 个应在联合整数化以前排除，合格原始商品行为 3,354 条。V1 与 V11 均永久不可复用；下一步先发布并独立重放训练就绪联合签名 V2，再以新版本、新输入摘要和新路径重求训练／开发登记负例计划。V11 的中止不构成数据、质量或可满足性结论。

训练就绪联合签名 V2 已发布并独立重放闭合：规范自摘要 `714237710d99eb2f31ba44b93bd3bd463e247b636c7805516e93933275af4e7f`，文件摘要 `05caa6a2b19591fdbf042384b1d31dccfde4a3f1096275ec44671f9f4b3c4d37`，独立派生表摘要 `472b7e976f9af79ec3651da24231de8d64ce5d9423986ec2d3b6cb97c28f392f`。28 个噪声槽均满足至少 1 个非空标题和 2 个非空描述。V1 载荷已删除，失败边界和摘要保留在文档。后继登记计划版本为 V12，路径 `registered_negative_preflight_v12_20260826`，必须从 V2 从头求训练／开发严格零违例计划。

V12 已机械失败关闭：训练在 55,000,000 步和既有粗修复后到 20 点残差，随后残差世界选择器把“每个超额单元希望至少选择 4 个贡献世界”的多样性启发式误作可行性硬条件；稀疏单元只有 3 个当前贡献世界时错误退出。开发、输出目录、计划、回执、文本、真值和模型均为零，对合同可满足性无结论，V12 路径永久不复用。V13 只修复该错误：选择数截断到全部可用贡献世界，但仍必须不少于实际超额量，固定批次再由完整世界顺序补足。稀疏反例回归已通过；下一次必须用新路径 `registered_negative_preflight_v13_20260826` 从相同 V2 和冻结初态干净重放。

V13 唯一运行已越过 V12 机械错误并再次得到训练拆分 20 点残差，但 71／72／96 世界残差端点模型分别在 300／600／900 秒返回 `UNKNOWN`；128 世界、284,424 个选项的 1,200 秒批进入求解后按用户明确要求终止。证据表明继续扩大高度对称独热选项不能形成有效传播，不构成可满足性或数据结论。进程已退出，开发拆分未启动，V13 输出目录、`.building`、计划、回执、文本、真值、正式数据和模型均不存在。V13 路径和扩张策略永久不复用；下一版必须冻结合法现状，只对非零残差构建可审计的局部交换／有限复合交换模型，候选解继续接受全部 5,324 项清空重建与严格零违例门。

V14 已失败关闭。训练拆分重放到 `45→38→37→36→28→24→20` 后，在任何新整数求解以前，两层补偿闭包候选数超过 250,000 条安全上限，程序拒绝截断并退出。根因是未压缩补偿闭包重新展开了过多局部候选，不是 `UNKNOWN`、不可满足证明或数据质量结论。开发拆分、V14 输出目录、`.building`、计划、回执、文本、真值、正式数据、模型和质量结果均为零，缓存为零。V14 路径、101,475 字节构造器摘要 `175d560691b57ecc9082177d904cf19a3a09c02df01965047020372bdc4d09e8` 及该闭包表示永久不复用；后继只能先做等价候选压缩或按残差方向增量列生成，不能提高上限或截断继续。

失败记录完成后，V14 专用交换求解器、补偿闭包、三项求解测试和路径引用已从活代码删除；构造器入口终止守卫阻止复用，方法政策计划根改为未分配哨兵。清理后聚焦回归 21 项通过、5 项因后继计划未发布跳过，政策自哈希 `c71b43c9c95a6213320f9c8815f5e09717827aa693caacca6c96b480502c98ca` 闭合；V14 输出、哨兵目录、缓存和残留进程均不存在。当前只允许设计并验证后继紧凑列表示，不能运行构造器、冻结方法政策、生成数据或训练。

V15 紧凑后继现已完成实现但尚未正式运行。构造器版本为 `2026-08-26-step28-v13-v1-13-construct-registered-negative-plan-v9-3-solver-v13-incremental-priced-residual-columns`，唯一输出路径为 `registered_negative_preflight_v15_20260826`。它不再一次性物化两层补偿闭包：先对当前实际残差生成完整增量列并作严格等价压缩；若主问题只有中性解，则只投影这一组实际行，针对它真正制造的少数旁路违例增量补列，再回到原状态复算。未等价候选不截断，池超过 40,000、48 次定价耗尽或任何机械异常都失败关闭且不作可满足性结论。每次真实改善后独立重建全部 5,324 项，最终仍只接受平方目标和一范数同时为零。当前 16 项构造器原语测试通过；方法政策仍为未分配哨兵，正式输出、计划、文本、真值、质量结果、正式数据和模型均为零。下一步是相邻回归与静态复核，随后才允许从 V2 干净运行 V15。

V15 随后唯一运行并机械失败关闭。训练严格到达 20 点残差；初始 26,369 条直接列无截断。第一个单世界探索种子只在内存投影为 22 点，3 个实际旁路目标新增 1,022 条列；重复种子随后被禁忌。第三次主问题到时只给出大于 24 点的较差 incumbent，而实现把允许的 `20+4` 邻域放在求解后检查，错误地让未提交越界种子终止整轮。开发、输出目录、`.building`、计划、回执、文本、真值、质量结果、正式数据、模型、缓存和残留进程均为零。本轮不构成合同可满足性或数据结论；V15 版本、路径和控制流永久不复用。后继必须在主问题内硬性限制种子总松弛，并把严格改善主问题与中性种子选择分离。

V16 后继已经实现但尚未正式运行。构造器版本为 `2026-08-26-step28-v13-v1-13-construct-registered-negative-plan-v9-3-solver-v14-strict-master-deterministic-safe-seed-pricing`，唯一新路径为 `registered_negative_preflight_v16_20260826`，三项输入路径和文件摘要继续钉住 V2。严格主问题现在硬性要求总松弛不超过当前值减一；求解器没有合格 incumbent 时不再致命退出，而是从初始直接列中确定性选择每轮最多 4 个安全种子，预测松弛只允许保持或最多增加 4，最多 12 轮。越界种子跳过。种子仅作内存事务投影，实际旁路单元、总松弛和平方目标都必须与完整重建一致，恢复基线后补偿列还须重新计算完整 5,324 单元增量。未等价列不截断，池上限仍为 40,000；最终仍只接受训练与开发各自严格零违例。17 项构造器原语通过；相邻 V9.3 回归 60 项中 55 项通过、5 项因计划未发布而跳过、0 失败。最终字节全仓回归 828 项：814 项通过、14 项有明确边界的跳过、0 失败、0 错误，用时 1,600.202 秒；构造器摘要 `716844ed787f6010a957f5008428cd1a706759b6ae9bcabaf5d7ed7c4db8d08f`，测试摘要 `cf2cd93add6b5a4502b979163ba8cb6d48149335807793907bc0c535645acbb0`。回归后缓存、随机测试目录、`.building`、正式输出和残留进程均为零。运行前门已经闭合；下一步只允许唯一运行 V16 抽象登记计划构造，仍禁止文本、真值、正式数据和模型。

V16 唯一运行已经机械失败关闭。训练固定轨迹与粗修复精确得到 `45→38→37→36→28→24→20`，全量重建一致。20 点时 19,236 条直接列的严格主问题在 120 秒无合格 incumbent；首轮 4 个安全种子预测松弛为 `20、20、20、21`，实际旁路目标为 `1、1、2、2` 个，事务投影核验全部通过。补偿定价新增 19,674 个非等价列并规范替换 873 类，池达到 38,910；再次严格求解仍无 incumbent。第二轮在原子加入前发现会超过 40,000 上限，正确失败关闭，未截断、未部分加入、未提交种子。开发、输出、`.building`、计划、回执、文本、真值、质量结果、正式数据、模型、缓存和进程均为零。这只证明四种子全量补列仍过宽，不证明合同不可满足；V16 版本、路径和控制流永久不复用。当前应先删除 V16 专用活代码并恢复后继未分配守卫，再另行冻结更窄定价方案；仍禁止进入文本、真值和训练。

V17 已把残差目标收缩为直接严格闭合：到达不超过 20 点违例后，不再建立松弛变量或要求至少下降 1，而是用全部 5,324 个冻结上下界和世界互斥约束直接寻找 `20→0` 的候选组合。初始直接列无解或超时时，每次只事务投影一个确定性安全种子，完整补偿列批次只有在总池不超过 40,000 时才原子加入；禁止截断、部分写入和提交中间违例状态。最多 48 个种子尝试，唯一新路径为 `registered_negative_preflight_v17_20260826`。任何解仍须经 500 世界全量重建确认一范数违例和平方目标均为 0。聚焦回归 32 项中 27 项通过、5 项按未发布边界跳过；最终字节全仓回归 828 项中 814 项通过、14 项有明确边界的跳过、0 失败、0 错误，用时 1,592.381 秒。构造器 120,085 字节／`4e0e5355bdf98768eafc8b78354095bb13dd265e53bc6caf101b79f4f78e94ba`，测试 42,600 字节／`85c58af9ead3cea22805a9d0bc2295f455854bbc6a61a3ba30c3160e4d0184d5`；缓存、测试临时目录、V17 输出和 `.building` 为零。下一步只允许唯一干净运行 V17 抽象构造；文本、真值、方法资格数据和 M0／M1／M2／M3 仍为零。

V17 唯一运行已失败关闭。训练精确重建 `45→38→37→36→28→24→20`。初始 19,236 条直接列的零违例主问题明确不可行；48 个安全种子中只有 6 个完整批次能在 40,000 上限内原子加入，新增数为 `3,545、888、2,624、12,617、614、311`，池最终为 39,835，规范替换累计 1,091 类。每次扩充后直接求 0 均在 120 秒内无 incumbent；其余 42 批全部过大并在加入前拒绝。故结论只是“全批补偿列永久累积表示在预算内未找到零解”，不是 5,324 项合同不可满足。开发、输出、`.building`、计划、文本、真值、数据、质量结果和模型均为零。20 点安排没有持久化是新的效率错误；后继须先冻结一个经全量重建的、小型无标签 20 点抽象检查点，再使用不要求全批永久物化的零闭合方法。V17 路径与控制流永久不复用。

训练残差检查点发布器随后补齐精确 20 门、20 个非零单元门、源码摘要复核和写盘重开验证；5 项检查点合同全部通过。清理后的相邻回归共 34 项，29 项通过、5 项因正式计划未发布而跳过、0 失败。此前最终字节全仓回归共 828 项，用时 1,583.699 秒：814 项通过、14 项有明确边界的跳过、0 失败、0 错误。检查点长轨迹没有运行，输出和 `.building` 均不存在。

用户随后明确终止长期卡在 `20→0`。网页端 GPT-5.6 Sol Pro 在完整读取旧合同、构造器、验证器、训练配平计划和联合签名 V2 后，提出过理论等价的世界块分支定价方案，但该方案仍需高工程量实现、训练／开发双拆分求解，且 14,400 秒训练预算不保证成功；本地未采纳。原 V9.3 严格零违例路线永久关闭，20 点仍不得称为合格计划、理论配平通过或不可满足证明。

当前唯一后继为 `docs/STEP28_V13_V1_13_V9_3_R1_BOUNDED_RESIDUAL_STRUCTURE_GATE_AMENDMENT_20260827.zh.md`。V9.3-R1 使用旧公开种子和固定轨迹分别冻结训练／开发合法终态，完整公开 500×12 端点和全部 5,324 项实际值／上下界／带符号偏差；理论最均衡直方图降为诊断，不根据已知训练 20 点设置新性能门。计划冻结后，在任何文本、方法根和模型以前只运行原合同已经冻结的卖家槽矩阵与噪声槽／可见结构矩阵探针。只有机械门以及固定逻辑回归、深度 2 浅树、对称 ROC-AUC、AP 提升和 9,999 次世界重抽样上界全部一次通过，才可表述为“未检出超过冻结阈值的可学习结构捷径”；任一失败立即关闭本版本，不换种子、不调阈值、不返回修改计划。审核甲乙真值和 M0／M1／M2／M3 仍全部禁止。

R1 的唯一运行随后失败关闭。训练拆分在内存中精确重放到 `20／20／20`；开发拆分完成 55,000,000 次固定搜索并经粗修复走出 `29→25→24→23→22`，第 5 轮只能 `22→22`，被严格改善守卫拒绝。原子清理后没有五文件计划根、`.building`、结构门、方法资格根、文本、真值或模型；R1 不具有任何资格且不得重跑。

用户明确决定不再优化 22 点残差。当前后继改为 `docs/STEP28_V13_V1_13_V9_3_R2_USER_ACCEPTED_RESIDUAL_22_STRUCTURE_GATE_20260827.zh.md`。R2 如实披露这是看见开发终态后的放宽，唯一算法变化是残差检查点 `20→22`；训练仍须精确 `20／20／20`，开发须精确 `22／22／22`，结构矩阵、四模型、阈值和 9,999 次重抽样不变。

R2 唯一确定性重放已经发布精确五文件计划根：训练与开发的独立全量重建分别为 `20／20／20` 和 `22／22／22`，构建回执规范自摘要 `08e49176f9c4f8e3480378578881e55d3248c2e0743198c7ecfd442875eab77f`。结构门入口重开五文件并复算两个拆分和计划对一致；49 项相邻合同回归通过。政策冻结前全仓回归运行 846 项、用时 1,692.258 秒：832 项实际通过、14 项按既定边界跳过、0 失败、0 错误。结构门通过以前仍无方法资格数据，M0／M1／M2／M3 继续禁止。

政策冻结前最终集成审查又修复三项问题：完整质量审计的私有坐标观察由常量真值改为逐行计算；方法根和质量审计对前置结构门改用与生产者一致的“删除自摘要字段后散列”规范；私有标识符扫描删除两端边界，防止完整标识符被前后字符包裹后漏检。三十三维身份特征现与商品和卖家画像一起接受精确模式、行连接及私有坐标扫描。卖家画像的冻结连接字段只有 `seller_uid`，其全集必须等于 `sellers.jsonl` 的唯一 `seller_uid→world_uid` 映射域，不能后验增加 `world_uid`。网页端最终复核为阻断／高级／中级 `0／0／0`，末行为“修复后可冻结R2政策”。最终相邻回归 81 项中 76 项通过、5 项跳过；全仓回归 849 项中 835 项通过、14 项跳过、0 失败、0 错误，用时 1,554.363 秒。49 文件路径摘要仍为 `efd87c51567d3cde6f5d3b86edeeb97da8897447362c962d97ea1307101bccce`，总字节 `4,954,664`，完整记录摘要 `5fb1cc65c7499bfe8d39bda0e7a8d171e7952789f7ea4b701bcfcb5cc36e87dc`。当前仍未实际冻结政策或创建私钥、结构门、方法根、质量结果和模型。

政策随后正式冻结，规范自摘要 `9088de1437e795bf348191da810adc98286756bd965964101e94535774ecece2`、文件摘要 `ad692d8a14cea17d610e22dc358ac41bb6af0dc7891386494908fa66d1a3f275`。一次性随机权威只输出承诺，十四个私钥值未打印。唯一前置结构门正常完成并得到有效 `DATASET_INVALIDATED_PREBUILD_STRUCTURE_GATE`：卖家槽两个模型的对称曲线下面积约 `0.5002–0.5003`，噪声可见两个模型为 `0.835981`、`0.836099`；最大单特征值 `0.768156`，最大平均精确率提升 `0.114283`，两项 9,999 次世界重抽样百分之九十五上界为 `0.837003`、`0.116693`，五项门全部失败。结果文件 83,554 字节／`e06225ba78e20003bbd4fca25fc6be659fa6560aa1405a5017e078e9b0f6c6d4`，规范自摘要 `573f29a2bb5d7202803f2a6c5f29cb18bc2d0c90a50cc5e887f5c242a44f100f`。这证明噪声可见结构仍有强标签捷径，不证明残差 22 是唯一原因。自然文本、方法资格根、审核真值、M0／M1／M2／M3 和训练结果仍全部为零；R2 永久关闭并必须按失败纪律清理。

## 6. 固定评估指标

最终每个模型必须分别报告平均精确率、梯形精确率—召回率曲线下面积、接收者操作特征曲线下面积、精确率、召回率、F1、特异度、平衡准确率、马修斯相关系数、布里尔分数、对数损失、假阳性率 1% 时召回率，以及原始和世界等权混淆矩阵。

检索评估必须报告平均准确率均值、平均倒数排名、前 1／3／5／10 召回率和前 1／3／5／10 归一化折损累计增益。平均精确率与梯形精确率—召回率曲线下面积必须分别计算，不能混称。区间和模型比较按世界进行 9,999 次配对重抽样，不能把 189,000 个卖家对当成相互独立样本。

## 7. Git、清理和失败纪律

本轮清理的父提交为 `8f935adae741dce83f505edb3ef2ef9384d998ae`，质量失效关闭提交为 `51eb92c`。删除后的全仓回归已运行 768 项、用时 1,777.665 秒：759 项实际通过、9 项按历史记录跳过、0 失败、0 错误。回归后未发现字节码、测试缓存、浏览器临时目录、`.building` 或测试夹具残留；五份清单、小型终态、唯一已消费回执和冻结方法基线均已重新核验。Git 大文件状态没有待推送对象，提交范围恰为 80 个路径；接手时仍须核对本地与远端分支头一致。不得把无关历史报告加入提交。

失败实验必须先在文档记录运行编号、阶段、根因、结论边界、关键摘要和不可复用承诺，然后删除失败载荷、缓存、临时目录和该失败版本专用代码。V9.1 根已失去质量资格：只保留根／拆分清单等小型谱系，72 个数据文件已删除。V9.2 根也已取得完整失效结论并完成清理：80 个数据载荷／1,616,618,475 字节已删除，仅保留五份清单／29,975 字节、完整证据、小型终态、已消费回执、文档和 Git 历史。

## 8. 恢复后的严格动作顺序

1. 先阅读本交接和 V9.1 尝试 1 包装失败与质量失效记录；
2. 已完成失效载荷与尝试专用代码清理；先核验五份清单、小型终态、已消费回执和冻结方法基线仍在；
3. 删除后的 768 项全仓回归、Git 大文件状态和提交范围核验已经通过；确认提交 `51eb92c` 及其文档同步提交均已推送；
4. V9.2 科研协调合同、实现、回归和提交后精确字节复审已经完成；
5. V9.2 初版提交 `6eb0655db883a2f7478e0123fa770858e54b54b9` 已被提交后网页复审否决；第一轮修复提交 `2a4eedf8700efea76e81cf0bea2ea12c8866988c` 已获准进入随机权威仪式前审查；
6. 可选接口闭合提交 `c3449a3b334a95beb83652c309afd0e85ef5af73`、最终全仓回归和精确网页复审均已通过；纯文档闭合提交 `70397a1a53d539cba4d8b36fe7c863f451f71ffa` 为本次运行基线；
7. 随机权威仪式和 1,004 世界构建分别取得独立单次放行并已各执行一次；随机权威和构建回执均已消费，不得重试、重抽或复用；
8. V9.2 方法资格根已经成功发布并完成独立全量持久化核验，但仍禁止科研使用和训练；
9. V9.2 质量审计尝试 1 已取得独立许可并唯一运行，但因 `_validate_endpoints()` 必需参数漏传而机械失败；完整质量计算、监督真值读取和数据结论均未形成，本次许可、回执、提交和结果路径永久不得复用；
10. 尝试 2 已修复漏传 378，但又因把拆分内序号误当全局创建序号而机械失败；监督真值仍未读取，质量结论仍不存在；
11. 尝试 2 专用代码和临时产物必须删除，只保留失败文档、小型终态、已消费回执和 Git 历史；其提交、许可、回执和结果路径永久不可复用；
12. 尝试 3 已正确重建世界全局序号并完成全部 97 项质量观测，取得有效 `DATASET_INVALIDATED` 终态；V9.2 根永久失去训练资格；
13. V9.2 的 80 个大型载荷、20 个运行专用策略／脚本／测试、缓存和临时文件已经删除，只保留五份清单、完整证据、小型终态、已消费回执、文档和 Git 历史；
14. 删除后 768 项全仓回归和保留文件／Git 大文件状态核验已经通过；提交并推送本次关闭字节；
15. 后继只能建立全新 V9.3 合同：人工商品码不得进入正式模型表面，500 世界控制者分组须对卖家槽位对确定性均衡，商品数量和缺失模式须绑定均衡噪声层，并在大规模文本构建前加入同族结构／统计预检；
16. 只有全新版本重新取得完整质量通过终态后，才能申请正式 500×4 数据生成。

当前允许：提交推送 V9.2 小型完整证据、终态、清单和同步文档，并设计、审查和实现消除公开代码／私有槽位捷径的 V9.3；实现阶段只能运行合同测试、全仓回归和未读取正式真值的设计预检。当前禁止：重跑 V9.2 的任何质量尝试、复用任何 V9.2 网页许可或已消费回执、覆盖其输出路径、恢复或训练 V9.2 根、立即生成正式中文数据、打开审核甲乙真值做监督评估、训练或评估 M0／M1／M2／M3。

## 9. 接手优先阅读文件

1. 本文；
2. `docs/STEP28_V13_V1_13_SCIENTIFIC_EXPERIMENT_CONTRACT_20260810.zh.md`；
3. `docs/STEP28_V13_V1_13_QUALITY_AUDIT_C_AMENDMENT_20260811.zh.md`；
4. `docs/STEP28_V13_V1_13_V9_2_SCIENTIFIC_RECONCILIATION_CONTRACT_20260823.zh.md`；
5. `docs/STEP28_V13_V1_13_V9_2_METHOD_QUALIFICATION_BUILD_RESULT_20260824.zh.md`；
6. `docs/STEP28_V13_V1_13_V9_2_QUALITY_AUDIT_ATTEMPT1_EXECUTION_FAILURE_20260824.zh.md`；
7. `docs/STEP28_V13_V1_13_V9_2_QUALITY_AUDIT_ATTEMPT2_EXECUTION_FAILURE_20260824.zh.md`；
8. `docs/STEP28_V13_V1_13_V9_2_QUALITY_AUDIT_ATTEMPT3_DATASET_INVALIDATION_20260825.zh.md`；
9. `docs/STEP28_V13_V1_13_V9_1_DESIGN_BUILD_RESULT_20260822.zh.md`；
10. `docs/STEP28_V13_V1_13_V9_1_QUALITY_AUDIT_ATTEMPT1_EXECUTION_FAILURE_20260823.zh.md`；
11. V9.2 尝试 1、尝试 2 和尝试 3 的小型终态及尝试 3 完整证据；
12. `docs/PROJECT_PROGRESS.md` 最后一个 Step28-v13 段落。
