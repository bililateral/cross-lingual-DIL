# Step28-v13 v1.13 V9.4.1 公开投影 V1 授权实现就绪记录

日期：2026-08-31

状态：`AUTHORIZATION_IMPLEMENTATION_READY_NOT_ISSUED_NO_PROJECTION_NO_TRAINING_NO_TRUTH_READ`

## 1. 本阶段结论

一次性、无标签、四拆分公开投影的授权合同、机器政策、签发器、三阶段执行器和失败清理边界已经实现并通过当前回归。该结论只表示“授权机制可以进入提交后复核”，不表示授权已经签发，更不表示正式投影、模型训练、审核预测或真值读取已经发生。

当前正式中文 V9.4.1 数据根及其训练资格保持不变。公开投影、M0／五个 M1／M2／M3-base／M3-joint 训练以及审核甲乙真值仍全部未执行。

## 2. 科研相关性审查

审查意见被强制分为科学阻断、可复现性缺陷和超范围过度设计。仅修复了两个会影响普通失败恢复和冻结重放的可复现性缺陷：

1. 失败清理不再因为同名 `consumption.json` 存在就删除尚未真正消费的权限；现在必须先验证完整、自哈希且与本次授权精确绑定的消费回执。
2. 成功发布后，准备和编码入口均拒绝重新进入；终结恢复必须验证已有完成回执，并要求它与重新计算结果逐字段相同，不能用内存中的成功结果掩盖磁盘错配。

数字签名、私钥回传、网络攻防和操作系统加固建议不属于本科研合同的威胁模型，已按超范围过度设计拒绝。网页端 GPT-5.6 Sol Xhigh 对最终六个附件复核后确认：上述两项缺陷均已最小闭合，新增科学阻断为 0、新增可复现性缺陷为 0；该意见只作辅助证据，采纳结论来自本地代码、测试和边界复核。

## 3. 验证结果

授权及相邻公开投影合同测试通过 44／44；第七版回归分区合同通过 5／5，合计 49／49。合同静态校验状态为 `PASSED_PUBLIC_PROJECTION_AUTHORITY_CONTRACT_NO_EXECUTION`，并确认正式投影、模型训练、监督读取和审核真值读取均未授权或执行。

第七版当前回归发现 1,020 项测试，精确分离 34 项冻结 V9.1 历史测试和 5 项已由前代结果证明通过的发现边界测试，共 39 项；当前调度 981 项。一个冻结类在装置阶段整体跳过，逐标识登记其 20 项未启动测试；实际运行 961 项，正常成功 954 项，记录 8 个既有跳过事件，失败 0、错误 0，用时 2,156.330 秒。结果明确为 `was_successful=true`、`training_authorized=false`、`audit_truth_authorized=false`。

结果文件为 7,563 字节，SHA-256 为 `6522debe25e321c78db2b3cec87138f3cea6a7f9c28593bc9c3c765402950efa`，规范自哈希为 `7d475979cd974d215066dbab51e641cdacf6ebe18c2265c7a2a6e43638d0b53b`。结果绑定的政策规范自哈希为 `3bfaaab01ccc8a592d4e29560c84caef722f499f35993c28323c8e9702067411`。

开发期间首个第七版回归在输出结果前发现聚合端错误地从第六版顶层读取不存在的历史钉字段，已主动停止；未形成结果文件或正式实验产物。修复为统一沿第六版追溯第五版历史钉，并增加实际执行末端聚合的回归测试，随后才启动上述干净运行。

## 4. 后继边界

下一步只能提交、推送并复核本里程碑的精确提交与树。复核闭合后，方可另行签发一次性公开投影权限。公开投影成功并重新验证以后，仍需另起训练／开发监督授权；本阶段不得投影、训练、生成审核预测或打开审核甲乙真值。
