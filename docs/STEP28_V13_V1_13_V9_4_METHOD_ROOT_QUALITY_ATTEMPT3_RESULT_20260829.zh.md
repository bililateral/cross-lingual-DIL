# Step28-v13 v1.13 V9.4 方法根质量审计尝试三正式结果

## 1. 正式结论

质量审计尝试三已在提交 `d07bf52cc52b5aecff2d5e44253906d2fe460a6e` 上唯一运行并正常退出。机器状态为 `PASSED_METHOD_ROOT_QUALITY_ELIGIBLE_FOR_FORMAL_500X4_APPLICATION`，二十项硬门全部通过。V9.4 的 1,004 世界方法资格根因此取得质量通过结论，可以另行申请正式 500×4 中文合成数据生成。

这不等于正式训练数据已经生成。结果继续明确 `training_qualified=false`、`formal_500x4_generated=false`、`m0_m1_m2_m3_training_authorized=false`。审核甲乙真值未打开，M0／M1／M2／M3 尚未训练或评估。

## 2. 运行与谱系

一次性授权文件为 1,313 字节，SHA-256 为 `46a1952fe5459cc8366cc5a4d041028c2b292840d72a6a8cb803a7ced76a7ce3`，规范自哈希为 `03c6aaf2a757a6c316fa6e64e05285489561a34ebf207971779d92a66a23a982`。它绑定提交 `d07bf52`、树对象 `b74506e86c72708a475a1a5c1867ea79901cf61c`、政策、主程序和方法根清单，并明确禁止审核真值、正式 500×4 和模型训练。

授权于 2026-08-28 22:54:55（亚洲／上海）在首个数据行前消费。消费回执为 845 字节，SHA-256 为 `a72ec0f5968e7eaf8ddc90d55a4ed20fd8dc69ecac884bb47f8347a1d480cd80`，规范自哈希为 `5c24c3ece8490774f627ee6cc3512e1be784618104741d2265299683c281a353`。结果于 2026-08-29 00:22:32 发布；同一尝试不可重跑。

唯一成功结果 `quality_result.json` 为 15,785,893 字节，SHA-256 为 `e7902667d1243221a3ff0ab8e30742ba52393acc8fd9dd4a3944d4e7dddfcf5e`，规范自哈希为 `886f86fffee6435977156c1f0a2d6520c79b934192332cfeffa1df2320036b98`，独立复算一致。成功路径按设计只发布该文件；`terminal.json` 只用于异常路径。运行期三十个冻结矩阵文件约 2.16 吉字节，结束后临时根已删除。

## 3. 主要质量结果

训练和开发各完整重放 500 个世界、189,000 对。全部文本和两张 `189000×33` 身份矩阵先冻结、散列、关闭并只读重开，随后才各读取一次训练／开发标签、控制者成员关系和检索相关性，并与冻结调度独立重放逐行逐序一致。审核甲乙语义读取均为零，结果不返回逐行标签或预测。

反事实文本硬门保持商品支持、缺失位置、时间桶、卖家对、身份历史和标签不变，只用公开拆分、世界标识和卖家集合进行控制者盲作者风格错排。结果为：

- 十四模型最大对称接收者操作特征曲线下面积 `0.5043334866`，门限 `0.53`；
- 十四模型最大平均精确率提升 `0.0010382102`，门限 `0.01`；
- 三百四十六个单特征最大对称接收者操作特征曲线下面积 `0.5053036085`，门限 `0.52`；
- 9,999 次按世界配对重抽样的曲线下面积九十五分位上界 `0.5103058705`，门限 `0.53`；
- 平均精确率提升九十五分位上界 `0.0020170613`，门限 `0.015`。

原始文本的描述性作者风格模型最高对称曲线下面积为 `0.6580058091`，平均精确率为 `0.1023035395`。错排后同族探针回到接近随机，主题视图在原始和错排表面均接近随机；这支持“原始可学习信号来自有意保留的作者风格身份线索，冻结探针未检出超过门限的剩余可见捷径”。严格边界仍是“未检出”，不能宣称任何可能模型都不存在捷径，也不能把描述性差异升级为带置信区间的正式因果效应。

三十三维身份正对照在独立开发拆分取得接收者操作特征曲线下面积 `0.9052126321`、平均精确率 `0.8129906760`，明显超过 `0.65／0.15` 门限。这证明当前合成方法根包含由实际可见身份历史解析得到的可学习身份信息，但不证明真实中文市场外部泛化，也不证明 M2 必然优于 M0／M1／M3。

## 4. 网页端结果级复核

网页端 GPT-5.6 Sol Pro 对四个精确附件复算文件摘要、结果自哈希、政策自哈希、二十项硬门、逐模型指标、门限方向、逐世界回执聚合、真值时序和结论边界。它确认原始作者风格与控制者盲错排结果的解释在当前冻结合同下成立，并指出 9,999 次重抽样向量和三百四十六列完整得分只保留摘要，不能从四个附件逐元素复算；这是已披露的聚合证据边界，不是结果矛盾。

最终复核为阻断／高级／中级／低级 `0／0／0／0`，接受“方法根质量通过并允许另行申请正式 500×4”，同时明确正式数据尚未生成、模型训练尚未获准。本地逐项核对后接受这一窄结论。

## 5. 下一动作

下一步是冻结新的正式 500×4 生成合同、输出路径和一次性授权，明确四个拆分各 500 世界及审核甲乙继续盲封的边界。只有正式数据生成、独立质量确认和训练资格签发完成后，才可按科研合同训练并比较 M0、M1、M2、M3；最终模型报告必须包含平均精确率与梯形精确率—召回率曲线下面积的独立值，以及平均准确率均值、平均倒数排名、前 1／3／5／10 召回率和前 1／3／5／10 归一化折损累计增益。
