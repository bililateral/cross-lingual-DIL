# AI 科研交接补充：Step 7-v4+ / Step 28-v13

更新时间：2026-08-09

本文件只记录 `docs/AI_RESEARCH_HANDOFF_20260719.zh.md` 之后的新事实。原交接文档以及 Step28-v12.1 的历史同步清单和测试原字节不得改写。`docs/PROJECT_PROGRESS.md` 是持续更新的全局进度文档；历史清单中对其 2026-07-20 字节的记录只代表当时快照，不要求当前工作树回退。该单一例外由
`reports/step28_synthetic_chinese_dataset/design_preflights/v1_12_cleanroom_20260803/historical_manifest_waiver_receipt.json`
精确绑定；任何第二项失败或已绑定字节漂移都会使该豁免失效。当前事实以本补充和本补充列出的专项合同、结果审计为准。

## 当前活动阶段

Step 7-v3 已完成正式 GPU 打分，但没有区分出唯一编码器，也没有证明高分管线中的增益来自编码器。随后发现五个 tokenizer 下均有约六成卖家被 512-token 单窗口截断，因此 v3 只能作为失败的单窗口敏感性记录。其代码、policy、tests、runner 和结果文件已删除，只保留 `docs/STEP7_V3_CLEAN_SOURCE_SELECTION_20260722.zh.md`。

Step 7-v3.1 后来已完成正式 GPU 编码和修正后的折内特征/L2 选择，但没有区分出唯一编码器，也没有证明编码器增量。原始编码器比较中 E5 对 LaBSE 的主口径 AP 差仅 `+0.000309`，分组 bootstrap 95% 区间为 `[-0.040881, 0.043090]`，而且其他口径改由 LaBSE 排第一；完整管线第一名相对无编码器迁移特征对照反而为 `-0.024652`，95% 区间 `[-0.069212, -0.000299]`。这是编码器选择和归因的负结果，不能单独回答哪套完整英文来源流水线应成为当前定义的 M0。

Step 7-v4 已完成并冻结。它不再把 Step 3 卖家摘要误称为完整文本，而是回到两个固定原始快照，对有效边界内每条商品的完整标题和描述做身份删除、无遗漏共享分块和卖家两级聚合。v4 没有找到稳定 M0；作者风格编码器也没有提供可靠增量。

当前最新复核是 Step 7-v4.1。它完全删除 PCM、mStyleDistance 和 `stylometry22`，只复用 v4 冻结的 E5/LaBSE 完整商品分块聚合与 legacy18，在完全匹配的数据和分折上比较 L2 逻辑回归、RBF-SVM 与 LightGBM。所有选择、超参数、阈值和重复外层评估仍只用 401 条 train；已反复查看的 valid 只在两道不可变锁后作描述性诊断，历史 test 标签继续封存。

## 当前 M0 权威定义

当前 Step 7 → Step 28 主线中的 M0 是“只用真实英文马甲标签训练和选择，随后冻结并给 Step 28 提供基础分数的完整英文来源分类流水线”。它不是编码器名称，也不要求编码器必须显著超过 `legacy18`。只要完整流水线能按冻结的同一输入合同在 Step 28 数据上产生分数，`legacy18`、编码器特征及其融合都可以参加 M0 选择。

Step 28 固定比较同一个冻结来源模型：`M0` 不适配；`M1` 加五份按
world/C40 分层、整 33 维向量置乱且 pair 端点不重合的保守匹配对照模块；
`M2` 加只用合成中文身份训练集拟合的身份迁移模块。M1 不声称
controller-disjoint，不能描述成严格无身份关联的因果 null。主要方法价值
由 `M2-M1` 和 `M2-M0` 判断。编码器相对无编码器对照的增量属于独立消融，
只限制“编码器贡献”表述，不是 M0 资格门。

旧 Step27 文档中 `M0=真实中文 residual baseline` 只属于旧实验角色；Step 7 历史产物中的 `no_transfer_capable_m0` 只表示编码器没有通过额外归因门，不能解释为当前定义下不存在 M0。完整覆盖说明见 `docs/STEP7_STEP28_M0_ROLE_DEFINITION_20260724.zh.md`，其定义优先于早期文档中的同名符号。

## v3 历史结论与 v3.1 修复边界

- `same_market_bool`、`same_source_dataset_bool` 只作标签关联审计，不能进入任何可选择模型。
- 主 AP 在市场/来源分层内部计算后宏平均，防止分层标签比例或其文本代理制造赢家。
- v3 的原始分层 AP 排名为 GTE `0.733250`、MPNet `0.721820`、LaBSE `0.719920`、E5 `0.708868`、BGE-M3 `0.609077`；GTE 与 MPNet 的 bootstrap 区间跨零，且其他 AP 口径排名不一致，不能声称 GTE 最强。
- v3 的 GTE+迁移特征为 `0.911597`，但无编码器的仅迁移特征对照为 `0.911909`，所以编码器增量未获支持。
- v3.1 的五个原始编码器只比较完整字段分块的“字段等权余弦”；不训练分类头，不加迁移特征。
- v3.1 当时称为“M0 候选”的 `5 × 2 = 10` 项实际是编码器管线消融；两个无编码器模型只用于匹配归因，一个捷径模型只用于审计。该历史候选边界不能覆盖当前定义下的全部完整 M0 流水线。
- LR/L2 使用 Newton + Armijo 回溯，只有归一化最终梯度达到 `1e-9` 门槛才可记录收敛。
- E5 只作连续性报告，不改变排名或保送。
- 英文 test 已被历史开发使用，只能在验证选择冻结后作内部诊断。

v3.1 的历史 CPU tokenizer 预检为 `855` 个卖家、`4198` 个非空字段、`5378` 个共享块；其正式结果和失败结论保留在对应报告与协议中，不再作为当前实现。

## v4 当前冻结状态

- 原始结构审计在不读标签的情况下确认 Agora 两条“卖家”其实是长描述续行；对应独立 valid 组件固定隔离。有效边界为 `109,756` 条商品、`733` 条 pair、`853` 个卖家。
- 正式 CPU public/private 准备已经完成并通过逐项重放：`33,434` 个全局唯一清洗文本；train 保持 `401=116 正+285 负`，valid 为 `151=42 正+109 负`；test 未物化。
- 共享 token 预算固定为 `256`，不超过 LaBSE 的原生窗口；PCM、mStyleDistance、E5 的原生窗口保持 `512`，代码禁止向上改写模型窗口。实际 `SentenceTransformer.tokenize()` 与底层 tokenizer 必须逐块同 ID 哈希。
- `sentence-transformers` 固定为 `5.6.0`；真实 `BatchEncoding` 按映射接口读取。Linux 全量编码前必须逐个真实加载四模型，对最长共享块做两次字节一致的 CUDA 冒烟。
- 最小 GPU 包只含 `7` 个文件、`25,653,396` bytes；14 条禁止路径明确覆盖原始源、v4 私有文件和两份父标签，预期只回传 11 个紧凑结果文件，不发布 embedding 矩阵。
- v4 正式 GPU 编码已经完成：`33,434` 个唯一文本形成 `41,808` 个共享块，其中 `4,226` 个文本需要分块、最大 `23` 块；四个 tokenizer 超预算数均为 `0`，不发布 embedding 矩阵。
- 初次 CPU 选择因 Armijo 目标函数比较落入 float64 分辨率平台而误报未收敛；报错中的 `24.700000000000006` 只是 `24.7` 的正常浮点显示。独立数值补丁不改数据、特征、L2 网格、指标或 `1e-9` 梯度门槛，也不重跑 GPU。正式复跑的 `32,423` 次拟合全部按原门槛收敛，数值驻点兜底使用 `0` 次。
- 训练主口径暂列第一的是 `fusion__legacy_e5_stylometry`，但相对 `control__legacy18` 的分量等权 AP 只高 `0.000939`，95% 区间跨 0；同时超过所有候选的概率只有 `0.395`。去除完整文本克隆并完整重训后，它降到第 9，五个种子胜率为 `0`。
- 匹配的四编码器比较没有唯一赢家，预注册作者风格候选也没有稳定超过简单文体或 E5 对照。旧 valid 锁后诊断同样由 `legacy18` 优于训练赢家，历史 test 标签未读取。
- 正式状态为 `no_stable_unique_provisional_m0`：完整流水线赢家受克隆结构影响且排序不稳定，因而不能冻结任何候选为 M0。编码器和作者风格增量失败是另一个独立结论，不是 M0 资格条件。

当前协议：`docs/STEP7_V4_RAW_ITEM_AUTHORSHIP_SOURCE_SELECTION_20260722.zh.md`。

正式结果审计：`docs/STEP7_V4_RESULT_AUDIT_20260724.zh.md`。

## v4.1 无风格分类器复核

- 候选为 7 组无作者风格特征 × 3 类分类器，加 1 个空对照，共 22 项。主训练与完整去克隆训练分别执行 `43,286` 和 `43,498` 次嵌套拟合，全部通过数值合同。
- 包含克隆的完整 train 上，完整流水线 `LightGBM + legacy18 + LaBSE` 暂列第一，分量等权 AP 为 `0.944873`；`LightGBM + legacy18` 为 `0.939309`。二者差 `0.005564` 且 95% 区间 `[-0.009726, 0.022047]`，只能说明 LaBSE 独立增量未获支持，不能据此取消前者或后者的 M0 候选资格。
- 总体第一名五个种子只赢 `1/5`；相对次优的 AP 差为 `0.002114`，95% 区间跨 0；同时超过全部候选的概率仅 `0.4036`。
- 对相同 `legacy18`，完整集上 LightGBM 的 AP `0.939309` 显著高于 RBF-SVM `0.901967` 和 L2 `0.863341`，证明 v4 固定 L2 的比较范围确实过窄。
- 精确文本去克隆后剩 `286 = 27 正 + 259 负`。被删除的 115 条中有 89 个正例，即原正例的 `76.7%`，精确文本重合是明显标签捷径。原 LightGBM 赢家降至第 10，五个种子胜率为 0；L2 `legacy18+LaBSE` 改列分量 AP 第一，但其 pair AUC/AP 只有 `0.433004/0.259182`。
- 去克隆集只有 8 个含正例的连通分量，且 20 个正例集中在同一个 `40 = 20 正 + 20 负` 的巨型分量。分量等权和 pair 等权指标因此强烈分歧，不能依据其中较高者宣布成功。
- 旧 valid 上最佳 C0 继续优于训练暂列赢家，编码器增量未复现。两个落盘模型对 valid 的重放概率与开标签前盲算文件逐位一致，历史 test 标签未读取。
- 对当前 M0 选择有效的正式状态是 `no_stable_unique_current_best_style_free_pipeline`：完整模型排名不稳定，尚不能冻结 M0。历史字段 `no_transfer_capable_m0` 命名过度，只表示没有编码器候选通过相对 `legacy18` 的额外归因门，不等于当前定义下不存在 M0。

v4.1 正式审计：`docs/STEP7_V4_1_STYLE_FREE_CLASSIFIER_AUDIT_20260724.zh.md`。

下一步数据边界：在收集前冻结少量完整流水线候选，新收集互不连通、无精确文本克隆的真实英文正向身份分量，并作一次性前瞻确认。新的英文确认集直接选择最稳定的完整来源流水线；是否含编码器、以及编码器是否超过 `legacy18`，只作为并列消融报告。

## 2026-07-27：v4.2 新折分种子复跑

Step 7-v4.2 在 Windows CPU 上用五个从未用于 v4.1 的外层种子完整重跑相同 401 条 train、22 个候选、全部内层调参和去克隆消融。它没有读取旧 valid 或历史 test 标签，也没有改变候选、特征、网格或排序口径。

原 operational primary `LightGBM + legacy18 + LaBSE` 在完整集汇总中由第 1 降至第 2；新第 1 为 `LightGBM + legacy18 + E5 + LaBSE`。两者分量 AP 差仅 `0.002655`，95% 区间跨 0，新冠军只赢 `2/5` 个种子。去克隆后原 primary 排第 10，汇总第 1 改为 `L2 + legacy18`，但后者 pair AUC 仅 `0.351780`，并且也未赢得任何单种子。结论仍是 `no_stable_unique_current_best_style_free_pipeline`，当前没有正式选定 M0。

该复跑只增加同数据折分稳定性证据，不是独立数据确认。完整审计见 `docs/STEP7_V4_2_REPEAT_STABILITY_RESULT_AUDIT_20260727.zh.md`。

## 2026-07-27：废弃产物清理

已按显式 allow-list 删除未进入当前依赖闭包的 Step28-v10/v10.1 派生应用产物和本地 Python 缓存。v6–v11 中被 v12.1 同步清单哈希绑定的最小跨版本审计输入继续保留，不能因其历史结论失败而误删。完整清单和原因见 `docs/WORKSPACE_DEPRECATED_ARTIFACT_CLEANUP_20260727.md`。

## 2026-07-29：Step 28 operational M0 与正式中文合成数据

为继续 Step 28，现固定 `LightGBM + legacy18 + LaBSE` 为 operational
M0。这里固定的是从身份脱敏的完整 seller 文本、legacy18、LaBSE 到分类
概率的整条英文来源流水线，不是单独编码器。该选择提供可复现操作基线，
不推翻 Step 7-v4.2 的稳定性负结果，也不得写成“已由独立新英文数据证明
唯一最强”。权威角色定义见
`docs/STEP7_STEP28_M0_ROLE_DEFINITION_20260724.zh.md`。

`dataset_smoke_v3` 只保留为工程开发记录，禁止用于 M1/M2 训练、调参或
正式评估。正式版本由
`docs/STEP28_V13_TRAINING_READY_SYNTHETIC_CHINESE_RELEASE_CONTRACT_20260729.zh.md`
约束：train、development、audit_a、audit_b 各 500 个 world，每世界 28
个 seller、378 个完整 pair 和 40 个机制分层分类 pair。分类标签唯一公式
是 `int(controller(left)==controller(right))`；不得读取英文标签、真实中文
标签、M0 分数或 adapter 结果来造标签。Audit A/B 另各有每世界 4 个查询和
每查询 27 个同世界 gallery，用同控制者公式生成 qrels。

M0 在合成中文上保持冻结；M1 只用 train 内按 `(world,C40/非C40)` 分层、
端点不重合的五份整行身份33维置乱矩阵训练同构适配器；M2 用未置乱的
train 身份33维训练。development 只定阈值和校准，Audit A/B 是固定留出，
不是本机人员盲法。主比较为 `M2-M0`、`M2-M1均值` 和相对最差 M1 seed。

当前仍处于 `IMPLEMENTATION_LOCK_IN_PROGRESS`：正式四把私有结构密钥和
正式四 split 数据均尚未生成。构建器已固定递归发现的 33 文件源码闭包，
任何成员增删或字节漂移都会拒绝运行；所有使用旧构建器字节的精确预检只
是历史诊断。

截至 2026-07-30 最终审核前，历史科研实现合同
`b85798d7d8b446f32847f90aaf9e59db14a0e181164632c3c3ea6c17822ad73b`
下的四份 500-world 精确预检曾全部通过并登记到 training-ready overlay：

| split | 最大对称 AUC | world bootstrap 95% 上界 | 身份33维秩 |
|---|---:|---:|---:|
| train | 0.509703 | 0.516557 | 31 |
| development | 0.508037 | 0.515723 | 29 |
| audit_a | 0.509954 | 0.517763 | 30 |
| audit_b | 0.505748 | 0.513693 | 32 |

四个 split 均低于预注册的 `0.52/0.53` 捷径门，33 个身份特征均有非零
支持；train 的“不得有全零列”强制门已实际启用并通过。矩阵仍有明显共线
和秩亏，后续 adapter 必须使用冻结正则化并报告秩与条件数，不能把单个
系数解释成独立因果效应。Step28-v13 回归当前为 80 项通过、1 项旧
execution-blocked lock 因精确父字节漂移按声明跳过；完整仓库回归为
349 项通过、7 项按既有声明跳过、0 失败。7 项中 6 项来自已撤销的
Step28-v11 测试类，1 项为上述旧 lock。

上述结果不等于正式数据已经生成，也不等于 M1/M2 效果成功。

## 2026-07-30：最终审核 NO-GO 与 remediation

三路最终审核发现四项私钥仪式前阻断：

1. 父 draft 要求的确认性功效 artifact 从未生成，路径与哈希一直为 null；
2. 已有私钥目录的 recovery 只检查内部自洽，未完整拒绝公开、design-only
   或已泄露 commitment，也未强制目录和 receipt 精确 schema；
3. 精确预检摘要未强制绑定 checkpoint、OOF 分数和 bootstrap 原始统计，
   登记器无法拒绝人工伪造但字段自洽的 PASS 摘要；
4. target release claim 被误读成当前 bytes-ready，AP/PR-AUC 和 MAP/MAP@10
   口径也不够明确。

因此私钥仪式继续禁止，overlay 的四份精确预检登记已清空。旧四份报告和
349/7/0 回归只保留为修复前历史证据；源码闭包改变后必须全部重跑。

本 training-ready child 不会为未知的 M1 相关性、world ICC 和 score 分布
填入有利数字来伪造 5,000 次 Monte Carlo 功效。它在任何正式私钥前改为
固定四 split 各 500 world 的估计性设计：报告效应和 world-cluster 配对
bootstrap 区间，禁止“确认性功效已认证”或二元成功声明。固定样本量敏感性
artifact 为
`training_ready_fixed_sample_sensitivity_v1_20260730.json`，SHA-256 为
`cd8464d6efb9be16f98614a785dfefa628e35ea99f533fccc01527831f24a3bc`；
机器科研规则由
`schema/step28_v13_training_ready_scientific_contract.json` 冻结。

C40 同时明确为使用合成 controller oracle 在正负及机制 strata 内抽样的
case-control 设计，不再称为标签盲候选。它没有把 selection 字段暴露给
模型，但 estimand 仅限机制分层合成总体，不支持自然 prevalence、自然校准
或真实地下市场泛化声明。

截至 2026-07-30，本轮 remediation 已完成本地实现闭环，但仍未恢复
GO：私钥恢复会重算并拒绝正式公开流、design-only 和已泄露 commitment，
要求四份私钥加一份 receipt 的精确成员集、精确 schema、自哈希和非
symlink/非 Windows reparse 属性；精确预检现落盘并绑定 14 维原始投影、
标签、fold、三模型 OOF、9,999 个 bootstrap 统计和各阶段 checkpoint，
登记器会重算关键统计。分片/总发布清单也显式绑定机器科研合同，最终化器
拒绝 JSONL 重复键和重解析点成员。当前科研实现合同 SHA-256 为
`14970a98f2f9a7f37223d5b515874206063aa7b8b7102cf88c7b60748cc4dae5`；
23 项针对性回归通过、0 失败。新的四份 500-world 精确预检、完整回归、
三路复审、Git 基线冻结和私钥仪式均尚未完成；正式私钥、正式分片和最终
发布清单仍全部为 0。

随后第一次新 train 500-world 预检的摘要虽通过 0.52/0.53 门，登记器在
独立重放 checkpoint 时拒绝了它：生产标签证据按既有 schema 保存为规范
字符串 `"0"`/`"1"`，登记器却误要求 JSON 数字，导致 20,000 行类型检查
全部失败。现已修正为先要求规范字符串，再逐行转换并与 int8 标签向量
核对；旧 checkpoint 的其余证据已完整重放通过。由于该修复改变稳定科研
实现合同，旧 train v5 报告仅保留为无效诊断，不能登记，所有 split 仍须
重新运行。当前科研实现合同 SHA-256 为
`67b8c720e7287fb5742b5417a98be6578a87b657ca58858bc3992713510f660a`；
私钥仪式和正式数据生成继续禁止。

按科研实现合同
`67b8c720e7287fb5742b5417a98be6578a87b657ca58858bc3992713510f660a`，
四个 split 曾各自重新完成 500-world 精确预检，并由当时的登记器核对：

| split | 最大对称 AUC | world bootstrap 95% 上界 | 身份33维秩 | 全零列 | 历史报告 SHA-256 |
|---|---:|---:|---:|---:|---|
| train | 0.509703 | 0.516557 | 31 | 0 | `6716cbe2aac9e220f8ae17a090ebe0aa0f9059ad457bea75105d02a198ace72d` |
| development | 0.508037 | 0.515723 | 29 | 0 | `a35373be8b01af52c93c2f726adf8e6a856b99a409f632ec81fe7d32a782b523` |
| audit_a | 0.509954 | 0.517763 | 30 | 0 | `8c7df397df286fd37afcbc3f3727422e55f1a6aac181a2b8b56d9cc5ae62ec6a` |
| audit_b | 0.505748 | 0.513693 | 32 | 0 | `9c856fa701d76f90c4021e1c0f3381a5112fb7899c22b59d1a69b7946c36f4bc` |

四份报告均低于预注册 0.52/0.53 门；train/development 各有 5 个
checkpoint，Audit A/B 各有 6 个，共 22 个。报告登记后
`--validate-config-only` 曾返回通过。但最终代码复审发现，该登记器没有
从 checkpoint 的 14 维输入和标签重新训练三个冻结审计模型，只重算了所
提供 OOF 的 AUC；bootstrap 也只核对固定位置，而不是逐元素重算全部
9,999 个统计量。因此内部自洽的伪造 OOF/bootstrap 仍可能通过，以上报告
现仅为历史记录，不能证明当前实现通过。

随后回归结果为：Step28-v13 专项 90 项通过、1 项旧
execution-blocked lock 按精确父字节漂移声明跳过、0 失败；完整仓库
359 项通过、7 项按既有声明跳过、0 失败，其中另 6 项来自已撤销的
Step28-v11 测试类。这些计数只属于上述历史实现。

当前实现已升级为 v3 科研合同
`22e136c5bea376aedc68f784b148d4ab67b81216c69ccf34acd836a3710ce601`：
登记器从保存的 14 维输入、标签和冻结 world folds 重新训练逻辑回归、
梯度树、RBF-SVM，三个 OOF 数组逐元素核对；再调用同一冻结函数重算全部
9,999 个 world bootstrap 统计并逐元素核对。split 写盘阶段新增发布前
Windows 重解析点拒绝。非哨兵 bootstrap 篡改和连同 AUC 一起伪造 OOF
的攻击测试均已加入。v2 四份报告保留但已从 overlay 撤销。

v3 现行四份精确预检随后完成：

| split | 最大对称 AUC | world bootstrap 95% 上界 | 身份33维秩 | 全零列 | checkpoint | 现行报告 SHA-256 |
|---|---:|---:|---:|---:|---:|---|
| train | 0.509703 | 0.516557 | 31 | 0 | 5 | `628bda9181b9063f2b9978f6d8b2e615f19c115cb0604a647f00976bed5aaec8` |
| development | 0.508037 | 0.515723 | 29 | 0 | 5 | `518dc19d26b82111fa12782863f6a58a07089e9a7a16d2ff3142d9c8e6a54563` |
| audit_a | 0.509954 | 0.517763 | 30 | 0 | 6 | `d9c2a03d32f7ed9316964bfbf9ac0c29bdb4b779b4816392a9699142256af2d6` |
| audit_b | 0.505748 | 0.513693 | 32 | 0 | 6 | `3dbcc4642ca0862e551df3ec5275e9bd3cc376ea6b2db183f0b23ebae23c8700` |

四份均通过 0.52/0.53 门，每份又由登记器独立重训并完整重放；登记到
overlay 后，标准 `--validate-config-only` 新进程用 437.6 秒顺序深验
四份及 22 个 checkpoint，返回 `PASS_CONFIG_VALIDATION`。Audit A/B
各绑定 2,000 个 query、54,000 条 directed relation 和 54,000 条 qrel。
第一次 v3 train v7 因运行命令漏传 checkpoint 前缀，报告明确为
`checkpointing_enabled=false`，只保留为无效诊断，未事后补造 checkpoint；
现行 train 为 v8。Step28-v13 专项回归为 91 项通过、1 项按既有声明跳过、
0 失败；完整仓库回归为 360 项通过、7 项按既有声明跳过、0 失败。三路
只读终审随后均同意进入 Git 基线冻结：文档/指标审核和发布代码审核均无
blocker/high/medium/low，科研审核无 blocker/high/medium，唯一 low 是在
冻结记录中写明实跑命令、计数和环境，现已记录到正式发布合同 v16。576 个
显式白名单文件已提交为 Git
`1a420b309ed269c84bb1c0a9874b3d884ce20469`；其中没有正式私钥、正式
数据、`AGENTS.md`、论文 docx 或 Step7/Step24 结果。从该提交字节重新执行
标准 `--validate-config-only`，590.5 秒后返回
`PASS_CONFIG_VALIDATION`，四 split 均为 500 worlds。overlay 随后以独立
Git `46d145d94026021e8184e9db4a341e85e9d3871b` 推进到
`READY_FOR_KEY_CEREMONY`，仍为 `generation_enabled=false`。一次性四分片
私钥仪式已在该提交上完成；公开回执 SHA-256 为
`b421d905e15644d70b92fee9eaea3b653053b25899f8f0ea7b33279523d970d9`，
四个承诺两两不同、与禁用承诺交集为 0，原始密钥没有返回且私钥目录 Git
跟踪数为 0。现将公开承诺和回执写回 overlay，并推进到
`FROZEN_READY_FOR_GENERATION / generation_enabled=true`。这仍只是本地
逻辑保管，不是 OS custody 或人员盲法；正式分片和最终发布清单继续为 0。

## 2026-07-31：C40 行序失效、v1.2 修复与正式发布

`v13_training_ready_v1_20260729` 生成后，发布后逐行审查发现 C40 最终文件
行序错误复用了选择/补齐 HMAC 排名。由于正负分层池大小不同，文件位置可
预测标签；原 v1 虽有发布清单，也不得用于 M1/M2。第一次行序修复 v1.1
生成了四个 split，但最终比较器把 train/development 的公开统计审计路径
套到 Audit A/B，忽略后者按合同使用 `sealed_supervision/`，因此在 Audit A
拒绝封版，且没有生成发布清单。修正后的只读比较证明 v1.1 数据内容确实仅
改变三个行序相关文件，但旧产物绑定旧封版工具哈希，禁止事后补签。

正式修复版本为
`v13_training_ready_v1_2_order_repair_20260731`，实现合同 SHA-256 是
`ee4d249aaf421d4e6a6603e9e0ae779d9b3c384cef266e5234b0b0918800a831`。
它复用原四把结构 key，保持原 world、文本、C40 成员集合、identity33 和
`pair_uid -> label` 映射，不换 key、不重抽、不删行。新一轮四个 500-world
精确预检及 checkpoint 全量重放均通过：

| split | 最大对称 AUC | 95% 上界 | 身份33维秩 | 报告 SHA-256 |
|---|---:|---:|---:|---|
| train | 0.509703 | 0.516557 | 31 | `aecac7e846a49b30053e06e057c7c66bd0bfc72ccc4ed0aaa2e2dd7a1b0551f6` |
| development | 0.508037 | 0.515723 | 29 | `9e51567bacd0dc0a4b471a3a8a4562e03cda73f508d98de3d07ee4e36d2fea53` |
| audit_a | 0.509954 | 0.517763 | 30 | `222835593b6f4f01076eb88556f2c41f29ffb49b7df1eada449bce9d6bf7f4b3` |
| audit_b | 0.505748 | 0.513693 | 32 | `ac810bf8999cec28da6ad0a3503dc9f0200800640b32cc9f60c35915b3faec43` |

四个正式 split 共 2,000 worlds、56,000 sellers、756,000 完整 pair 和
80,000 C40，磁盘约 4.824 GiB；Audit A/B 各有 2,000 query 和 54,000
relation/qrel。最终发布状态为
`PASS_DATASET_ONLY_READY_FOR_M0_M1_M2`。发布清单文件 SHA-256 为
`81ca7d9d2040d500b3bcb2ffc9af6aeb72c581754dbd075b94dd6cf8904b8275`，
父子等价报告文件 SHA-256 为
`3b0d1e29a713ce0402a14170d68936bd9a5b25f745321a5b988b1071817d2998`。
四个 split 都只有
`candidate_pairs.csv`、`candidate_sampling_audit.csv` 和
`world_generation_audit.jsonl` 三个文件字节改变，按主键语义一致，越界
变化为 0；全部跨 split UID、controller 和身份值交集为 0。

发布后逐行审计打开四个 split 共 202,071 条可见商品、56,000 份卖家档案
和 335,016 个计划身份槽，内部生成标记、原始身份 surface、规范身份值残留
及行序错误 world 均为 0。UID/文件位置组最大对称 AUC 为 train `0.508649`、
development `0.512257`；无关元数据组为 `0.505178/0.502243`。正式报告的
24 项 `visible_m0_proxy`（含 5 项 nuisance）在 train 的
gradient tree/logistic AUC 为 `0.570414/0.572021`，development 为
`0.572561/0.577379`；这是允许 M0 使用的共享文本/作者风格基线诊断，不是
真实迁移成绩。私有 AST/oracle 风格禁止模型挂载。逐行报告文件 SHA-256 为
`62bcc36cdfaadc5a257f7bfaaf0915b50b09b3e7902cc14efd349cf47c22390e`。

终审发现原逐行审计没有把整个正式文件树、500-world 精确集合和跨 world
连续块自身绑定成一个门。新增的独立发布树加固审计不导入生成器、预检器、
封版器或原逐行审计，哈希核对全部 160 个正式文件，并用标准库 HMAC
独立重放四 split 共 2,000 worlds、80,000 条 C40 的完整顺序；未解析
Audit 标签、qrel、controller、身份资产或 M1。状态为
`PASS_INDEPENDENT_FORMAL_RELEASE_TREE_HARDENING_AUDIT`，报告文件
SHA-256 为
`a053f71101b320868c8a52658bf78ae18bc3d67e84f71064f536a28d594fa3bd`，
自哈希为
`fbfe00737077f6422d24030e65840c447f9e1f314597ba3664171515f16580a7`。

v3 精确预检中的 `candidate_output_order_audit` 来自构建器返回收据，预检器
只核对固定字典，不是第三套独立 HMAC 重算；“预检通过”不得扩大解释为
“预检器独立验证”。正式 release 的独立顺序证据来自封版器磁盘重算和上述
独立发布树加固审计。冻结修复合同已被 release 哈希固定，不作事后改写，
本段用于收紧其解释边界。

完整结果与结论边界见
`docs/STEP28_V13_TRAINING_READY_ORDER_REPAIR_RESULT_AUDIT_20260731.zh.md`。
当前只证明数据字节可供冻结 M0、五个 M1 和 M2 使用；M0/M1/M2 尚未在该
正式数据上训练/评分，不能声称适配效果成功。Audit A/B 仍只是同一 Windows
用户下逻辑封存，人员盲法为 false。

父子等价证明完成后，已按显式路径清单删除失效 v1、未发布 v1.1 及绑定旧
实现的 order_repair_v1/v2 预检，共释放约 9.68 GiB。删除前把 v1 发布清单、
v1 四份 split manifest 和 v1.1 四份 split manifest 共 9 个文件原字节归档
并逐一验证 SHA-256。正式 v1.2、v3 预检、四份发布后审计、私钥保管目录、
release inputs、跟踪中的 development smoke 和全部历史文档均保留。清理
审计见 `docs/WORKSPACE_DEPRECATED_ARTIFACT_CLEANUP_20260731.md`。

由于失效 v1 完整数据已按合同在等价证明后删除，当前工作区可核验等价报告
和九份父 manifest 的哈希，但不能在不恢复 v1 全部原字节的前提下重新执行
父子语义比较；必须称为清理前完成并哈希封存的历史仪式。当前 v1.2 自身的
全部 160 文件、2,000 worlds 和 80,000 C40 已由上述独立加固审计重验。

提交前静态检查通过。首次全仓回归发现测试夹具把 5-world 设计期迷你构造
误留为 `generation_enabled=true`，生产门因此按预期拒绝；修复只把该测试
夹具显式标为非正式生成，没有修改正式数据、正式生成门或实现合同。针对性
18 项随后全部通过。最终 `python -m unittest discover -s tests` 共发现
381 项：374 项通过、7 项按既有声明跳过、0 失败，用时 798.904 秒。7 项
跳过由已撤销 Step28-v11 测试类的 6 项，以及已被 training-ready 测试替代
的 `dataset_smoke_v3` 不可执行旧锁 1 项组成。

## 2026-07-31：Step28 身份实验执行前锁与可见文本捷径控制

发布后攻击性复核确认，原 24 项 `visible_m0_proxy` 的 development AUC
约 `0.577` 低估了可见文本可学习性。现已把字符三元组 T_text 固定为只用
train 拟合、只在 development 报告的描述性探针；它不读取 UID、
`profile_text`、原始身份 surface 或 Audit A/B。正式结果为 train
`AUC=0.663437/AP=0.559696`，development
`AUC=0.627569/AP=0.346028`。一次性旧探针的
`0.628126/0.347326` 已原样披露，未据此调参。

该探针使用合成中文标签拟合，不能冒充冻结英文 M0，也不能把分数解释为
身份历史价值。生成器私有基础风格约 `AUC=0.91` 是模型不可见 oracle，不能
称作可见文本泄漏。当前处理不是重生成或抹掉作者风格，而是强制 M0、五个
M1 和 M2 在相同 `canonical_pair_uid` 上使用逐行完全相同的冻结 `p0`；
M1/M2 仅可在错配或正确对齐的 33 维身份矩阵上不同。科研结论只认
`M2-M0`、`M2-mean(M1)` 和最差 `M2-M1`，同时要求 M1 平均与 M0 的
90% TOST 区间完整落入 `[-0.01,+0.01]`。

新的执行前锁是
`schema/step28_v13_identity_transfer_experiment_policy.json`，状态为
`FROZEN_PREEXECUTION_CONTROLS_FORMAL_EXECUTION_BLOCKED`。它不改变 v1.2
任何正式数据字节或发布合同；正式 M0 评分、adapter 训练与 Audit 解封仍为
false。控制实现、剩余阻塞项和正式探针哈希见
`docs/STEP28_V13_IDENTITY_EXPERIMENT_PREEXECUTION_LOCK_20260731.zh.md`。
新增控制的 7 项针对性测试全部通过；最终全仓回归共 388 项，381 项通过、
7 项按既有声明跳过、0 失败，用时 819.420 秒。独立发布树加固审计重跑仍
得到原 self-hash，确认本次没有改写 v1.2 正式数据字节。

## 2026-08-01：模型训练就绪判定与直接中文训练基线

当前状态必须写成“数据就绪、正式训练未授权”。正式 v1.2 数据根目录、
160 文件发布树和 2,000-world/C40 顺序已通过发布与独立加固审计，可以作为
模型阶段冻结输入；数据集不需要重新生成。但现行身份实验锁仍为
`FROZEN_PREEXECUTION_CONTROLS_FORMAL_EXECUTION_BLOCKED`，其中 M0 正式
评分、adapter 正式训练、Audit A/B 解封均为 false。因此可以继续实现和
审核训练链，不能直接把一次训练称为正式 Step28 结果。

为正面回答“为什么不直接在合成中文标签上训练”，后继版本化策略必须在
Audit 解封前加入两个强基线：M3-base 复用相同公开预处理、legacy18 和冻结
LaBSE 表示，只用 synthetic train 标签训练目标域分类器；M3-joint 使用
同一 base 特征与正确对齐 identity33 联合训练单体分类器。两者只能在 train
内进行 world-grouped 选择，development 只冻结阈值和校准。若要声称模块
优于直接训练，必须预先冻结 M2 对 M3 的配对 world-bootstrap 比较；否则
只能并列报告数值。

现有 M0 是冻结表示加 LightGBM，不能把它描述成可端到端反向传播的 LaBSE
模型。真正微调 LaBSE 需要另立 M4-encoder，并先冻结可微 pair head、损失、
分块聚合和 GPU 搜索预算；当前未进入正式矩阵。正式训练前仍须完成
label-free 兼容夹具、CPU/GPU 策略、冻结 M0/C0 打分、M1/M2 求解器与完整
指标/bootstrap、M3 实现、Audit 授权链和提交后重验证。完整最新启动门见
`docs/STEP28_V13_MODEL_TRAINING_READINESS_20260801.zh.md`。

## 2026-08-02：full-378 v1.4 写盘失败，v1.5 合同已冻结

本节覆盖上面的 v1.3/v1.4 执行状态。v1.4 train 已完成 500/500 world 内存构造，但在写盘复核时失败：目标 JSONL 的绝对路径为 264 字符，原子写入使用长路径接口而直接 `Path.read_bytes()` 没有，因此文件实际存在却被误报缺失。只读复核又发现 split manifest 生产者写 v2、消费者仍要求 v1。train 未发布，development/Audit 未启动；v1.4 四份 seed、失败 stage 和 invalid marker 原样保留且禁止重试。

后继 `v13_training_ready_v1_5_20260802` 已修复长路径读/遍历/stat，并把 build manifest 和 execution lock 的版本分别收敛为单一权威常量。v1.4 失败 train 的 42,000 个身份值以哈希进入 `base_exclusions.v5`；与既有 283,496 个禁用哈希交集为 0，总计 325,496。v1.3/v1.4 共八个 master commitment 均进入禁用集合。

v1.5 两-world 预检、完整临时 public/private 写盘、manifest 消费、超 260 字符 Windows 路径和全部 Step28 后半段回归均通过。正式 Windows `Validate` 为 480 项：473 通过、7 声明跳过、0 失败。identity design self-hash 为 `d3515e7791f164b7a6a2ac55345c0306cec2fc82dcce2800a7649c0d58c67be5`；prelock self-hash 为 `04636dcf45bf295111d434aadf240425af451c268501073bee9eea094b21c589`。四份 seed 已按固定顺序一次性提交，原始密钥未公开；split、模型和结果仍全部为 0，下一步是 train/development 正式生成。

## 2026-08-02：v1.3 正式生成失败，v1.4 修复完成冻结前验证

本节覆盖上节“合同与随机性边界已冻结”的当前性。v1.3 四份一次性 seed
承诺已按序产生，但正式 train 在完成 500/500 world 的内存构造后被最终
聚合校验器以 `Mechanism assignment lineage collision` 拒绝。没有 split
发布；development 与 Audit A/B 未生成。私有失败标记与公开 seed 回执保留，
v1.3 按 one-shot/no-retry 合同永久 fail closed。

根因是 `mechanism_slot_uid` 表示每个 world 复用的模板槽名，旧校验器却要求
它跨完整 split 全局唯一。v1.4 只把该唯一键改为
`(world_uid, mechanism_slot_uid)`，并保留所有真实实体的全局唯一约束。新增
同 world 重复槽负测，以及两个真实 K28 world、756 pair 的端到端生成和
二次字节重放硬门；回执 self-hash 为
`ce77646a2b9314c55096434829e38f5f72a79bcbd1ff939b040585ee0f4622e0`。

新目标为 `v13_training_ready_v1_4_full378_scope_repair_20260802`。候选 identity
design self-hash 为 `6b629f139b34c464bc907cbe77f91488b4a39f82f1a5ae1f2a9bf7a83256f31f`，
draft prelock self-hash 为 `1ba6770eb09c65a4b150899fdd7e5885010fefae1027d8ec435ebf1978385944`。
冻结前全仓回归为 476 项：469 通过、7 声明跳过、0 失败。v1.4 正式
identity policy 与 prelock 已正式冻结；四份 seed 承诺已按固定顺序完成且原始
密钥未公开。v1.4 split、模型和效果目前仍为 0。

## 2026-08-01：训练前攻击复核撤销 v1.2/v2 启动资格

在任何正式 M0/M1/M2/M3 数值产生前，三路独立攻击复核发现三项足以阻断
训练的问题。第一，base policy 公开的 candidate key 可从可见 world/pair
UID 重算 C40 分层抽样；攻击在 train/development 的 AUC 约为
`0.89/0.86`。第二，即使候选 key 改为私有，40 条 C40 边形成的子图仍泄漏
类别；仅使用端点度数在 development 的 AUC 约为 `0.63`。第三，v1.2
Audit A/B 的 controller membership、机制、身份资产和候选审计已被 Git
跟踪，旧真值可以连接回任何复用 UID/文本/卖家历史的后继版本。目录名
`sealed_supervision` 不构成真实密封。

因此 v1.2 冻结字节和历史文档不改，但状态降为
`BLOCKED_PUBLIC_CANDIDATE_AND_AUDIT_CUSTODY_COMPROMISE`；v2 训练链从未执行，
现已禁止启动。后继版本固定为四 split 全量重生，并把每 world 的全部
`C(28,2)=378` 对作为主分类总体。每 split 为 500 worlds、189,000 pairs、
10,000 positives 和 179,000 negatives，随机 AP 基线为 `20/378`。完整图使
每 seller 度数恒为 27，从结构上删除 C40 选择与候选图捷径。C40 只能在所有
预测冻结后作为私有机制诊断，不能参与训练、阈值或主指标。

新 Audit A/B 必须使用全新 structure/UID/text/identity 随机域，从零生成且
相对 v1/v1.2 的 UID、身份值、卖家文档/profile/history 指纹不可连接；
labels、qrels、oracle 和逐行私有审计只进入 Git 忽略的 private custody。
五个 M1 改为完整 378-pair universe 内、不读标签的整 33 维端点不重合双射；
旧 C40 截距、L2、阈值、class weight、M1 分层及 20,000-row M3 配置全部失效。
正式修正案见
`docs/STEP28_V13_FULL378_FRESH_RELEASE_PREEXECUTION_AMENDMENT_20260801.zh.md`。
完成新数据、逐行捷径审计和 full-378 执行闭包前，当前状态始终为 NO-GO。

## 2026-08-01：Step28-v13 v2 执行实现完成，可以开始计算

上节“正式训练未授权”是 v1 锁当时的准确历史状态；不得删除或改写。其
后继 v2 已把全部登记 blocker 实际实现并另立新锁：
`schema/step28_v13_identity_transfer_experiment_policy_v2.json`，状态为
`FROZEN_PREEXECUTION_IMPLEMENTATION_COMPLETE`，canonical self-hash 为
`dc5be1258f5379864fb55eeb6493ce3f495760db05a245650fa0eb5c9dbd0c92`。
v1 文件仍保持 blocked 原字节，v2 只继承并关闭 blocker，不修改正式 v1.2
数据或历史结论。

已冻结并验证的执行链包括：

1. 英文 M0/C0 joblib、真实英文盲回放参考和 756,000 完整中文 pair 的
   label-free 公共投影；
2. 同时适配四个冻结 tokenizer 的完整文本分块，以及只接收匿名文本、匿名
   pair 和 LaBSE 的 Linux CUDA 工作区；
3. Step7 历史 LaBSE 兼容夹具。分块、重复 embedding 和历史六项聚合的
   12 位小数必须全部相等，任何一项不等立即失败；返回 bundle 同时绑定
   当前 GPU 策略和编码脚本哈希，禁止混入旧版本结果；
4. 冻结 M0/C0 前向评分；两个 base ×（五个 M1＋一个 M2）的 12 个
   train-only 33 维加法适配器；M1 只在 train 使用错配历史，推断时与 M2
   读取同一份正确目标 identity33，防止测试阶段人为压低 M1；
5. 四个 train-only 直接目标域 M3 LightGBM 对照，每个在 36 个候选中做
   5 折 world-grouped OOF 选择；M3 只持久化 JSON 兼容元数据和 LightGBM
   原生模型字符串，写盘前验证其预测与训练封装器 float64 字节完全相同，
   审计环境不反序列化 sklearn `1.7.2` estimator；
6. development-only 阈值（不拟合概率校准变换），AP/AUC/PR-AUC/F1/Recall/Precision 等分类指标、
   MRR/MAP@10/Recall@K/Hits@K 检索指标、hard-negative FPR 和 9,999 次
   配对 world bootstrap；
7. 在 Audit A 标签打开前共同冻结 A/B 盲预测；A 不通过则 B 保持封存，
   只有 A 通过才生成一次性 B 授权。

为补足原 release 中未公开的 hard-negative 诊断输入，已在不开分类标签、
qrel、controller 或机制标签的条件下从冻结观测数据重放，并把 A/B 各
20,000 行投影保存在 Git 忽略的 private custody。公开 v2 承诺 self-hash 为
`fede8eab3411e8cf61c03da29669efaa2cff32c2dd4630c05321dd66ac510675`；A/B
CSV SHA-256 分别为
`27a6f6127c4659c5f89d172a193fce7839aebcf249a08d604dbdee2fa0204530` 和
`f1df37a96c45353c76e59b1e6da3a906ea792905e0d2492648c976d546557538`。

执行前总验证器
`scripts/step28_v13_validate_identity_experiment_v2.py` 已逐文件核对正式
release、公共投影、夹具、英文模型依赖、诊断承诺和 25 项双向源码/传输
闭包，返回 `PASS_STEP28_V13_IDENTITY_V2_PREEXECUTION_CONTRACT`。其中
`.gitattributes` 也进入闭包，唯一 PowerShell runner 固定 LF，避免提交后
CRLF 转换造成跨 Windows/Linux 假漂移。

最终冻结实现上，40 项 Step28-v13 专项合同测试全部通过；全仓回归共 421
项，414 项通过、7 项按既有合同跳过、0 失败，用时 1103.765 秒。另用训练
环境生成仅含原生 LightGBM 字符串的 M3 夹具，再在 sklearn `1.7.1` 审计
环境以 warning-as-error 方式加载和预测，跨版本持久化检查通过。

当前科研状态必须准确表述为：**可以开始正式计算，但尚未计算。** Linux
LaBSE bundle、冻结中文 M0/C0 分数、M1/M2/M3 模型和 Audit 结果均尚未
产生。当前同一 Windows 用户逻辑封存仍令
`independent_blind_confirmatory_claim_authorized=false`；即使合成 Audit
通过，也不能替代新收集的真实中文 ground truth 或宣称真实市场外部有效性。
执行命令、环境和同步范围见
`docs/STEP28_V13_MODEL_TRAINING_READINESS_20260801.zh.md`。

## 2026-08-01：full-378 v1.3 实现终审完成，正式仪式尚未开始

本节是当前最新事实，优先于上节旧 v2“可以开始计算”的历史表述。旧
`schema/step28_v13_identity_transfer_experiment_policy_v2.json` 已因 v1.2
数据资格撤销和运行器源码漂移而 fail closed；旧私有诊断投影与过期 v2
执行树已清理，不得恢复为当前输入。v1.2 冻结字节和历史文档继续保留，
但没有 M0/M1/M2/M3 正式训练资格。

fresh 后继版本固定为
`v13_training_ready_v1_3_full378_fresh_20260801`。数据生成、完整 378-pair
主分类、全 28-query 检索、五份 train-only M1、M2、M3-base/M3-joint、
development M1 等价门、Audit A/B 顺序解封及全部指标/bootstrap 执行链
已经实现。Audit 数据生成另有一次性授权：必须先完成 train/development、
五份 M1、M1 独立性和公开捷径门，且 B 文本只能在 A 文本完成后生成。

终审新增三层深度重放回执。development 必须从冻结模型重算逐行预测、阈值、
指标和 9,999 次等价 bootstrap；Audit A/B 盲预测必须在真值打开前重算全部
分类与检索概率；每个 Audit 评估必须重算分类、检索、私有诊断和所有
bootstrap 数组。只验证文件哈希而不重算已不满足合同。Audit B 授权还必须
绑定 Audit A 的深度重放回执。

正式 identity design self-hash 为
`0b2ae09ed17cd68e313f4add60960f8e9fa192c0bd56ccb1a7702db7687ba5c1`，
75 项模型源码闭包已经冻结；正式 prelock self-hash 为
`23faeeda89cae653af8f1a2d363f436341bb826d9502bf676aa73a051130ccac`，
50 项数据源码闭包通过 config-only。正式 Windows `Validate` 入口实际运行
472 项，结果为 465 项通过、7 项既有声明跳过、0 项失败，用时 792.518 秒。最初 7 项失败/错误全部是
清理后仍要求旧结果、旧私有诊断或旧 v2 runner 的过期测试，现已改为验证
旧链 fail closed，没有恢复任何废弃产物。

最重要的当前边界：正式 identity policy 与 preceremony lock 已落盘；四份
master seed 承诺已按 train、development、Audit A、Audit B 固定顺序完成且
禁止重抽，原始密钥未公开。四个 v1.3 split、M0 分数、模型和效果报告仍为
0。当前只能称为“正式合同与随机性边界已冻结”，状态仍为 **NO-GO**。下一步
严格执行 train/development 生成与审计、Audit 文本单独授权、Audit A/B 生成、
M0 Linux 编码、Windows 训练和分阶段 Audit。最新启动说明见
`docs/STEP28_V13_MODEL_TRAINING_READINESS_20260801.zh.md`。

## 2026-08-03：full-378 v1.3–v1.11 全部关闭并清理

本节是当前最新事实，覆盖上文仍把 v1.5 或其他后继写成待执行版本的历史
状态。v1.3 至 v1.11 均已永久失败，当前不存在可用于 M0/M1/M2/M3 的 fresh
full-378 正式数据集，也没有任何正式模型结果。v1.11 虽生成四个 500-world
split，仍在发布前跨版本精确审计失败：审计器把 world 内模板槽
`mechanism_slot_uid` 错当成 split 全局唯一键。每个 split 的正确复合键
`(world_uid, mechanism_slot_uid)` 均为 6,000/6,000 唯一，world 内重复为 0；
这证明根因是审计合同作用域错误，但 one-shot 纪律仍禁止原地修补或发布。

删除前已把历代及 v1.11 实际生成的身份值压缩成 915,996 个不可逆禁用哈希；
文件 SHA-256 为
`f70611a4b5df7ddbded6784820026352c92952a0245fcb184b4e7c282c1447a0`，
self-hash 为
`6f60e294bdbcae1d1da3802acdf4095d605c8e633755fe689d4a71b67fece4d3`。
其中 v1.11 实际新增 170,500 个，不是先前估算的 168,000：Audit B 为
44,500，另外三份各 42,000。归档不含原始身份值或私钥。

失败 private custody、stage、失败版本专用代码和配置已按
`docs/STEP28_V13_FAILED_RUN_CLEANUP_20260803.zh.md` 的边界清理。成功 v1.2
发布树继续作为历史证据保留，但训练资格仍被撤销。后续不得通过恢复失败
payload 重建排除库；新版本只能读取并校验压缩哈希归档。当前主线在清理后
自主暂停，下一次 fresh 设计必须另立版本、四份新 seed 和新的冻结闭包。
实际删除约 20.11 GiB private custody，并按哈希清单删除 223 个失败版本专用
脚本、配置和测试；清单 SHA-256 为
`c6fbe552a67e533165e43d991ce5f2b2bee312ebf9455139ed61928159dccf19`。
清理重放通过；全仓回归为 381 项、374 通过、7 声明跳过、0 失败，用时
848.857 秒。

## 2026-08-03：v1.12 干净重启仅通过预仪式设计基线

本节是当前最新事实。v1.12 已从压缩失败历史重新起步，不恢复 v1.3–v1.11
已删除 payload，也不沿用其 seed。策略
`schema/step28_v13_v1_12_cleanroom_preceremony_policy.json` 的正式 seed、
正式数据、模型训练和 Audit 解封授权仍全部为 false；实现本身也明确不包含
正式 seed ceremony 或四 split 正式生成器。

非正式两世界预检共重放 756 条完整 pair、40 正/716 负、168 个身份资产、
756 行 identity33 和 10 张 M1 映射。915,996 个失败身份哈希、90 个禁用
master commitment、逐资产碰撞推进、Windows 长路径、world-scoped 机制槽、
最终 self-hash、登记后缀投影、join UID 分域和单一成员合同均通过。回执
SHA-256 为
`d21964a248e1138e65a654262e026c8c1457f8500e4915dbf5b83cdaba09d243`，
状态为 `PASS_DESIGN_ONLY_NO_FORMAL_AUTHORIZATION`。

全仓回归现为 396 项：389 通过、7 项既有声明跳过、0 失败，用时 809.034 秒。
当前正式数据行、正式密钥访问、科学指标和模型训练均为 0。下一步必须实现并
验证正式 custody/seed 流程、四 split 生成与发布审计，以及基于完整输入的真实
优化器收敛预检，再另行冻结正式授权；在此之前不得开始正式中文合成数据生成。
完整审计见
`docs/STEP28_V13_V1_12_PRECEREMONY_BASELINE_RESULT_20260803.zh.md`。

## 2026-08-09：v1.12 可见文本捷径设计预检通过，正式授权仍关闭

本节是当前最新事实，覆盖上一节把完整数值与文本捷径预检写成待完成事项的状态。最终冻结源码闭包上的设计预检已使用 500 个训练世界和 500 个开发世界完整重算。第一次运行在两个登记门返回通过后被外部会话中断，没有形成最终回执或持久化世界/矩阵；中断回执固定边界后，唯一一次精确替代运行以相同运行编号、源码、策略、合同、设计主密钥、阈值、参数和环境从第零阶段完成，替代资格已经消耗。

规范最终回执为
`reports/step28_synthetic_chinese_dataset/design_preflights/v1_12_cleanroom_20260803/text_shortcut_preflight_receipt.json`，文件 SHA-256 为
`e371a460d65560f06e071a6961bf67c22d1f39a5e5e3a37431af5b1c88dd152e`，规范自哈希为
`ffe86c9ad07f77609c3463e831108e73504493b591c73815359c0419e58c4a20`，来源闭包哈希为
`75c3e940a873a2674300337d0d3f1042fa41626e7e0aa2e97f640744175da2a3`。六个阶段全部通过。反事实机制中性文本的开发集最大单特征对称曲线下面积为 `0.508517`，六模型最大值为 `0.504527`，9,999 次世界重抽样上界为 `0.509560`；平均精确率提升上界为 `0.001052`。原始描述字符三元组在训练和开发约为 `0.5480/0.5441`，只能解释为当前合成设计中的弱可见文本及风格相关信号；控制者盲错排后冻结攻击信号降至近随机，不得外推到真实市场或宣称完全因果归因。

训练和开发的八类跨拆分交集均为 0，可见禁止残留为 0，1000 个世界的生产解析、身份删除、卖家画像和逐世界统计已在内存中重算，原始世界及矩阵未持久化。针对性回归为 39 项通过、0 失败；全仓 464 项中 456 项通过、7 项声明跳过、1 项为已由精确豁免回执绑定的 Step28-v12.1 guarded sync manifest 历史测试，不属于当前实现回归。网页端 GPT-5.6 Sol xhigh 的结果级复核为阻断级、高级、中级和低级问题均 0，并给出 `RESULT RECEIPT GO`。

当前边界仍是：正式密钥访问 0、正式数据行 0、正式审核真值解封 0、正式模型训练授权 false。该结果只允许进入“冻结下一阶段正式授权闭包”的讨论，不能直接开始 seed ceremony、正式四拆分生成或 M0/M1/M2/M3 训练。完整结果见
`docs/STEP28_V13_V1_12_TEXT_SHORTCUT_PREFLIGHT_RESULT_20260809.zh.md`。

## 2026-08-09：v1.12 正式授权实现终审通过，尚未生成预锁或密钥

本节是当前最新事实。正式授权叠加层已把最终文本捷径回执、外部中断回执和历史清单精确豁免纳入强制证据，并保留旧设计证据只作兼容先决条件。完整测试回执必须保存机器可重算的原始测试集合；唯一允许的原始失败仍只有已固定完整名称的 Step28-v12.1 历史清单测试，任何第二失败、错误、预期失败、意外成功或失败/跳过子测试都关闭授权。

正式全库测试、预锁构建和首次密钥启动前均要求全部已跟踪文件的工作树、暂存区和提交分别一致，测试目录全部可发现 Python 文件必须被跟踪且闭包不变。正式来源闭包成员还必须逐一证明受 Git 跟踪。启动回执无替换落盘后、创建私有暂存目录和读取随机源前，会再次完整验证预锁及持续状态；此时失败将以零次主密钥抽取永久关闭该运行。

密钥仪式已增加覆盖整个状态机的进程级非阻塞互斥：Windows 使用整机跨登录会话的 `Global` 内核命名互斥量，类 Unix 系统使用现有预锁文件的系统文件锁；两者均由操作系统随进程退出释放，不生成科研锁文件。真实双线程和双进程竞争测试均证明第二调用在进入正式加载、回执、私有暂存和随机源之前零副作用退出。

本地授权叠加层、正式构建和执行合同共 46 项全部通过；全部 Step28-v13 v1.12 测试共 100 项全部通过，失败、错误、跳过、预期失败、意外成功以及失败/跳过子测试均为 0。网页端 GPT-5.6 Sol 极高思考复核最终分级为阻断级、高级、中级和低级问题均 0，并给出 `PRELOCK IMPLEMENTATION GLOBAL MUTEX GO`。

当前仍未写入正式全库测试回执、正式预锁、正式密钥、正式数据或模型结果。下一顺序固定为：提交精确源码闭包，运行提交后全仓测试并生成正式测试回执，构建只授权一次密钥仪式的预锁，再做运行资格复核。正式测试回执生成后到首次密钥启动之间不得修改或提交任何已跟踪文件，否则回执按设计立即失效。

## 2026-08-09：首次正式全库测试记录被计数模型拒绝，零正式副作用

提交 `1256f10ed695eb1aa7aa7ae74f9a33d78a9848ae` 后首次执行正式全库测试记录入口。底层实际发现并启动 481 项测试，失败完整名称仍恰好只有固定的 Step28-v12.1 历史清单测试，错误、预期失败、意外成功和失败/跳过子测试均为 0；另有六个已撤销 Step28-v11 普通跳过和一个旧 metadata-shortcut 类级跳过。记录器在写回执前关闭，正式测试回执、预锁、start receipt、私有暂存、主密钥抽取、正式数据行和模型结果均为 0，故没有失败 payload 可复用或保留。

根因不是科研测试新增失败，而是原结构化计数错误地要求所有跳过事件都属于 `testsRun`。Python `unittest` 对 `setUpClass` 声明跳过会登记一条跳过事件，但不会启动其中测试，也不会增加 `testsRun`。修复将六个已启动测试的普通跳过与一个未启动的类级跳过分别记录，并逐条固定完整名称和原因；测试总数只与成功、普通跳过和唯一历史失败构成严格分区。任何跳过增减、原因变化或两类跳过重分类均继续关闭授权。补充合同的最终字节钉也已同步并由真实文件回归验证。修复后的五个 v1.12 专项模块为 103 项全部通过；网页端 GPT-5.6 Sol 极高思考复核为阻断级、高级、中级和低级问题均 0，并给出 `FULL-SUITE FIX FINAL REVIEW GO`。正式全库测试尚未重跑，正式边界仍为零回执、零预锁、零密钥、零正式数据和零训练。

提交 `acdf1d7ca2d6eee76319182b57f91d93b1fe91bf` 后的第二次正式记录运行在 950.2 秒后再次于写回执前关闭。底层启动 484 项测试，除固定历史失败外，新增失败恰为新加的类级跳过回归本身；六个普通跳过、一个真实类级跳过及所有其他异常集合仍符合预期。根因是回归测试把内部夹具类的模块前缀写死为按包运行时的 `tests.test_...`，而全库发现会以 `test_...` 导入同一文件。修复只把该测试的期望名称改为由运行时 `__module__` 和 `__qualname__` 精确组成，不放松生产回执中固定的真实类级跳过名称或原因。按包和全库发现两种导入方式分别通过 1/1 和 20/20，五个 v1.12 专项模块为 103/103；网页端复核四级问题均为 0，并给出 `DISCOVERY IMPORT FIX REVIEW GO`。该失败同样没有生成正式测试回执、预锁、私有暂存、密钥、正式数据或模型结果，也没有保留失败 payload。

## 2026-08-09：正式全库测试通过，但旧设计回执不能授权预锁

提交 `1ed2b039e6abfd0609f7edf7d0fdef9675a5ce55` 上的第三次正式全库测试记录已成功完成，用时 949.2 秒。底层启动 484 项测试：477 项原始通过、6 项已启动普通跳过、1 项未启动类级跳过、1 项固定历史豁免失败、0 项错误；其余异常集合均为 0。成功回执文件 SHA-256 为 `9440fc2711efb9c5fef7347fa12fbff93067c5c2862740e951bfbe9c0d6f8dcd`，规范自哈希为 `c04c063e4e5b09b08edbba2fbdf0610f04e3d49a5cc15cbb323ac18dec7425bc`。该回执没有访问正式密钥，也没有产生正式数据行。

随后预锁构建在写出预锁之前关闭。根因是预锁仍引用 2026-08-03 的两份旧兼容层设计回执，而两份旧文件都缺少 `producer_path`、`producer_sha256`、`formal_common_sha256`、`formal_build_draft_sha256` 和 `runtime_versions` 五项来源证明；当前生产入口已经会真实写出这些字段。给旧回执事后补字段会伪造其生产来源，因此禁止采用。失败没有生成预锁、启动回执、私有暂存、主密钥、正式数据或模型结果。

任何已跟踪修改都会使上述成功全库测试回执过期；其哈希与计数在本节登记后，过期文件按清理纪律删除，不得继续授权。修复采用两次提交：先提交只指向新日期化路径的预锁代码、真实文件集成测试和本文档；再在该提交上真实重跑两世界写盘重放与 500 加 500 世界精确数值捷径预检，并只提交两份新回执。旧回执只保留为历史迁移审计，不能进入当前授权来源闭包。该修复不重跑已经耗尽替代资格的可见文本捷径预检，也不生成密钥或正式数据。网页端 GPT-5.6 Sol 极高思考复核确认阻断级和高级问题均为 0，并给出 `DESIGN EVIDENCE RERUN PLAN GO`。

## 2026-08-09：最终源码后的两份兼容层设计证据已真实重跑

源码提交甲为 `2cbc69a6b4f79af66309735549b5e40a12aa18ad`。在该提交上，冻结入口真实重跑了两世界写盘重放和训练、开发各 500 世界的精确数值捷径预检；没有重跑已经关闭替代资格的可见文本捷径预检。两份新回执随后作为仅证据提交乙 `1dc41ce1c1470b0a383e61603f1fc328e5d4667f` 的唯一内容进入版本控制。

新两世界回执为 `two_world_persisted_stage_receipt.postclosure_20260809.json`，文件 SHA-256 为 `addd6d15fb3d50e1f3965315886fd36f55130b9bc7a3d9b666cbbf6da3cc8f85`，规范自哈希为 `c9fe8a2244573ac44f61646356ef4a01ebf4acd90809abf06e573af6fe060c55`。它通过 2 个设计世界、756 对和 5 份 M1 结构错配回执，且正式授权、正式密钥访问和正式数据行仍为假、假、0。第二次新路径诊断重放得到逐字节相同的文件 SHA-256，诊断文件随即删除。

新精确数值回执为 `exact_shortcut_preflight_receipt.postclosure_20260809.json`，文件 SHA-256 为 `b18ed0d1bb45a3f4976f2483590f2d440bfe10f24c13f66560b1009e7647e84f`，规范自哈希为 `b7f21dfc9c8bd546ea01d876f7ecc3f9184c39541f33bef04f65f00929350954`。训练和开发各重算 500 世界、189,000 对、24 项特征和 9,999 次世界重抽样；六个门全部通过。最大单特征对称曲线下面积为 `0.504956`，组合值为 `0.506145`，组合平均精确率提升为 `0.000956`，重抽样上界分别为 `0.507239` 和 `0.000676`。这些仍是设计期兼容层检查，不是正式科学指标或模型结果。

旧新迁移比较中，精确数值回执除新增来源字段、耗时和自哈希外的确定性内容差异数为 0。两世界回执只有训练私有清单、训练公开清单和开发公开清单三个历史总哈希变化；其余状态、数量和阶段语义完全一致，开发私有清单哈希也一致。旧临时阶段树已按既定纪律删除，不能也不应伪造恢复；当前生成器的第二次逐字节一致重放排除了现实现非确定性。网页端逐字节复核四级问题均为 0，并给出 `POSTCLOSURE EVIDENCE GO`。

提交乙的正式来源闭包共有 50 个成员，规范总哈希为 `b60e88886b696c8db5a0fc719a8c4379a1498aca55d4e3d2aff8cd8a5d566a95`；本交接补充和当前进度文档均不属于该闭包。本节与进度文档作为只改非闭包文档的提交丙写入后，必须证明闭包逐成员和总哈希与提交乙完全相同，再在提交丙的干净工作树上运行新的正式全库测试。当前仍无正式预锁、密钥、正式数据、模型或审核结果。

## 2026-08-09：v1.12 正式预锁与一次性种子仪式通过

提交丙为 `5cdc57a86577b9090d6148c128222d15db2a6b83`。该提交没有改变 50 个正式来源闭包成员，规范总哈希仍为 `b60e88886b696c8db5a0fc719a8c4379a1498aca55d4e3d2aff8cd8a5d566a95`。随后正式全库测试用时 942 秒，启动 488 项：481 项原始成功、6 项已启动普通跳过、1 项固定历史豁免失败，另有 1 项不计入启动数的夹具级跳过；错误、预期失败、意外成功和失败或跳过子测试均为 0。五个 v1.12 专项模块 107 项全部成功。测试回执文件 SHA-256 为 `4d593a5f7e6f76c2945ccdbc8748901c84a9eebc7972f5303686d503b73ffd5b`，规范自哈希为 `2fef395a1cb0c65273d6a563ed5afc665fb246ce8b8756034532e5a268e79757`。

正式预锁状态为 `READY_FOR_SEED_CEREMONY_ONLY`，文件 SHA-256 为 `eaf81c82c54129f211c1be30887983cf7b18c27566fffdb98bc2d51ac971e32f`，规范自哈希为 `58634f784f72a949acd1c53887056bda92ea5dcf42602f57323e0c6d193083c3`。它只开放一次种子仪式；训练、开发、审核甲、审核乙生成、模型训练和审核真值解封均保持关闭。复合本地验证和网页端 GPT-5.6 Sol 极高思考运行资格复核均通过，网页结论为 `SEED-ONLY PRELOCK RUN-READINESS GO`，实际启动前再次实时复核为四级问题均 0，并给出 `LIVE SEED CEREMONY START GO`。

唯一一次正式种子仪式随后成功返回 `PASS_NEW_ONE_SHOT_SEED_CEREMONY`，按训练、开发、审核甲、审核乙固定顺序恰好读取操作系统随机源 4 次，每次 32 字节，没有返回或公开原始主种子或派生能力。启动回执文件 SHA-256 为 `0b76d74c401badaa9b4b033b3300e01ba09acb985a9d38c6121c853de908ab1b`，规范自哈希为 `f55bccc8a1079b68eab51eee9e4f9a43f9e8a4546a24deeabe7fb00b922c6c41`；公开仪式回执文件 SHA-256 为 `d14d949104b865e672e318862f97d717b486a7eb99c32a4a577faf9b52682255`，规范自哈希为 `66a6c1aa487c83910e356f6a82868de0c3b6845531da8ec7c3135b6899dcbf21`；训练、开发执行锁文件 SHA-256 为 `6e0a203b3a87b66503b69ebaa07ac393a4f3344c89de6d5b81020f1bc190860a`，规范自哈希为 `f312ecdd8dfd5be63b0ef3bc4a875eba2e45cb6e0439f7d4c965d4a82d875f03`。

公开回执中的 4 个主种子承诺、28 个生成能力承诺和 5 个训练错配能力承诺全局 37 个值两两不同，且没有原始密钥字段。私有 seed bundle 位于 Git 忽略的 `private_custody/`；私有暂存目录和永久失败回执均不存在。执行锁只开放未来训练集和开发集生成，同时关闭再次运行种子仪式、审核甲、审核乙、模型训练和审核真值解封。网页端对三个公开回执逐字节复核后给出四级问题均 0 和 `FORMAL SEED CEREMONY RESULT GO`。

当前可成立的边界只有“v1.12 正式种子仪式成功，训练集和开发集具备正式生成资格”。正式数据行、质量回执、审核锁、模型和科研效果仍全部为 0。下一步必须停止使用种子入口，改用不可覆盖的执行锁，依次完成训练集和开发集的 core、五份 M1、finalize、quality 和 publish；不得提前生成审核拆分或训练模型。

## 2026-08-09：v1.12 训练/开发联合质量失败，永久终止

本节是当前最新事实，覆盖上一节仍把训练、开发生成写成待执行事项的状态。v1.12 已正式生成并封存训练集和开发集，各 500 个世界、189,000 对；训练集五份 M1 结构回执全部通过。联合质量审计随后在任何质量回执、质量标记、发布或模型训练之前，以 `Fresh/historical exact-intersection gate failed` 关闭。

只读定位证明训练与开发九类集合互相隔离，且本轮身份哈希与既有 915,996 个失败身份哈希交集为 0；唯一历史冲突是训练 22 个、开发 13 个完整 `{title, description}` 文档哈希命中成功 v1.2，合计 35。v1.12 内部另有训练 7 次、开发 3 次完整商品文本重复。生成器缺少完整可见文档的生成时历史及同次运行碰撞防线，是本轮根因。

v1.12 正式暂存共 378,000 对，但没有发布或训练资格；禁止解释性豁免、原地修补、同种子重跑或复用能力。终止失败回执、25 项终止血缘清单和哈希排除归档的文件哈希依次为 `03aa550ec695cd21c98771edf1fb91fd105e869c318be6cff3b468900c8ca31f`、`bfaa540c3634c54c92ced2f1a01d3281ffc9bf4fb8d95f641c22e7df062b4f96` 和 `b29e2e7dc46921aa3f9d1f7e03d9e75baabde5093dd3fb63b93fd3bc9c19eb01`。归档加入 84,000 个失败身份哈希，使累计禁用数达到 999,996，并保存商品/卖家文档、六类编号及 37 个已消费承诺的不可逆哈希或承诺；不保存原始私密内容。

后继版本固定为 v1.13。它必须把商品文档和卖家冻结五字段文档同时纳入历史、失败运行、同次运行和跨拆分碰撞防线，且使用统一无标签规则，禁止靠编号或重试标记制造唯一文本。该规则属于事后工程修复，未来通过不能追认 v1.12。完整边界见 `docs/STEP28_V13_V1_12_TERMINAL_FAILURE_AND_CLEANUP_20260809.zh.md`。

终止证据已在提交 `48bca5c6208130a5a266bb55a9d9898b1e4ba6ab` 中固定。提交后再次验证三份终止 JSON 的文件哈希、规范自哈希和 Git 大文件对象均可解析，随后删除失败私有暂存、已消费种子保管区、浏览器缓存和空运行目录，共 111 个文件、1,887,149,059 字节。清理回执文件哈希为 `06fe76bf1fe93f913439b6aebd68f469912e832a4c597814010f7ccbfff253e2`，规范自哈希为 `1eff3fe8db684594e4e93eb4ef3be8157ec1ccf0900c965c72457b5d4643eab8`。v1.12 到此停止，任何后续正式执行都必须使用全新的 v1.13 合同、源码、测试和种子。

## 2026-08-09：v1.13 可见文档碰撞合同最终通过，尚未实现

本节是当前最新事实。v1.13 已完成可见商品文档和卖家五字段文档碰撞防线的设计冻结；合同为 `docs/STEP28_V13_V1_13_VISIBLE_DOCUMENT_COLLISION_CONTRACT_20260809.zh.md`，大小 17,811 字节，文件 SHA-256 为 `0f1906a8be46be3d3cdf16738814e0c83192e97299d162857b71ca585208eca6`。网页端 GPT-5.6 Sol 极高推理经过首次 2 个阻断级、3 个高级和 2 个中级问题的拒绝、V2 闭环及最终三个中级实现细节闭环后，最终分级为四级问题均 0，并给出 `V1.13 DOCUMENT COLLISION CONTRACT FINAL GO`。

冻结修复不把候选键替换原基础文本能力。每个世界先建立候选无关结构与身份父状态，身份只在实时已提交集合的副本上分配一次；32 个自然表述候选全部复用该身份状态。候选编号只允许因完整商品文档或卖家文档的精确碰撞递增；解析、脱敏、资料贡献血缘、身份、标签或结构闭包失败均立即关闭整个版本。精确标题克隆的目标商品必须同时具有非空标题和描述；固定风格资料七个标题缺失分位点全为数值零的机器证明文件 SHA-256 为 `ee3e84ac0ed111027c41ff1760db29935f6473b1b244445b51085fd7a4daf326`，规范自哈希为 `be951a3e6ee1c4cca58f26ca4b1a5658c0105adabcf66706c492a21f5f2bdaa8`。

持久化改为逐世界不可覆盖提交标记和确定性最终封存：大体积世界包全部同步到磁盘后最后写入 `WORLD_ACCEPTED.json`；拆分状态只能从连续有效标记重建；最终表恢复只能验证已有文件或补写缺失文件，禁止删除覆盖，最后写 `SPLIT_SEAL_COMPLETE.json`。开发和审核拆分必须精确钉住前序封存。设计预检还要把卖家参与碰撞的次数变成配对级攻击特征，继续使用既有冻结攻击模型和阈值检查后选择捷径。

当前授权边界仍是：v1.13 源码、策略、事务、注册表和测试尚未实现；正式种子、派生能力、正式数据行、质量回执、模型和指标全部为 0。下一步只能按合同从纯文档哈希、历史注册表和候选键小模块开始实现并逐阶段复审，不能直接开启种子仪式或生成数据。

## 2026-08-09：v1.13 第一阶段纯碰撞原语实现通过

本节是当前最新事实。v1.13 已完成合同第七节第一项的纯实现，不含候选循环、生成器接线、事务、正式能力或正式数据。机器策略为
`schema/step28_v13_v1_13_document_collision_policy.json`，文件大小
12,084 字节，SHA-256 为
`2331ccbcf4c08171204f86fbe44a430780d11967b944e72996b707de2c9cca99`，规范自哈希为
`d993ad7252789eee341f8875b6a11cd757234c9e07da1ec71fe620bec3a740a9`；六项正式授权全部为假。纯实现脚本为
`scripts/step28_v13_v1_13_document_collision.py`，文件大小 34,480 字节，SHA-256 为
`5cb1907ed6d15ea52cdc0b35069fdff1cc651dca2b570debbb53ee77a8f9d4d4`。

商品文档字节直接机械实现冻结的 `json.dumps` 参数，不做任何额外空白或 Unicode 规范化；卖家文档严格按五字段顺序执行逐字段去首尾空白、丢弃空值和换行连接。行级基数门同时检查行数、哈希行数、行哈希唯一数、注册表行数、注册表唯一数和成员完全相等，注册表重复不能再被集合转换静默吞掉。候选键已用冻结 HMAC 字节向量锁定，并且本模块没有原 `text_key_hex`、生成器或写数据入口。

只读历史重放从成功 v1.2 四份公开商品和卖家资料逐行重算出 202,043 个商品文档哈希和 56,000 个卖家文档哈希，再与失败 v1.12 的 100,936/28,000 个哈希合并并精确核对 35/0 个历史交集，最终禁用集合为商品 302,944、卖家 84,000。累计 999,996 个身份值哈希、六类编号哈希、编号哈希方法、4/28/5 三类共 37 个已消费承诺也全部闭合。历史加载不使用缓存，同一进程每次调用都重新读盘验证。

网页端 GPT-5.6 Sol 极高推理首次复审发现 2 个高级、3 个中级和 1 个低级问题；直接序列化、注册表多重性、唯一策略来源、编号方法、分组承诺、缓存和异常类型均已修正。修正版复审为阻断级 0、高级 0、中级 0，仅剩 1 项低级回归覆盖建议并给出 `V1.13 COLLISION PRIMITIVES PATCH GO`；该建议随后已固化为同进程双调用测试，网页端确认 `COLLISION PRIMITIVES LOW CLOSED`。当前定向测试 24 项通过，和旧 v1.12 preceremony 合同联合共 39 项通过。全仓标准发现运行 512 项，用时 952.525 秒：504 项通过、7 项既有声明跳过，唯一失败仍是冻结的 Step28-v12.1 guarded sync manifest 要求 `docs/PROJECT_PROGRESS.md` 回到 2026-07-20 的 199,490 字节快照；当前持续更新文档不得回退，且全部 v1.13 新测试通过。旧 manifest、旧测试和当前进度文档均未为制造全绿而改写。

当前边界仍然是正式种子、能力、数据行、质量回执、模型和指标全部为 0。下一阶段只能建立候选无关父结构、私有资料贡献血缘和只执行一次的试算身份分配；不能开启正式种子仪式或构造正式四拆分。

## 2026-08-10：v1.13 第二阶段实现与网页复审最终通过

本节是当前最新事实。合同第七节第二项已经形成仅限内存开发烟雾重放的实现：`schema/step28_v13_v1_13_candidate_parent_policy.json` 大小 8,158 字节，文件 SHA-256 为 `b550fbb0579e84a46f896b9f39fb29edeec64021d32d61eff94c42b63f768b26`，规范自哈希为 `2c35e235b90a38f317b194cccda66a049767c68eb40cd3c44e439268e9c732ed`；`scripts/step28_v13_v1_13_candidate_parent.py` 大小 57,236 字节，文件 SHA-256 为 `b700265b7791866587f685cdda24d1f299849a0397209dc14fec27c754831e84`；独立 v1.13 身份重映射模块大小 13,854 字节，文件 SHA-256 为 `a85ff8460c86c339489beb1ca5101e16b955e09c22b95dcc965e8662604f249b`。五项正式授权全部为假，正式保管路径明确未发布。

候选无关父对象以规范字节保存世界、结构指纹、公开资料字节和私有资料贡献血缘。结构指纹覆盖 28 个卖家、378 对、商品结构、登记覆盖目标、噪声目标、身份资产和槽位结构、控制者机制以及每个卖家的六项有效风格因子；标题和描述原文不进入该指纹。资料血缘独立重放 Step3：五个冻结字段的每个输出位置记录支持商品集合、首次位置、聚合角色和名次；描述签名片段另记录首次商品、提取片段序号、卖家文档频率及其卖家集合摘要。血缘不保存贡献原文，并与原 Step3 公开资料逐字节对账。

父代构造入口不接受参数，只能从钉住的开发烟雾记录、结构密钥承诺和经实际文件校验的模板、解析夹具、风格资料重放。19 项本地源码与策略依赖形成精确闭包，递归导入测试要求已导入的本地源码集合与策略钉完全相等。一次性身份试配公开入口只接受固定父对象；本阶段没有调用者可传入的历史集合、已提交集合、密钥、计数上限或强制碰撞开关，内部已提交集合固定为空。身份重映射使用新的 `step28-v13-v1.13-identity-value` 域，进入分配器后无论成功失败都禁止第二次进入。

试配后重新运行生产解析、身份脱敏、资料聚合、投影证明和 378 对三十三维身份历史，逐字节证明结构、资料和贡献血缘均未改变。逐资产审计在外层从第零计数开始独立重放首个可接受身份值，核对选择计数、可见文本拒绝数和最终值哈希，不再只信重映射回执。第二阶段 25 项测试与第一阶段 24 项联合 49 项全部通过。网页端连续复审先后发现并关闭候选指纹边界、父代自由选择、身份筛选、失败版本依赖、源码闭包、重复键、回执伪造和独立重放等问题，最终结论为阻断级、高级、中级和低级问题均 0，并给出 `V1.13 CANDIDATE PARENT LOW CLOSED`。全仓标准发现运行 537 项，用时 1052.129 秒：529 项通过、7 项既有声明跳过，唯一失败仍是冻结的 Step28-v12.1 同步清单要求 `docs/PROJECT_PROGRESS.md` 保持 2026-07-20 的 199,490 字节快照；没有 v1.13 新失败或运行错误，也未改写历史清单或回退当前进度文档来制造全绿。没有生成数据、回执或临时实验产物。自然表述候选、候选重试、事务目录、正式种子、正式数据、模型和指标仍全部为 0；下一阶段只能实现受限候选可见视图和独立自然表述变化能力。

## 2026-08-10：v1.13 第三阶段受限视图与自然表述变化最终通过

本节是当前最新事实。第三阶段仍只在一个钉住的开发烟雾世界内存重放，不写候选世界或正式数据。机器策略 `schema/step28_v13_v1_13_natural_variation_policy.json` 为 8,227 字节，文件 SHA-256 为 `f66290e6d95628d133da0f1eef55f53b82a6f884b67909340a5f235b8be1186b`，规范自哈希为 `23dfa4b3b489d9af4ed27a253446ee5e107cc87ef39e4fab0e5131ab5a034777`。独立纯渲染器 `scripts/step28_v13_v1_13_pure_natural_renderer.py` 为 27,637 字节，SHA-256 为 `de61f3e83fdb6f0c67a70cd629e0c93a3b8099c0bb50e81bb117c5698c8c1cb0`；可信宿主 `scripts/step28_v13_v1_13_natural_variation.py` 为 69,849 字节，SHA-256 为 `5ba93638765b3988e78462f962a5d29c0190df63b3bcdf0b5666983ad93c30d5`。

纯渲染器只接受受限候选视图和 32 字节候选键，只导入标准库；受限视图只有匿名商品、噪声目标和安全表述库，不含标签、控制者、身份、登记覆盖、标题克隆或高语义机制。六条私有登记覆盖只保留在可信绑定中；固定标题克隆也只在可信组装器内按登记顺序施加。可信组装器重新建立唯一视图和绑定、重新派生候选键、重绘并逐字段比较候选，同时以两个冻结的完整状态承诺覆盖候选父对象和身份试配对象的全部字段，调用方不能靠构造一套自洽但不同的父对象绕过固定根状态。任何候选调用未正常完整返回，包括 `KeyboardInterrupt` 和 `SystemExit`，都会永久毒化本次会话，禁止跳过候选。

32 个候选的最终自然文本和完整世界均各自 32 个互异；相对第零候选变化商品数范围为 51 至 105。受限视图 SHA-256 为 `47ba01d22eb6a55694620970f30cd4acfcf4a1f3e696ed88579bc6e61aa9e85a`；第零候选最终自然文本 SHA-256 为 `5db775cf5040bf7f2f782e3bfacc4de2038bbbdefc1472f26da0404be56c7644`，完整世界 SHA-256 为 `fac76459aeab02992f74993f253a43bc13c6380ddefb923ee7554f83004a3722`；32 个自然文本哈希和世界哈希的有序合并 SHA-256 分别为 `8901662f1790d0d1bd921b815a3f425fe6470eab4da32fd1c7766aff67bf5cef` 和 `ca6c53d8025c1a452fdb838cb1be354a4e6eef50e5ebf37c51c367b12611c161`。这些数值只证明候选变化链可确定重放，不是质量指标。

网页端 GPT-5.6 Sol 极高推理首次攻击审查为阻断级 0、高级 4、中级 2、低级 1；修复受限视图泄漏、纯函数隐性权威、组装器重建、异常跳号、策略键集、递归密钥隔离和死代码后，第二轮只剩调用方伪造父对象的 1 个高级问题。加入覆盖全部数据类字段的完整状态承诺和三类重绘前拒绝测试后，终审四级问题均为 0，并给出 `V1.13 NATURAL VARIATION FINAL GO`。本阶段 34 项定向测试通过；第一至第三阶段联合 83 项全部通过。最终全仓发现运行 571 项，用时 1069.510 秒：563 项通过、7 项既有声明跳过，唯一失败仍是冻结的 Step28-v12.1 同步清单在运行时要求当前 208,993 字节的 `docs/PROJECT_PROGRESS.md` 回到 199,490 字节历史快照；没有 v1.13 新失败或错误。

第三阶段通过不授权正式运行。正式种子、派生能力、正式候选、正式数据行、质量回执、模型和指标仍全部为 0。下一阶段只能实现精确商品/卖家文档碰撞驱动的候选循环、不可覆盖的逐世界事务和拆分封存，再经过同样的复审与全仓验证；不能直接开启种子仪式或训练模型。

## 2026-08-10：v1.13 第四阶段甲——内存候选选择最终通过

本节是当前最新事实。第四阶段甲只实现一个钉住的开发烟雾世界的内存候选循环，不写事务目录，也不授权正式候选或正式数据。自然变化策略和可信宿主因补入候选零之前的精确标题克隆资格证明而更新：策略大小 8,227 字节，SHA-256 为 `eb24f5ac99ffadb278ff6ec0ccad1a25d44d74818bf242d27f277fe6449e6fb0`，规范自哈希为 `562dce55d87f39b87b80dad8060bf16130c801213c22a89f70bf249932e665bf`；可信宿主大小 82,103 字节，SHA-256 为 `6f49f3c9e1ed322195a9a541a2960d25bd64f50a6d823c0c0c3a9f44fdae9e5b`。资格证明固定原始登记顺序、左右卖家与商品、来源标题非空、目标标题和描述非空、端点此前未使用、唯一匹配的负例标记，以及候选父代和冻结身份父代完整状态根；受限候选视图仍不含这些私有机制。

候选选择策略 `schema/step28_v13_v1_13_candidate_selection_policy.json` 大小 6,355 字节，SHA-256 为 `ddce84eeaf8c5e74067efacf497426c682acd14b62c13e87deb5ab8e53ebe6a0`，规范自哈希为 `32f8a431311eba03e97068dc3b4597a24e4befe8867dd8311dde755c580a783d`；脚本 `scripts/step28_v13_v1_13_candidate_selection.py` 大小 49,420 字节，SHA-256 为 `73888224e44b606c6204d002397c2cec986c9d61d60222fdada56e5d1d048a55`；测试大小 32,424 字节，SHA-256 为 `1c8d4f51b6f6075586a49d7c516b961c3d944a45e8f13ec45349f44bdbac16e8`。每个依赖键同时绑定唯一规范路径、大小和哈希，重新绑定到另一真实文件并重算策略自哈希也会失败。

选择器公开构造器和 `select()` 均不接受历史集合、当前拆分集合、前序拆分集合、密钥、候选上限或强制故障参数。每个候选先完成可信自然组装，再独立重放生产脱敏商品与最终五字段卖家资料；只有同世界、历史、当前拆分或前序拆分的精确商品／卖家文档碰撞可以继续候选。其他异常优先关闭选择器。重复摘要先被分类为可重试碰撞，只有零碰撞候选才进入 105 个商品、28 个卖家和注册表多重性接受门。

接受对象保存完整 84 项身份分配增量、商品／卖家注册表增量、选择上下文、标题克隆资格和全部候选字节。验证器会从接受世界重新运行生产脱敏、资料、文档哈希和八类碰撞分类；同时必须逐字段等于本次选择器实际获得的可信完整候选。选择器还保留本次真实接受状态根，完成态复核不接收调用方提供的权威根；即使同步修改拒绝计数及其所有派生摘要，也不能冒充真实搜索历史。失败复核会永久毒化选择器。模块没有写盘调用，接受对象明确为 `design_smoke_only=true`、`committable=false`。

真实烟雾选择在候选零即得到零碰撞结果：105 个商品注册摘要、28 个卖家注册摘要和 84 个身份分配增量。选择上下文 SHA-256 为 `b0aef4fe60d1b2a117b30be9788a42bd6cba2a40123dfb82f8946640b199428d`，标题克隆资格 SHA-256 为 `145c94bfd1ad0ffe5639d9c62e3c5a7c7c8531923e7723aa34e5bb803857b9fe`，接受完整状态 SHA-256 为 `6f14e23acbb0b7adbfda3e7044c9d5389338db30fd5692dee2a2aaff3802343f`。这些是开发烟雾重放金值，不是科研效果指标，也不是正式数据结果。

网页端 GPT-5.6 Sol 极高推理先发现接受对象重放和规范路径绑定问题，修复后又发现可信候选与真实拒绝历史尚未绑定；最终通过保留可信完整候选和原始接受状态根关闭。终审为阻断级、高级、中级、低级问题均 0，并给出 `V1.13 CANDIDATE SELECTION IMPLEMENTATION GO`。本阶段 31 项定向测试通过；第一至第四阶段甲联合 114 项全部通过，用时 238.920 秒。全仓标准发现运行 602 项，用时 1430.937 秒：594 项通过、7 项既有声明跳过、1 项失败且没有运行错误。唯一失败仍是冻结 Step28-v12.1 同步清单要求运行开始时 212,007 字节的 `docs/PROJECT_PROGRESS.md` 回到 199,490 字节历史快照；没有 v1.13 新失败。

第四阶段甲通过仍不授权正式运行。正式种子、派生能力、正式候选、事务、数据行、质量回执、模型和指标全部为 0。下一阶段只能在同一排他锁内实现状态重建、候选选择、不可覆盖的 `WORLD_ACCEPTED.json`、连续标记恢复、拆分注册表和 `SPLIT_SEAL_COMPLETE.json`；不得把本次不可提交的开发对象直接改为正式对象，也不能开启种子仪式或训练。

## 2026-08-10：v1.13 第四阶段乙——开发烟雾事务、恢复与封存代码通过

本节是当前最新事实。第四阶段乙只实现一个钉住的 `development_smoke / audit_a / world 0` 临时工作区状态机，不授权正式拆分。机器策略 `schema/step28_v13_v1_13_split_transaction_policy.json` 大小 8,972 字节，文件 SHA-256 为 `b282f62496e65df214870236df05e294d00f4a1117d7dbbcbe839372a8f7bff0`，规范自哈希为 `2ea3c38e2f0b1a10feaad50ef751406c7a67f18dfb23d88efdbb80e4d667128e`。源码守卫大小 12,339 字节，SHA-256 为 `00b3b24ef5ad099175142e36545fea98acf1e142636385c68159c272fcf42136`；事务实现大小 97,248 字节，SHA-256 为 `58ce5d52e1dfb97090acc9c3f1a3499822050fd99612362742fe8a1b4b049fcf`；非发现式合同测试大小 56,832 字节，SHA-256 为 `63792333f78a3f1692af0a5fb8cd3914a747d630879d652d01e10af30c93641f`。

排他锁在建立临时根、读取恢复状态和选择候选之前取得。世界成员先以排他临时文件写入并同步，再不可覆盖发布；`WORLD_ACCEPTED.json` 最后发布。恢复会重放完整生产文档和冻结候选金值；封存后又用固定 `render(0)` 独立推导世界标记、七个最终投影、拆分封印和清理回执。已有结果只允许逐字节验证，未知条目、链接、硬链接、待发布文件错态、成员复活、布尔值伪装和同步篡改均关闭失败。完成后只保留世界标记和清理回执，整个开发烟雾临时目录由外层自动删除。

正式入口固定为 `python -I -S -B scripts/step28_v13_v1_13_source_guard.py --focused-tests`。守卫在读取项目策略和源码前拒绝项目字节码、预载项目模块、`site` 启动和缺失的隔离参数；它直接编译执行刚验证的事务和测试源码。导入顺序固定为解释器标准库、第三方依赖、规范项目脚本；第三方环境字节尚未固定、用户包路径可能受操作系统环境影响，敌对第三方不在本开发烟雾威胁模型内，正式运行前必须另做环境证明。每个冻结项目模块仍须在首次导入前由 `find_spec` 精确指向钉住的规范源码。普通 `unittest` 发现明确不包含、也不认证本阶段合同；合同文件在任何导入前要求守卫注入的固定模块名和专用标记，直接脚本执行会在测试开始前关闭失败。

权威专项攻击测试 60 项全部通过，另有 1 项因 Windows 普通用户无符号链接权限而声明跳过，用时 370.870 秒；第一至第四阶段甲 114 项最近一次回归全部通过，用时 244.212 秒。真实隔离烟雾达到 `SEALED_CLEANUP_COMPLETE`，退出后项目字节码缓存和烟雾临时目录均为 0。网页端 GPT-5.6 Sol 极高推理经过九轮攻击与修正，最后关闭普通发现入口的字节码抢占、发现期字节码冲突、平台相关跳过数硬编码和合同直接执行旁路；终审为阻断级、高级、中级、低级均 0，并给出 `IMPLEMENTATION CODE GO, EXTERNAL ANCHOR PENDING`。随后普通全仓发现明确不包含这 60 项权威合同，共运行 602 项、用时 1681.523 秒：594 项通过、7 项既有声明跳过、1 项失败且无运行错误；唯一失败仍是冻结 Step28-v12.1 清单要求运行开始时 219,186 字节的 `docs/PROJECT_PROGRESS.md` 等于 199,490 字节历史快照，没有 v1.13 新失败。

四文件实现已由提交甲 `dbafb62f91a51b057b5a4846b8028de4076c7c1c` 及树 `c7ea90eafe62b0682722e87bf780bf2adab95358` 固定，并由后继提交中的独立回执 `docs/STEP28_V13_V1_13_SPLIT_TRANSACTION_IMPLEMENTATION_REVIEW_20260810.zh.md` 锚定提交号、树号及四文件固定值；后继提交不得修改这四份字节。该父锚只关闭实现来源循环。本阶段仍只证明一世界开发烟雾状态机；正式种子、正式能力、正式候选、正式数据行、质量回执、模型和指标仍全部为 0。不得直接启动正式 500 世界、种子仪式或训练；下一步只能设计正式多拆分、前序封存、Linux 目录持久性和环境证明升级。

## 2026-08-10：科研实验合同取代过度工程化下一步

本节是当前最新事实，并覆盖上一节最后一句关于继续设计 Linux 目录持久性和环境证明的旧下一步；历史实现与测试记录保持不变。此前未提交的正式拆分链就绪草案、来源追踪草案和浏览器临时审查文件已清理，不进入科研产物。后续不得把网络攻防、敌对运行环境或通用文件系统隔离扩展为本项目主线。

新的权威主合同为 `docs/STEP28_V13_V1_13_SCIENTIFIC_EXPERIMENT_CONTRACT_20260810.zh.md`，文件大小 19,037 字节，SHA-256 为 `936ba6d52fa983307c3be654d9996814e740aea154c290c86411064d35e480f6`。网页端 GPT-5.6 Sol 极高推理只从科研设计、统计与可执行性进行了四轮审查；最终结论为阻断级 0、高级 0、中级 0、低级 0，明确给出“合同可进入实现”。该结论只允许进入构建器实现，不等于正式数据或模型成功。

科研主线固定为：先生成互不复用的训练集、开发集、审核集甲、审核集乙，每拆分 500 个世界、189,000 个完整卖家对；通过精确文档碰撞、禁止元数据和文本反事实等训练前质量门后，冻结英文 M0 分数，训练五个 M1、正确身份适配 M2、M3-base 和 M3-joint，再依次盲评审核集甲和审核集乙。核心证据是 `M2-mean(M1)` 及五个逐 M1 对比；`M2-M0` 为辅助，`M3-joint-M3-base` 为直接中文监督下的身份增量，`M2-M3-joint` 为策略级强对照。

最终指标纪律不可省略：分类必须分别保存和报告 AP 与梯形 PR-AUC，并报告 ROC-AUC、Precision、Recall、F1、Specificity、Balanced Accuracy、MCC、Brier、Log Loss 和 Recall@FPR=1%；检索必须报告 MRR、MAP、Recall@1/3/5/10 和 NDCG@1/3/5/10。M0、五个 M1、M2、M3-base、M3-joint 在开发集、审核集甲、审核集乙均受此约束。

当前正式种子、正式数据行、M0 分数、M1/M2/M3 模型和正式指标仍全部为 0。下一步只能按合同第九节第一项扩展 500 世界乘四拆分的数据构建和最终合并链，随后实现同合同一致的机器策略与质量测试；质量门通过之前不得开始训练。

## 2026-08-11：v1.13 科研多世界构建器通过实现终审

本节是当前最新事实。依据 2026-08-10 科研实验合同实现了四拆分、多世界、候选重试和最终合并构建器，但授权范围仍只是设计预检，不是正式生成或训练。机器策略 `schema/step28_v13_v1_13_scientific_dataset_builder_policy.json` 为 7,493 字节，文件 SHA-256 为 `061b6308b2b85446b08526e32580e7919f0a232624bcb10f9e9460f4f37f806e`，规范自哈希为 `35d925d98a12203bb580015992dfdd047e87299887b6a3d1127b64a273bafd36`。三个运行源码分别为：`scientific_common.py` 16,457 字节、SHA-256 `60841e798407aa9b41362676de8d067aa0cde2cf310779adb73df54933e8a3ae`；`scientific_world.py` 37,180 字节、SHA-256 `4be02d0e1f43a4a5e6c573cfe40863b78a3595ce4487b2797f2037598c6dadbd`；`scientific_dataset_builder.py` 41,001 字节、SHA-256 `db2c4383418b066e10b2cbc7c9c23713e37956d69c199ca77e62bb20cb3afb82`。策略逐一钉住三份源码的规范路径、大小和哈希。

每个世界固定为 28 个卖家、12 个控制者、378 对卖家关系，其中 20 对为同一控制者、358 对为不同控制者，并生成 33 项身份历史。候选阶段先建立结构和身份，再把匿名受限文本交给自然变化模块；只有六类精确商品或卖家文档碰撞可以推进候选。标签和控制者真值只在候选接受之后构造。六类编号全集、各拆分和全局的商品文档、卖家文档、身份值均采用原子注册，世界序号必须严格等于从 0 开始的连续集合。最终写盘后重新读取根清单、拆分清单和每拆分 11 份文件，核对路径、字节数、SHA-256、行数和自哈希后才允许原子发布。

模型可见卖家表采用正向字段许可清单，只含连接用卖家编号、五个冻结文本字段、商品数、标题／描述长度中位数和五项既有安全风格统计；拆分、候选序号、标签、控制者、来源市场、来源数据集和宽表资料均不可见。商品表只有连接编号及标题、描述。自动发现特征和挂载完整卖家宽表均被策略禁止。冻结 `legacy18` 投影回归分别从完整资料和严格投影独立建立参考状态、独立生成全部 378 对的安全特征，并逐值证明 18 项模型可用特征相同。

定向合同测试最终为 16 项全部通过，用时 77.388 秒，覆盖策略关闭、随机权威互斥、真值和标签隔离、候选碰撞推进、耗尽后注册表不污染、确定性重放、模型字段许可清单、六类编号碰撞的原子失败、真实四拆分端到端双重构建、输出树单字节篡改、世界序号和 `legacy18` 独立投影等。四拆分各一世界的小烟雾清单为 2,638 字节，文件 SHA-256 `08832aea24e7937050a8e28ab5a90f0668cdcf2ce3109914de9e3de705a0d6bf`，规范自哈希 `698be3c55233c72a6708f2dfae2621d0626cc0185f12f9ef1994ed67b5ba1277`；共 4 个世界、112 个卖家、423 个商品、1,512 对关系、80 个正关系、48 个控制者、112 个查询和 341 个身份值。该烟雾仅证明构建与封存闭合，不是数据质量通过，也不是科研效果结果。

网页端 GPT-5.6 Sol 极高推理在多轮审查和修正后，最终给出阻断级 0、高级 0、中级 0、低级 0，并明确只允许启动训练 50、开发 50、审核甲 2、审核乙 2，共 104 个世界的设计预检。正式模式继续由策略和代码双重关闭；正式种子、正式候选、正式数据行、M0/M1/M2/M3 模型和指标仍全部为 0。下一步是运行 104 世界设计预检并完成统计、文本和逐行捷径质量审计；在这些质量门通过、科研文档再次更新并取得正式授权以前，不得启动 500×4 正式生成或任何训练。

## 2026-08-11：首轮 104 世界设计预检在第 4 个世界关闭失败

运行编号为 `design_preflight_v1_20260811`。使用正确的非隔离 Python 入口后，训练拆分第 0、1、2 个世界完成；训练拆分第 3 个世界、候选 0 在任何最终目录发布前触发 `Exact-title clone endpoint is unqualified`。构建器自动删除 `.building` 目录，最终输出目录不存在，因此本轮没有可发布数据、质量结论、训练资格或模型结果。

只读确定性复现进一步查明：该世界第 1 条精确标题克隆关系的来源标题和目标标题均非空，但目标商品的结构标记 `description_nonempty=false`，候选基础描述也为空；第 2 条克隆关系三项均合格。根因是既有登记覆盖选择器对所有覆盖商品只要求标题非空，没有对精确标题克隆的目标商品额外要求描述非空。固定四世界烟雾没有覆盖这种多世界状态，因而此前 16 项测试未发现该缺口。

旧策略自哈希 `35d925d98a12203bb580015992dfdd047e87299887b6a3d1127b64a273bafd36`、旧世界源码 SHA-256 `4be02d0e1f43a4a5e6c573cfe40863b78a3595ce4487b2797f2037598c6dadbd` 和旧构建器源码 SHA-256 `db2c4383418b066e10b2cbc7c9c23713e37956d69c199ca77e62bb20cb3afb82` 仅用于标识本次失败边界。`design_preflight_v1_20260811` 永久关闭且不得复用；旧实现不得再生成数据。修复必须在候选文本生成前、保持卖家关系和标签不变的条件下确定性限定商品端点：来源标题非空，目标标题和描述同时非空；选择不得读取标签、模型分数或捷径探针。还必须增加训练拆分第 3 个世界固定回归以及全部 104 个世界的端点资格预检，通过后才可使用新的版本化输出根清洁重跑。

## 2026-08-11：端点资格 v2 修复通过回归与网页终审

失败的构建器 v1 已被 v2 原地替换，旧字节仅保留在上一节哈希记录中。v2 策略文件为 8,189 字节，SHA-256 `5c4ba22cbbd001efab521384a9a988410f30703bf2657a1d425bd8eba4d2629a`，规范自哈希 `8f40cc0b008e6447e5ace55b59159545346c6527a91d2129677e59ce087a7a47`。当前三份运行源码为：`scientific_common.py` 17,390 字节、SHA-256 `99aaaef84766eb4dd57a7adcbf7f70a0b7ba04bcf153697883980145c8163385`；`scientific_world.py` 47,252 字节、SHA-256 `9a3a3101a01f1cd671e381b9bc86b7d3b664eaec533e9f9e80914ac1cd40550a`；`scientific_dataset_builder.py` 41,124 字节、SHA-256 `e15e4e1125f782e069d1d4c123003d8562a4dc2a4024faa7c2b30c6ba5a05028`。测试文件为 29,561 字节、SHA-256 `368c08dc439b21ea21f0b1aa3de61c5b07c42304a7f8db3481134a91b240bd9c`。

新增资格步骤位于基础世界生成之后、身份值重映射和自然候选渲染之前。它固定卖家对、左右方向、规范关系编号、控制者成员关系和负例标记，只允许在已指定卖家内部重新选择商品：来源标题必须非空，目标标题和描述必须同时非空。商品排序只使用已有拆分结构密钥、新的独立域、世界编号、资产序号、左右角色和商品编号，不读取标签、模型分数、捷径探针或候选结果。旧两条克隆先逐条重放并撤销，旧目标恢复自身基础标题，再施加更新后的两条登记克隆；四条高语义覆盖商品保持不变，六项覆盖最终仍为 12 个互异商品端点。资格回执及更新后的覆盖审计均进入结构父对象承诺和私有世界审计。

新增三项测试固定复现训练拆分第 3 个世界原始目标描述状态 `[false, true]`，证明至少一个不合格目标被迁移且卖家关系不变；逐一重建全部 104 个设计预检世界和 208 条精确标题克隆，证明控制者成员关系、负例标记、卖家对与方向逐世界不变，所有端点合格且 12 个覆盖商品唯一；人为让目标卖家没有任何带描述商品时必须关闭失败。包含既有端到端、篡改、六类编号、序号、字段许可清单和 `legacy18` 独立投影在内的完整 19 项合同测试全部通过，用时 87.272 秒。

网页端 GPT-5.6 Sol 极高推理逐字节复审最新六个附件，独立核对上述大小、哈希、策略自哈希、源码钉和 19 个测试定义，最终为阻断级 0、高级 0、中级 0、低级 0，并明确给出“允许清洁重跑104-world v2设计预检”。授权上限仍仅为训练 50、开发 50、审核甲 2、审核乙 2。v2 输出根为 `design_preflight_v2_20260811`；正式授权仍为假，正式种子、正式行、模型和指标仍全部为 0，不得启动正式 500×4 或训练。

## 2026-08-11：104 世界 v2 设计构建完成，尚未获得质量或训练资格

`design_preflight_v2_20260811` 使用正确的 Windows CPU 入口一次完成，用时 298 秒。最终根清单为 2,663 字节，文件 SHA-256 `c7b323d9d3b76a0795ce45452b4989926cae51fca7101bc6d04acf6fdf4a93f3`，规范自哈希 `9baa90828cf459bcee3cc6101c166f6c1084353dd2997e40e5d3d85d29f49d48`；输出树共 49 个文件、约 89.59 兆字节，发布后 `.building` 目录不存在。独立重新读取根清单、四份拆分清单和全部拆分文件，大小、哈希、行数、自哈希及根到拆分绑定再次通过。

本轮共有 104 个世界、2,912 个卖家、10,561 个商品、39,312 个完整卖家对、2,080 个正例和 37,232 个负例；每个世界仍为 378 对和 20 个正例。训练集与开发集各 50 个世界、18,900 对和 1,000 个正例；审核甲、乙各 2 个世界、756 对和 40 个正例。六类编号全集、10,561 个商品文档、2,912 个卖家文档和 8,746 个身份值均无跨世界复用。公开卖家资料和商品表逐行复核后，字段集合、嵌套字段、连接编号、类型和禁用字段命中均为零异常。

103 个世界在候选 0 接受；开发拆分第 45 个世界的候选 0 与历史商品文档发生一次精确碰撞，按冻结规则推进到候选 1 后接受。没有同世界、当前数据集或卖家文档碰撞，也没有依据标签、模型分数或捷径结果推进候选。208 条标题克隆资格回执全部闭合；来源商品迁移 130 次、目标商品迁移 116 次，候选商品数均至少为 2，且卖家对、方向、控制者成员关系和负例标记保持不变。

根清单状态是 `PASS_DESIGN_BUILD_NOT_TRAINING_QUALIFIED`，并明确 `scientific_use_forbidden=true`、`training_started=false`、正式种子和正式行数均为 0。该输出当前只作为训练前质量审计输入保留，不是正式中文数据集，也不能用于 M0/M1/M2/M3。下一步必须按科研合同实现并运行逐行文本与身份审计、拆分隔离和精确碰撞复核、禁止元数据探针以及控制者盲作者风格反事实审计；全部硬门通过后仍只能进入正式代码与策略冻结，不能直接训练。

## 2026-08-11：质量审计实现第一版在运行前审查中关闭

本节是当前最新事实。质量审计第一版只完成了代码和机器策略草案，尚未运行 104 世界质量审计，也没有创建 `quality_audit_design_v1_20260811` 成功或失败输出。网页端 GPT-5.6 Sol Pro 用时 42 分 48 秒完成调用链审查，结论为阻断级 5 项、高级 5 项、中级 3 项、低级 1 项，明确给出“不允许运行”。五项阻断分别是：向 scikit-learn 传入超出 32 位范围的模型随机种子；审核集甲乙通过世界重建实际物化控制者、标签和检索相关性；世界重抽样按不透明世界编号排序而非冻结序号；私有字面扫描漏掉真实身份值和反事实输入；商品数及字段缺失率没有按模型实际可见投影计算。

第一版质量策略原始 SHA-256 为 `9f5a6b973acf84d6a0efd57272a449a0bca258af0a7eaa5c685ec4f720cae2a2`，规范自哈希为 `d14712a774eb609a686a689a5aa32063805bfd566c1a4dd20bcae3d3c97cc57f`；第一版质量脚本为 62,770 字节、SHA-256 `166ac614ab35fed1a45fa2acc749433fc1cb1b8ad4b85ba86f1b7555c8148dc8`；第一版质量测试为 9,031 字节、SHA-256 `b5726175b17591d5191d0a75bc68af7bcb2fe9f56368b4242c6d1171f1e5df3c`。这些值只标识被审查否决的实现边界，不得再用于运行。失败实现代码已经由版本化第二版替换，不保留旁支副本；由于没有启动实验，没有失败数据、模型、缓存或临时运行目录需要保留或删除。

这次否决针对质量审计器，不是对既有 104 世界设计数据质量的判决。因此 `design_preflight_v2_20260811` 暂时只作为待审输入保留，仍无训练资格；如果第二版实际质量门失败，必须先记录小型失败回执和根因，再删除被判无效的数据载荷。第二版必须关闭全部阻断项和可接受的高级、中级问题，通过行为测试与同一网页模型复审后，才可清洁运行。正式种子、正式数据、M0/M1/M2/M3 模型和正式指标仍全部为零。

## 2026-08-11：质量审计 v2 血缘假设被实证否决，v3 三路径实现待终审

质量审计 v2 关闭了第一版网页审查提出的非法模型种子、审核集破盲、世界重抽样顺序、私有字面扫描、模型可见缺失来源、完整策略叶校验、精确文件全集、失败回执和证据绑定等问题，但没有运行 104 世界审计。针对首个固定训练世界的新增行为测试发现：原始 Step3 画像贡献血缘为 576 行，风格反事实后为 574 行；91 个按卖家、输出字段和排名对齐的槽位发生变化，全部位于 `signature_description_concat`，覆盖 25/28 个卖家。变化包括 66 个来源商品集合、70 个首次位置、24 个描述片段序号和 39 个跨卖家文档频率变化。三十三维身份历史、商品非文本结构和语义 AST、身份值、噪声、机制及标签仍保持不变。

网页端 GPT-5.6 Sol Pro 据此作出方法学更正：原始与反事实 Step3 血缘相等不能作为硬门，因为重新分段、跨卖家文档频率和 top/signature 选择是风格干预后的合法下游中介；当前固定无固定点置换不是失败，不得重抽。修订必须采用同一置换和同一反事实商品的三条路径：固定商品支持文本 F、真实 Step3 生产输入 P、两者加 16 项模型可见数值的联合输入 U。F 固定 `(world_uid,seller_uid,item_uid,field)` 槽位并逐商品向量化后作顺序不变卖家聚合；P 要求反事实生产血缘与第二次独立重放逐字节相同，同时保存不可筛选的原—反事实血缘差异回执；U 联合 P 的 75 维全视图、F 的 33 维全视图，以及商品数、两项长度中位数和五项风格统计的绝对差与和，共 124 维。

当前工作版已把生产 3 视图、固定支持 3 视图和联合 1 视图分别交给固定 L2 逻辑回归与深度 2 浅树，形成 14 个探针。单特征、14 模型点估计和 9,999 次世界重抽样均在完整家族内取最大值，继续使用每世界 372 对、20 个正例及固定 `20/372` 平均精确率基线。新增负测覆盖删槽、重复槽、跨卖家移动、空值模式变化、输入行重排、商品标识一一改名、反事实血缘确定重放和第 14 个高分模型不得漏出 bootstrap 家族。当前 22 项针对性测试全部通过；包含候选父对象、文档碰撞、候选选择、自然变化、科学构建器和质量审计的完整 v1.13 合同组共 155 项，也在 296.996 秒内全部通过。质量策略为 17,003 字节、SHA-256 `bdb6178d1b87b766ab14706c5e97e24087dcda16729ee92d9c86130a97991d62`、规范自哈希 `76d942244d2d8c71a7019879eea13867073c01a8a90213d382c822e52c234f99`；反事实源码为 19,230 字节、SHA-256 `1c1756712ee3cdd9d56998825f47409a3f6a316d4fb81e6488d59f9c5877bce0`；质量审计源码为 144,744 字节、SHA-256 `717fbed3d29dbca831420b852863586bcea4d1e658c8d372fae57ff7bcdf6c2d`；质量测试为 37,252 字节、SHA-256 `2f4b24bf16fa738eaa582c1f9b11e29dc7c41af864947f470ae2c047ea7a4e70`。网页实现终审尚未完成。

本节不构成质量通过或运行授权。网页方法学裁决明确仍不允许启动 104 世界质量审计；正式种子、正式数据行、正式 M0/M1/M2/M3 模型和指标仍全部为 0。只有 v3 完整合同回归通过并获得网页端明确“允许清洁运行104-world质量审计”后，才可清理临时文件并运行该设计级审计。

## 2026-08-11：质量审计 v3 运行前终审否决，转入 v4

质量审计 v3 没有运行 104 世界审计，也没有创建质量 PASS、FAIL、模型或指标产物。网页端 GPT-5.6 Sol Pro 对本轮 7 个当前附件独立复算字节和哈希并逐调用链审查后，结论为阻断级 4 项、高级 3 项、中级 2 项、低级 0 项，最后一行明确为“不允许运行”。v3 的旧边界是：质量策略 17,003 字节、SHA-256 `bdb6178d1b87b766ab14706c5e97e24087dcda16729ee92d9c86130a97991d62`、规范自哈希 `76d942244d2d8c71a7019879eea13867073c01a8a90213d382c822e52c234f99`；反事实源码 SHA-256 `1c1756712ee3cdd9d56998825f47409a3f6a316d4fb81e6488d59f9c5877bce0`；质量源码 SHA-256 `717fbed3d29dbca831420b852863586bcea4d1e658c8d372fae57ff7bcdf6c2d`；测试 SHA-256 `2f4b24bf16fa738eaa582c1f9b11e29dc7c41af864947f470ae2c047ea7a4e70`。这些字节永久不得运行。

四项阻断为：F 路径把卖家空槽率均值错误送入布尔 `empty_both/empty_xor`，导致交换卖家对左右端点后特征变化；7 视图／14 模型 C 方案尚未进入机器可钉住的合同修订件；审核甲乙缺少不解封关系和标签的真实私有字面精确扫描；P 血缘只验证复数来源集合，没有完整验证单数来源商品、模式、类型、范围、计数、摘要和连续排名。高级问题是审核私有文件未作纯字节完整性核验、失败回执没有按失败类型真实处置设计数据，以及 22 项测试对上述错误产生假绿。中级问题是策略叶测试没有覆盖 pins/runtime/version/status 的外部启动边界，以及三路径运行后对齐回执证据不足。

v4 必须保持当前固定风格置换域和映射不变，修复所有上述问题，并新增原合同第 4 节第 5 项的版本化 C 方案修订件。修订件只替换质量审计方法，不改变原合同其余边界。v4 仍须通过针对性测试、完整 v1.13 合同组和同一网页模型复审；未取得明确放行前不得运行。正式种子、正式行、正式模型和正式指标继续全部为 0。

## 2026-08-11：质量审计 v4 运行前终审否决，转入 v5

质量审计 v4 没有运行 104 世界审计，也没有创建质量输出、模型或指标。它修复了 v3 的四项阻断，34 项质量合同和六组共 167 项 v1.13 合同分别全部通过；后者耗时 337.052 秒。v4 永久关闭字节为：C 方案修订件 8,694 字节／`c930d9f8c9d29ba796556d590603cc48a7ca05c898c174933d9fdf07234f6e46`；质量策略 18,390 字节／`884006ba248e602b06832292eb8039ce59d4feab0cf846794f9f243991649c2e`，规范自哈希 `c7e9a05504602e64bfdeb4bacd94a3d2b9cfc8e90a0e4066529433b812a56ebd`；启动锚 2,159 字节／`1d300e7c893ce1231816bb9ecfad77d06f7529fa9b92256a3e9ccf9754ac5e08`，规范自哈希 `595cefbb44b41294be5e3df1a725ce3f8285a5b92ef22b202abdd5e26ed970d4`；启动守卫 3,389 字节／`b2e162f06c5064ed1ad7e9195b59ebd7f4b128a5e09ac0c5e087b8fff9aea47a`；质量源码 176,379 字节／`ab60c45d0faed9307d4a4d1859739d89fee585d3321c1fa351856d539269092a`；封存扫描器 8,805 字节／`e68f8e25dda43e127dda956805066f4a5fae4bb3e27689f4f0c3b2186ebfbe19`；反事实源码 19,230 字节／`1c1756712ee3cdd9d56998825f47409a3f6a316d4fb81e6488d59f9c5877bce0`；测试 59,233 字节／`d238f0cbf3aa14fa54fbaa1fb94d4e28b97e4ae5c00e8ae9537e4fa98f7f8d8e`。这些 v4 字节永久不得运行。

网页端 GPT-5.6 Sol Pro 对 v4 的最终结论为阻断级 0、高级 1、中级 2、低级 0，最后一行仍为“不允许运行”。高级问题是启动锚仍由同一实现仓库维护，缺少独立发布清单和外部来源记录。两项中级问题是封存扫描器仍依赖固定身份类别，未来新增身份类型可能漏扫；剂量虽然进入逐世界回执和摘要哈希，但缺少更强的完整剂量表留存与变异测试。这是审计器运行资格失败，不是 104 世界设计数据质量失败；固定风格置换不得重抽。

## 2026-08-11：质量审计 v5 运行前候选完成本地回归，等待外部终审

v5 只修改训练前质量审计器，没有运行 104 世界质量审计。它增加候选发布清单；封存扫描器改为枚举全部非空私有字符串叶和编号型映射键，并加入网址去协议、去 `www`、平台前缀、账号和数字规范化等派生字面；完整剂量行继续保存在结果中，并分别绑定三条路径；生产画像的跨卖家文档频率、卖家数和卖家编号摘要改由原始与反事实模型可见商品独立重建；F、P、U 三条路径分别返回并核对卖家对顺序、世界顺序和资格掩码；数据失效与审计器执行失败继续分离，决定回执只声明“待清理”，只有真实清理回执才可声明数据已经删除。新增测试覆盖未来虚拟身份类型、裸网址、扫描器输入损坏、发布清单缺失或变异、剂量变异、独立卖家文档频率重放和单路径错序。

当前 v5 精确边界为：C 方案修订件 9,557 字节／`4761ac6ae282a2a8f0e1c6379122be80a2fc10f6a26d1cb51d6e2bd68516451f`；质量策略 18,542 字节／`cb1900e9812305a876c3fd302e15bb660af3e99a4242e8146b202ee5f3bebffc`，规范自哈希 `3c91bfe7e870d58d6495d465ab632f64dc2c9cf4f6f3b6f757a12c43cf171e56`；启动锚 2,380 字节／`8a69323c19e0cc3950597f0ed011f9d9e08acbb91912e683b6bdfdd72bdb42c4`，规范自哈希 `46f43c8fd659d7214a1200d2d4c28e4e93b7b919d53da58c0b818243c50187a3`；发布清单 2,254 字节／`c51892a1933f5f0d71bbfb81fb629a975ca0c7aa62abf0b1ec501d27f0f338c1`，规范自哈希 `bc0d23e3676f65e0d77ef48e30536036549df15725a3d487563aa8e53454519e`；启动守卫 6,639 字节／`7dcf193f8a98166f9079356e76240952cfe1576f7b89d8d37fdaea5a4387abbd`；封存扫描器 11,242 字节／`ab0b5c8b37f708f5c9ed9511c2d332a4fb1f6ce310e2b510577f272908ef309d`；反事实源码 19,230 字节／`1c1756712ee3cdd9d56998825f47409a3f6a316d4fb81e6488d59f9c5877bce0`；质量源码 187,569 字节／`aa4fbc28065e424c55da5609abc64ab2a5b594eb69cf5c19c2bd51f934760bbc`；测试 76,382 字节／`f46f995cdf0afb70c1f0ea64fb0aff421658d43a36fdab648607a47e1a9d0173`。四份自哈希和全部文件钉住关系已独立重算一致。质量合同 38 项在 34.623 秒内全部通过，六组 v1.13 合同 171 项在 416.309 秒内全部通过。测试后没有质量输出、失败目录或构建中目录，缓存已删除。

v5 当前状态只是 `EXTERNAL_REVIEW_CANDIDATE_NOT_FORMAL_AUTHORIZATION`。发布清单只有在网页端审查和后续 Git 来源记录共同固定后才构成外部证据；本地自洽不等于外部放行。下一步只能把上述当前字节交同一网页端 GPT-5.6 Sol Pro 复审。除非其最后一行明确为“允许清洁运行104-world质量审计”，否则不得运行 104 世界审计；正式 500×4 生成、正式种子、审核真值解封、M0/M1/M2/M3 训练和正式指标继续全部禁止。

## 2026-08-11：质量审计 v5 外部终审否决，转入 v6

网页端 GPT-5.6 Sol Pro 用时 47 分 29 秒完成 v5 十个附件的逐调用链终审，独立复算全部申报字节、自哈希和钉住关系一致，但最终给出阻断级 2、高级 3、中级 4、低级 1，最后一行明确为“不允许运行”。因此上一节 v5 候选状态已经终止；上述 v5 字节永久不得运行，`quality_audit_design_v5_20260811` 从未创建，也没有质量结果、模型、正式指标或需要保留的失败载荷。

两项阻断是确定性问题。第一，反事实画像／商品的私有字面泄漏与映射器多重集不守恒仍可能被误分成数据失效，进而删除原本有效的 104 世界设计数据；只有持久化原始数据泄漏和在实现不变量正确前提下低于冻结剂量门才可归数据失效。第二，封存扫描器统一丢弃少于四字符的私有字面，数字规范化只作用于禁止字面而不作用于可见文本，未来映射键仍只识别 `occurrence_counts`，训练／开发扫描又不是双侧对称规范化；独立最小用例已证明 `BAT`、`123·4567` 和未来注册表映射键可产生假通过。

三项高级问题是：候选发布清单没有独立外部 GO 回执，守卫却能把候选状态继续送入正文；审核甲乙的身份值、控制者编号和查询编号跨拆分隔离没有由独立封存检查器闭合；审核甲乙没有验证模型可见 `item_count` 等可由脱敏商品独立重算的数值与实际商品行一致。四项中级问题是启动／策略／运行时失败发生在失败回执保护区之外；调用帧文件名可通过 `compile(..., filename=guard_path)` 伪造；发布清单变异测试没有重算自哈希，可能只在顶层提前失败；删除成功后才写清理回执，存在已删除但缺完成证据的窗口。低级问题是统计失败会把完整大结果打印到标准输出，并把原始异常消息写入失败回执。

外部审查同时确认 F 固定商品支持、P 双重生产重放与独立卖家文档频率重建、U 的 16 项模型可见数值、三路径真实数组／掩码／完整剂量绑定、7 视图 14 模型、9,999 次世界重抽样主体以及审核盲边界没有被 v5 改坏。v6 只能修复上述剩余问题，固定置换、候选、世界、阈值和探针定义不得依据任何质量分数改变。修复后必须使用新策略、清单、源码、测试和输出根重新终审；正式种子、正式 500×4 数据、审核真值解封、M0/M1/M2/M3 模型和正式指标仍全部为零。

## 2026-08-11：质量审计 v6 运行前候选完成本地回归，等待外部终审

v6 只修复 v5 外部终审列出的质量门漏洞，没有运行 104 世界质量审计。禁止字面扫描改为禁止字面与可见文本共用 Unicode 兼容合成／大小写折叠、数字内部分隔折叠和网址／平台等价形式；`BAT`、`QQ`、`TG`、`WX` 使用 ASCII 词边界，`combat` 不会误命中。未来注册表、计数表及按卖家／控制者／查询编号索引的映射键由冻结字段模式收集。封存子进程又独立重算四拆分的身份值哈希、控制者编号和查询编号注册表，对账拆分／根清单并要求任意拆分交集为零，只返回计数、承诺、交集计数和输入哈希。

审核甲乙的商品数、标题／描述长度中位数、数字／标点比例和标题／描述重复率现由脱敏商品独立重算；最大类别占比因脱敏商品不含逐商品类别而明确不声称独立重算。持久化原数据泄漏仍判数据失效；反事实渲染、扫描、置换守恒或映射实现错误改判审计器失败并保留数据，只有实现正确而预登记干预剂量确实不足才判数据失效。数据失效清理在删除前提交不可覆盖的清理意图；删除成功但完成回执写失败时，可由原意图恢复补写。失败回执和标准输出不再保存完整结果或原始异常消息。

同仓候选发布清单不再被当作外部放行。守卫要求后置的独立审查证明绑定发布清单字节／自哈希、候选 Git 提交与树、网页审查原文及精确放行句，并明确只授权 104 世界设计级质量审计；正式生成、模型训练和审核真值解封均为假。主程序文件、命令行入口、直接调用文件和已导入质量模块必须同时一致；伪造代码对象文件名或同名模块不能通过。当前外部证明不存在，因此守卫只能报告等待审查，不能读取数据行。

v6 当前准确边界：修订件 12,180 字节／`1ca1e8b8cea551902cf39f42f5ea33be056c47d80c3693f5bf531c843aa94832`；策略 19,848 字节／`88eddd48eababf595ce095390e195e8991fe60430412bc3737d535ea61c5d2f3`，规范自哈希 `d0edff3b54b407c87d0400c40c25455c5c5e30cd662cd1660add08252faf8c7c`；启动锚 2,404 字节／`8f23706eb407818e95984a46a20a7096e2754cbecb3b2e40b024771ed76d6d79`，规范自哈希 `afc4061c61c0b9032345cd2972fe2ab899e70dde62a2f0fdf2bd78d42d6cbbd1`；候选发布清单 2,257 字节／`cb7c75792a19cbf3950cc3014e17dbcc8c2d43989e927d9de9032add4f62f606`，规范自哈希 `88caac222a519954afa8d2ab15232d2111c6deda66dfb6eec31266e8a3e31f30`；守卫 16,383 字节／`8f3221800261cdd646fb8a9ac5c41ce98edd4750effac2406388ab5c57bc331d`；封存扫描器 22,460 字节／`e25105d10c4f7a3dd4c51e5be35c71e22935804005d4b1cb05e63cb17144a68f`；反事实源码 19,230 字节／`1c1756712ee3cdd9d56998825f47409a3f6a316d4fb81e6488d59f9c5877bce0`；质量源码 207,494 字节／`62c3d89264b826e9159e63aba74e300c451eeeb3a33934b5cf03e2778902361a`；测试 103,666 字节／`6899f19768496f6e79f1add7fdc80047849c8de813bca6bf16514768d9518cd9`。质量合同 47 项在 29.982 秒内通过，六组 v1.13 联合合同 180 项在 315.086 秒内通过。缓存已清理，质量输出、失败目录和构建中目录均不存在。

当前只能把上述准确候选字节交给同一网页端 GPT-5.6 Sol Pro 终审。没有最后一行精确为“允许清洁运行104-world质量审计”的外部审查原文和后置证明，不得运行。正式种子、正式 500×4 数据、审核真值解封、M0/M1/M2/M3 模型和正式指标继续全部为零。

## 2026-08-12：质量审计 v6 外部终审否决，转入 v7

网页端 GPT-5.6 Sol Pro 用时 33 分 18 秒完成 v6 十个精确附件的终审，独立核对申报字节一致，最终给出阻断级 2、高级 1、中级 2、低级 1，最后一行明确为“不允许运行”。v6 从未运行 104 世界质量审计，未创建外部放行证明、质量输出、模型或指标；其策略、锚点、发布清单和源码字节永久不得运行。

第一项阻断是盲边界计数器初始化和实际调用包含 `sealed_registry_isolation_calls`，最终完整字典闭包却漏掉该键，导致正确输入也必然以审计器执行失败退出；47 项测试没有走到组合后的最终闭包。第二项阻断是审核甲乙的封存扫描只读取三份私有文件，遗漏真实 `noise_slot_uid` 以及另外两份私有回执中的内部字符串，与“全部私有字符串叶”和 `noise_slot_uid_scan=true` 的合同声明不一致。允许可见的噪声原文不得因此被禁止；必须新增关系无关的封存禁止字面注册表或等价证据包，并保持观察数据、世界、候选和固定置换不变。

高级问题是守卫只钉住网页审查原文的路径、大小和哈希，却不读取并验证其最后一个非空行，因而一份实际以“不允许运行”结尾的原文仍可能与伪称 GO 的证明组合。两项中级问题是 Git 提交与树只检查四十位十六进制格式而未核对真实对象及钉住文件的干净状态，以及清理完成回执写失败后的恢复函数没有官方可达入口。低级问题是质量运行异常仍可能越过守卫形成含原始异常消息的 traceback。

本地独立复盘另发现审核侧“重复描述占比”重算使用完整描述，而生产 Step3 先清洗并截取 280 字作为重复键；这会误拒绝前 280 字相同、后缀不同的正确画像。v7 必须同时冻结并测试该 280 字规则。外部审查确认 v6 的扫描规范化、四拆分注册表隔离算法主体、F/P/U、7 视图 14 模型和 9,999 次世界重抽样没有被破坏。下一步只修复上述问题并建立新输出根；正式种子、正式 500×4 数据、审核真值解封、M0/M1/M2/M3 训练和正式指标继续全部为零。

## 2026-08-12：v7 密封禁止字面登记器运行前审查尚未放行

为修复 v6 遗漏审核噪声编号及两类私有回执字符串的问题，新增只面向设计数据的密封禁止字面登记器。首轮网页审查曾对旧字节给出一次仅限登记器的许可，但随后按审查建议修改了源码，因此该许可随字节变化自动作废，旧字节从未运行。

第二轮精确审查给出阻断级 1、高级 1、中级 1、低级 1，最终为“不允许生成密封字面登记器”。问题依次是运行时源码闭包漏掉 `step3_build_seller_profiles.py`、`step7_v3_1_source_data.py` 和 `step7_v4_common.py`；原子改名成功后父目录同步失败可能留下最终文件；恢复路径对公开回执的模式验证不完整；构建器策略原始字节未固定。该轮 packer 为 45,789 字节／`9ba672b6e2bb8ed4fb7e51815d5bc7ae0716f6b7986b30620666097a60378f42`，扫描器为 36,574 字节／`db479dae3cf071f376acf0d64cfdbabc7691417a1459f438fd7a91486020f426`，质量源码为 216,106 字节／`0df97a9693a304ae8489f0f4fa6e4b03603707dad5ae8eeaf7e4af5c0af931a6`，测试为 145,097 字节／`f0620eb0cc0182c05b364a87f5e63634b3265898cccd012841b59e416f5b91b8`。这些字节永久不运行。

第三轮精确候选继续关闭了第二轮四项问题，但网页端用时 17 分 37 秒复审后给出阻断级 0、高级 1、中级 0、低级 0，最后一行仍为“不允许生成密封字面登记器”。被拒绝的 packer 为 53,004 字节／`45a250d21a52b38fcdbb5b388f49b603bfaf057fa18edda7b04e0f2c7c30904e`，扫描器为 43,492 字节／`723d53235f140064327a96108ac037e69eb3dbd8651082c8c8d446d41d9243d6`，质量源码为 216,147 字节／`37a0ef84b4ed7c88a2375f4a7a4b8f7bfa236db9143463d05a49abc73875a0a1`，测试为 160,610 字节／`10677e52876cb1912c5c7393db481d3bf344701d962eb1c2375e236f4c10b1f2`，25 文件闭包压缩包为 236,930 字节／`8eba7e549bf2e7c409c6eb45188dac51d0f59c89c4dd0e09560072f1ccf157f1`；完整审查快照哈希为 `02bab3ebe46283ee709d280835eb0c969acb05c8407b355524fcfd0e6b286dc7`。这些精确字节永久不运行。

唯一高级问题是合法的 `KeyboardInterrupt` 或 `SystemExit` 不属于 `Exception`，并且三个目标都在 `_write_once` 返回后才登记到 `created_paths`。中断若发生在原子改名后、登记所有权前，可能留下无事务意图的私有 sidecar；公开回执阶段甚至可能留下孤立的 `PASS_SEALED_LITERAL_REGISTRY_BUILD_NOT_AUDIT_GO` 文件。当前未审候选已改为发布前预登记精确目标，并在 `_write_once` 捕获 `BaseException`、清理本次拥有的精确临时／最终文件后重新抛出。新增测试覆盖三个阶段的 `OSError`、`KeyboardInterrupt`、`SystemExit` 目录同步中断，以及写入函数已返回后的两类中断，共 15 种场景；两项测试用时 21.426 秒全部通过，且证明事务外哨兵不被删除。仍需扩大回归并重新提交精确字节终审；私有 sidecar、公开回执、事务意图、质量输出、正式数据、模型和指标仍全部不存在。

修复后 `sealed_registry` 专属回归为 13 项全部通过，用时 30.586 秒。随后扩大到整个质量单元合同类时，45 项中 40 项通过、5 项错误：其中 4 项因质量策略仍钉住第六版扫描器而报告 `blind_literal_scan bytes drift`，1 项因旧发布清单仍钉住第六版质量源码而报告 `Release pin bytes drift: quality_audit`。这些是登记器生成后才能封闭的 v7 策略／锚点／发布清单待办，不是 packer 事务回归失败，也不能被忽略或改旧历史钉来刷绿。第四轮 packer 外审只核当前精确登记器字节；即使获得许可，也必须先完成一次登记器生成，再用实际 sidecar／公开回执钉住 v7 质量合同并使完整测试恢复通过，之后另行取得质量审计许可。

第四轮网页终审用时 14 分 1 秒，仍给出阻断级 0、高级 1、中级 0、低级 0，最后一行“不允许生成密封字面登记器”。本轮被拒绝 packer 为 53,528 字节／`5a3985a453b452b73f40d9c453e34bf1d72b575b0a2f41a5366ab47a4406ffc6`，测试为 165,595 字节／`85788d244afb8d23618391bf3f805125c43516d96b97d55ae56212300852e2b2`，25 文件闭包压缩包为 238,984 字节／`613fa3143872a33ee290471cfbb02fc3e0943f8688840abf6d344d0feca9285d`；审查快照哈希为 `49f733951141324fb64341a8e0db3d53dc7de442c4f1ef6418a6edee8d913596`。这些字节永久不运行。

唯一高级问题是修复中断窗口时把目标路径在真正取得文件所有权前加入清理清单。初始清洁检查通过后，104 世界长时间重放期间若第二次普通启动创建同一精确事务文件，失败调用会删除另一调用的文件；`Path.replace` 还可能覆盖在不存在检查之后出现的目标。下一候选必须在重放前原子排他取得单执行者锁；最终文件必须采用同目录原子不覆盖发布；清理清单只能登记已经由本调用原子创建并能证明文件实体归属的目标。必须增加三阶段外部目标插入、检查后插入、两个并发事务、外部 `.building`、锁与事务编号恢复等行为测试。该修复仍只属于科研证据事务，不改变世界、候选、标签、字面、门槛或探针。

## 2026-08-12：第五轮登记器候选未运行，活锁恢复反例已关闭

第五轮候选没有运行。其 packer 为 62,037 字节／`5de4bd0a0b1303801c97b7045eb3995fc4aea834869cbd27e96a2b8af3ece45a`，测试为 182,438 字节／`6d1aba19f0a1540d3ab4649ce9411420e4ea909f566c0e5361af4b38aa17df84`，25 文件闭包压缩包为 238,257 字节／`143060c2bd89b1edd8cd93da2c0fe6440c0ec50d1f6d2d64ef3356c5b2cec164`。网页端流式审查错误引用了附件中不存在的旧版“发布前预登记路径”代码，已主动停止，不能作为有效外审结论；大型无效页面快照不保留。本地新增并发反例同时证明：构建进程持有锁并正在重放、尚未写事务意图时，恢复入口会把活锁误作中断残留删除。上述字节永久不运行。

当前最小修复没有引入进程号、操作系统锁或网络相关设计。锁的同目录 `.building` 标记在构建全程保留；自动恢复看到该标记一律判为“活跃或状态不明”并失败关闭，不能删除锁。正常结束或可捕获异常只由本调用清理标记。登记器专属 16 项合同在 119.120 秒内全部通过，覆盖运行中恢复拒绝、第二普通启动拒绝、四阶段中断、不覆盖发布、外部实体保留及受限恢复。当前仍未获网页端有效放行，登记器、质量审计、正式生成和训练继续禁止。

第六轮精确候选仍未运行。其 packer 为 62,926 字节／`1884a5814d5a20c37c1025518beeb27f7bf4a702847eddd5eb091e1b2606c677`，测试为 183,547 字节／`3ac3e07ac1ab4db9dd9787f9e0ba62845e0c07cfa5b0664fe9f82f7bb6659154`，25 文件闭包压缩包为 238,492 字节／`fb6a6a79a0d47acbff600bd3a206f2b2ebc8b1a55b561d81b56ec8bb4c565a07`；有效网页审查原文为 14,739 字节／`56108c44b77b0e9db4e561d210e0716c5d7cfeebab6a4d97e220b28298ff9fd0`，分级为阻断级 0、高级 1、中级 0、低级 0，最后一行“不允许生成密封字面登记器”。唯一高级问题是恢复入口先检查 marker、后枚举 final lock；在两步之间新构建可能发布 marker 和 final lock，恢复随后错误接管并删除活事务。

当前修复先快照“恢复调用开始时 final lock 是否已经存在”，再检查 marker。若开始时没有旧 final lock，恢复永远不得重新枚举或处理随后出现的新事务，只能失败关闭；若开始时有 final lock，则活构建必然已有更早创建并全程保留的 marker，恢复仍拒绝。新增同步测试覆盖新构建在 marker 检查前或后发布 lock／marker／intent，两种交错下恢复均拒绝且新实体原样保留。登记器专属合同现为 17 项，在 115.746 秒内全部通过。当前字节仍须重新网页复审，未获许可前不得运行。

第七轮差异复审仍未放行，精确拒绝边界为 packer 63,464 字节／`7ef87d4f48f80a1a7acbe3a10d2af619d5bbc320e5460775ecff603b05c80018`，测试 187,784 字节／`cb031886d9c28c283fa1e944fccac66ac59fae644df1e9440b51c72d6212202f`，25 文件闭包压缩包 238,613 字节／`9fcde7ba1b6cb4d4a598f42e4121c8db7cc347b0998729aa6565a615ef2cb97b`；审查原文为 9,026 字节／`50deefd7e2fed7e9527c4bb712596fc700706adc165dd729d65ae4a3c3ad49b6`，最后一行“不允许生成密封字面登记器”。唯一剩余问题是两个恢复调用与新构建交错时，较早快照到旧 final lock 的恢复调用仍未绑定文件实体，可能在旧锁被另一个恢复删除、新构建发布内容相同的新锁后误接管新事务。

为避免继续把科研任务扩展成锁与恢复工程，当前候选彻底禁用自动恢复：命令行不再暴露恢复选项，兼容函数无条件抛出封闭错误，不读取、验证、删除或改写任何事务产物。强制终止后的残留只阻止重跑并保留审计证据，必须暂停、记录、人工核验后处理。正常一次性构建仍保留严格 intent／sidecar／公开回执验证和可捕获失败的本调用精确清理。新测试对空状态及八类逐步填充的残留布局逐字节拍照，证明恢复调用前后完全不变；当前精确版本的 17 项登记器合同在 110.915 秒内全部通过。仍须网页端对新字节复审。

第八轮网页端精确终审已对上述“禁用自动恢复”候选给出仅限一次正常构建的许可。获准边界为：packer 58,157 字节／`c789f235c978393701afa4866696a1d947a2dc6678368a3730ee755874970a49`，测试 176,901 字节／`18da2de1203973d18648d676cd8154bbb64221af422952e4a73221be4fa2875d`，25 文件源码闭包压缩包 237,711 字节／`6b6cf37390327f637cbe5571b5c545fcad9331a8f06bb2ed884feb5df501839c`；审查原文 9,605 字节／`ab709ea458985efcd69d222b7bfc24d7dff3721e8e199875a10d4843d5807917`，最后一行“允许生成密封字面登记器”。许可条件是运行前复核精确字节和所有事务路径为空，只执行一次无参数正常入口；若出现异常或强制终止，立即暂停，禁止自动恢复、人工拼接通过回执或重跑。成功状态只能是 `PASS_SEALED_LITERAL_REGISTRY_BUILD_NOT_AUDIT_GO`。该许可不等于 104 世界质量审计许可，也不授权正式 500×4 生成、真值发布或 M0／M1／M2／M3 训练。

同日按获准字节完成唯一一次正常构建，用时 381.3 秒，104／104 个冻结世界全部确定性重放，进程以 0 退出。公开回执位于 `reports/step28_v13_v1_13_scientific_builder/sealed_private_literal_registry_receipt_v1_20260812.json`，10,935 字节／`38b8757f3fcb98ae5587b85f213f0b6b801f03e0f11faf6c2470abb4f272a9f5`，规范自哈希 `3e13806a027b2fd7def156df76ace7c27326787848eb03a4d4638e0275461ecd`。私有登记表位于忽略目录 `private_custody/step28_v13_v1_13/`，1,211,990 字节／`f00018e302061d2f3f9e4b0b095ceab09661482ec7c619624fdd5332ea031d2a`，规范自哈希 `75a8eee10f298d9333af8aa4dd1ec2282b8db1031431eafa968bcaeae05d57ec`；禁止打印或公开其原始字面。事务意图、锁及所有 `.building` 路径均已清空，公开返回私值、私有关系、标签和排序真值均为 0。审计甲、乙各投影 2 个世界，禁止字面计数分别为 3,099 和 3,249。当前结果只补齐质量审计的密封参照，设计数据仍不具备训练资格；下一步是以这些实际哈希封闭 v7 质量合同、恢复完整回归并另取质量审计许可。

第七版质量候选已完成本地合同封闭。该版本不改变冻结数据、候选、置换、七个文本视图、十四个探针、硬门或 9,999 次世界重抽样，只把质量源码版本和独立输出目录推进到 v7，并将登记器构建器、私有登记表三元承诺、公开回执及 25 文件来源闭包承诺加入机器校验。来源闭包规范哈希为 `ad68911cb50144972c9927fffc01085875e7b054abe739e1206e3383da0e72d1`；运行时还会逐项复核每个来源文件的路径、大小和哈希。当前精确边界为：策略 20,838／`93e3a87302399b17dde85a1ab72fe8be0a8821ef9fc5de79d1e78fd635e0e858`，启动锚 2,913／`2ed2ff80669b3b8e734c219216d4c33b4519e77b2137d139c8ef64dda110eeba`，发布清单 2,766／`c926949646caebea00609b6939493ab24439e680099a8fd064a79d6ca019d2ea`，质量源码 217,189／`97ec6b3e8612053df885b1fa9e335548811973381ca0f2be9186ead2058c0897`，守卫 23,413／`885fa283b5a6e1ff5e1246ff8ab9dc66afd4f5e1cf5b2bbbff1a0f40f09aa292`，测试 178,431／`8869f5c505f528c0effec9e541e531f9c6d029e69d0b32476c35192354e0e6b2`。质量合同 71 项在 193.997 秒内全部通过；物理核心数探测失败后使用逻辑核心的提示不改变数值或随机状态。尚未取得 v7 外部审查许可，`quality_audit_design_v7_20260812` 不存在，质量审计、正式 500×4 生成和训练继续禁止。

标准全仓发现随后运行 692 项，用时 1,775.780 秒：682 项通过、7 项声明跳过、1 项失败、1 项错误。失败仍是已登记的 Step28-v12.1 历史同步清单要求持续更新的 `PROJECT_PROGRESS.md` 回到 2026-07-20 的 199,490 字节快照；旧豁免明确只允许它作为唯一失败，本次又出现第二项后不再满足豁免条件。新增错误来自永久关闭的 v1.12 正式预锁继续逐字节解引用来源闭包中的 `.gitignore`，而当前 v1.13 为保护 `private_custody/` 必须更新该文件；这表明旧预锁已经自然失效，不能再充当当前运行资格，并非 v7 质量实现或数据合同失败。禁止回退 `.gitignore`、`PROJECT_PROGRESS.md`，也禁止修改旧预锁、旧同步清单、旧测试或豁免回执来刷绿。全仓测试误删的已跟踪数据组成文档已从当前提交精确恢复；未产生 v7 质量结果。有效的当前候选证据仍是 v7 专属 71／71 全绿，外审必须看到本段已知历史回归边界。
