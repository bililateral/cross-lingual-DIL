# Step28-v13 v1.13 V9.3 结构配平与捷径修复科研合同

日期：2026-08-25
状态：`DESIGN_REVIEW_ONLY_NO_METHOD_ROOT_NO_FORMAL_DATA_NO_TRUTH_UNSEAL_NO_TRAINING`

## 1. 科研目的与结论边界

V9.3 修复 V9.2 质量审计定位到的“公开商品码／私有创建槽位”失败家族，并一并关闭失效后静态追踪确认的商品码克隆与有限样本结构未确定性配平风险。本合同不声称公开代码、私有槽位、商品数量或任何具体列分别取得了家族最大值。科研主线不变：在独立合成中文身份链接任务上比较冻结英文 M0、五个错配身份历史对照 M1、正确身份历史残差迁移模块 M2，以及直接中文监督的 M3-base 和 M3-joint；身份迁移的主要证据仍为 `M2-五个M1均值`。

V9.2 尝试 3 已完成全部 97 项登记观测并得到有效 `DATASET_INVALIDATED`。该根、随机权威、运行许可、回执、入口和结果路径永久不得复用；不得重跑、改阈值、删门或换解释恢复资格。V9.3 不是修补 V9.2 数据，而是使用新合同、新实现、新机器政策、新版本根和未来独立随机权威的后继机制。

本合同不授权方法资格根构建、质量审计、正式 500×4 数据生成、审核甲乙真值解封、M0 计算或 M1／M2／M3 训练。

## 2. 上位权威与替换范围

下列文件继续作为科研权威：

- `STEP28_V13_V1_13_SCIENTIFIC_EXPERIMENT_CONTRACT_20260810.zh.md`；
- `STEP28_V13_V1_13_QUALITY_AUDIT_C_AMENDMENT_20260811.zh.md`；
- `STEP28_V13_V1_13_V9_QUALITY_AND_CHANNEL_SENSITIVITY_CONTRACT_20260814.zh.md`；
- `STEP28_V13_V1_13_V9_2_SCIENTIFIC_RECONCILIATION_CONTRACT_20260823.zh.md`；
- `STEP28_V13_V1_13_V9_2_QUALITY_AUDIT_ATTEMPT3_DATASET_INVALIDATION_20260825.zh.md`。

本合同只替换与人工商品码、控制者随机切组、卖家／商品创建槽、商品数量、字段缺失和登记负例端点有关的生成与审计条款。V9.2 对作者风格错排反事实文本、完整证据、失败清理和最终指标的规定继续有效；V9.2 的代码遮蔽／代码中和表面及由此产生的 42 个原作者描述模型由本合同第 8 节明确取消。任何其他冲突以本合同的明确替换为准。

## 3. 三层结构与信息边界

每个世界仍固定为 28 个卖家、12 个控制者、4 个三卖家控制者、8 个双卖家控制者和全部 378 个无序卖家对，其中 20 个正例、358 个负例。

V9.3 将生成结构明确分为三层：

1. **身份层**：私有控制者归属、作者风格、身份资产和三十三维身份历史。控制者归属决定合法身份信号和最终真值；唯一允许影响非身份表面的例外，是第 7 节每世界事前登记、固定剂量的 2 个精确标题克隆负例和 4 个高语义负例。
2. **卖家槽层**：28 个抽象创建槽，仅用于建立卖家对象和审计坐标，不得进入模型可见字段、文本、画像或特征。
3. **噪声槽层**：每世界 28 个抽象噪声槽，与卖家槽一一映射，承载商品数量、空字段模式、商品语义、模板、交付、服务和非身份噪声等无意义或内容性因素。噪声槽编号同样不得模型可见。

身份层可以读取冻结的控制者分组计划；登记难例选择器可读取控制者关系，但只能生成第 7 节冻结的逻辑端点、角色和处理类型。该例外是受控、定量受限的标签条件化任务构造，不得表述为完全标签无关。除此以外，模型可见渲染器只能接收白名单化的作者风格、身份槽、噪声计划和已冻结登记处理，不能接收控制者编号、卖家对标签、卖家槽、噪声槽或私有商品坐标。私有真值仍须在全部非真值表面、矩阵和结构承诺冻结后才物化。

## 4. 500 世界严格配平计划

训练和开发各自必须使用一份预先物化、规范排序、自哈希且不可按模型结果修改的 500 世界配平计划。每个世界把 28 个卖家槽精确切成 4 个三元组和 8 个二元组。每份计划必须同时满足：

1. 378 种无序卖家槽位对中，恰有 206 种在 500 世界中同控制者 26 次，172 种同控制者 27 次；不得出现其他次数。
2. 28 个卖家槽中，恰有 20 个进入三元组 214 次、8 个进入三元组 215 次；不得出现其他次数。
3. 每世界卖家槽到噪声槽是一一映射；每个卖家槽在 500 世界中接受每个噪声槽 17 或 18 次，行和列总数都必须为 500。
4. 378 种无序噪声槽位对同样恰有 206 种同控制者 26 次、172 种同控制者 27 次；28 个噪声槽的三元组暴露同样只能为 20 个 214 次、8 个 215 次。
5. 训练与开发计划的规范字节和摘要必须不同。卖家槽对、噪声槽对的 26／27 次指示，以及卖家槽、噪声槽的 214／215 次三元组指示，其训练／开发交集数和 Hamming 距离必须由组合构造器事前固定并写入机器政策；只要求“不完全相同”不构成外推合同。登记难例计划、角色余数归属和逻辑商品循环起点也须具有不同规范摘要。具体交集数只能依据不读取文本、标签文件或模型结果的组合可行性证明冻结。

上述数值是总量约束下的理论最均衡整数分配，不是观察结果后选择的阈值。探索性、未落盘的可行性计算已确认卖家槽和噪声槽双重约束存在整数解；它不构成数据、随机权威、质量通过或运行许可。正式计划只能由后续冻结的确定性构造器生成一次并作为机器政策输入，不能依赖运行时搜索成功与否挑计划。构造器的变量顺序、目标函数、并列解选择、公开设计常数和不可满足终态必须冻结；`UNSAT`（无可行解）时本版本失败关闭，不得换起点、换设计常数或挑另一条求解轨迹。

方法资格随机权威只冻结其独立的训练 500、开发 500、审核甲 2、审核乙 2 个世界计划；审核两个世界的序号须在方法资格文本产生前固定。方法资格通过后，正式随机权威必须与方法资格权威完全独立，并在任何正式文本、训练标签使用或模型结果产生以前，原子地同时冻结正式训练、开发、审核甲、审核乙各 500 世界的全部分组、卖家—噪声映射、噪声计划和登记难例计划。不得把方法资格世界或其“剩余世界”继续物化为正式数据。

## 5. 人工商品码彻底退出模型表面

正式模型可见逐商品文件、卖家画像、五字段文本和 M0／M3 基础特征源中不得出现人工商品码、商品创建槽、卖家创建槽或其编码、散列、字符映射、长度分支和格式分支。既有 `Q[A-P]{10}` 商品码及其 `title_modifier`、英文标签显隐、模板选择等下游作用全部退出正式表面。

`logical_item_ordinal` 固定定义为商品在噪声计划规范商品列表中的位置；该列表在私有 `item_uid`、商品创建槽、插入／写盘顺序、散列值和碰撞登记顺序产生以前冻结。私有 `item_uid` 只可用于行键、所有权、碰撞谱系和审计，不得参与任何模型可见内容或登记难例商品选择。

实现必须把“私有坐标非干预”拆为三个硬测试，并在机器政策中冻结规范重标号集合、对齐映射、比较字段和排序规则：

1. **计划器等变性**：对卖家槽、噪声槽和逻辑商品序号作规范重标号，并同步重标号输入计划；逆映射对齐后，控制者分组、登记难例逻辑端点、端点角色和逻辑商品选择必须完全相同。
2. **选择器非干预**：保持逻辑对象和噪声计划不变，只改变私有 UID、创建槽、插入／写盘顺序和散列表示；所选逻辑端点与 `logical_item_ordinal` 必须相同。
3. **渲染器非干预**：保持已冻结逻辑计划不变，只置换私有坐标；按逻辑行对齐并排除只承担连接作用且明确不进入特征的行键后，全部特征承载字段、卖家画像和 M0／M3 基础特征矩阵必须逐字节相同。

任何一项不同都立即失败关闭。不得只登记两次方便通过的置换，也不得用“最终字节相同”代替对计划器和选择器的独立审计。

代码缺失门在监督真值打开前执行：扫描全部原始和派生模型表面，`Q[A-P]{10}` 命中数、注册商品码字段数、代码派生分支数和代码到模型特征的血缘数必须全部为零。V9.2 的 2,992 维公开代码监督探针不再具有输入对象，不得以零矩阵冒充通过。

## 6. 噪声计划、自然文本与碰撞

每个噪声槽承载一份代码无关的非身份计划。最大余数法的原子类别固定为卖家级联合噪声签名 `noise_signature=(item_count,title_present_mask,description_present_mask,joint_empty_mask)`；三个掩码均按 `logical_item_ordinal` 排列。统计单位为真实中文训练侧的去标签卖家等权记录，不得分别取整商品数量和缺失模式边际后再任意拼接，也不得读取真实身份关系、开发／审核统计或模型结果。

机器政策必须钉住真实训练侧汇总输入的路径、字节摘要、排除规则、签名规范顺序、每类频率和 28 槽最大余数结果。对每类先取 `floor(28*p)`，剩余额度按小数余数降序分配；余数相同时按联合签名规范 UTF-8 字节序。所得 28 个联合签名及其摘要一经冻结不得为登记角色合格性或质量结果重新分箱。

每个世界必须恰有一次完整的 28 噪声槽多重集。类别、商品、属性、交付、服务、模板骨架和自然非身份噪声按拆分隔离的代码无关词库，由世界与噪声坐标取得；其资产、选择公式和摘要在机器政策中冻结。世界级自然内容变化不得由控制者、标签、卖家槽、私有商品坐标或质量结果驱动。任何新增可见选择若绕开噪声层直接绑定卖家创建槽，本版本失败关闭。候选推进不得改变控制者分组、卖家槽到噪声槽映射、作者风格分配、身份机制、登记负例端点或联合噪声签名；这些候选无关父对象的规范摘要必须在候选文本产生前冻结。

移除商品码后必须重新计算可见文档容量和碰撞。碰撞键固定为各字段经过统一码规范化形式 C 后的 UTF-8 字节，以机器政策登记的字段顺序、字段名和长度前缀拼接；空值使用独立类型标记，不与空字符串或缺失字段混同。作用域覆盖历史保留排除摘要、同一世界、同一拆分和当前根先前全部世界。不得在检查后追加唯一编号。

唯一标题级例外是第 7 节每世界恰好两个已登记的克隆来源—目标标题对。每个授权标题只能有登记的两个所有者；出现第三个所有者、非登记标题重复、描述重复、逐商品文档重复或五字段卖家文档重复均失败。逐商品文档和卖家文档没有登记例外。

候选选择必须在控制者、作者风格和身份措辞不可见的代码无关非身份骨架上完成；身份和作者风格完成最终渲染后若再出现任何未授权碰撞，整世界失败，不得推进另一候选。候选序号、回退分支和碰撞类型仍须进入结构回执，但不得进入模型。随机权威生成前，必须完成覆盖历史排除与最大正式 2,000 世界登记作用域的符号容量下界证明，或完成无落盘、无真值、无模型的全作用域碰撞键回放；小规模样本不能证明容量充分。容量不足须关闭 V9.3 并修改新版本自然内容库，不得筛未来种子或用人工唯一串补洞。

## 7. 登记负例的均衡

每世界仍保留 2 个精确标题克隆负例和 4 个高语义负例，六对的 12 个卖家端点必须互异。它们属于事前固定、固定剂量的任务难例，不改变“同控制者为 1、异控制者为 0”的标签公式，但确属利用控制者关系选择负例的可见表面干预。除这六对外，任何非身份内容不得读取控制者关系。可见渲染器只能接收已冻结的逻辑端点、逻辑商品、角色和处理类型。

角色合格谓词固定为 `clone_source_eligible`、`clone_target_eligible`、`semantic_left_eligible` 和 `semantic_right_eligible`。它们只能读取联合噪声签名、代码无关非身份骨架和 `logical_item_ordinal`，不能读取私有 UID、创建槽、作者风格、身份历史或模型结果；每个谓词的非空标题／描述、商品数量和语义条件须在机器政策中逐项登记。

在每个 500 世界拆分内，选择计划至少必须满足：

- 精确标题克隆在卖家槽对和噪声槽对上各为 244 种 3 次、134 种 2 次；高语义负例各为 110 种 6 次、268 种 5 次；
- 精确标题克隆端点在卖家槽和噪声槽上各为 12 个槽 72 次、16 个槽 71 次；高语义负例端点各为 24 个槽 143 次、4 个槽 142 次；
- 1,000 个克隆来源角色和 1,000 个克隆目标角色，在卖家槽与噪声槽上各为 20 个槽 36 次、8 个槽 35 次；来源与目标的 8 个低计数槽集合必须互不相交，从而合并为 16 个槽 71 次、12 个槽 72 次；
- 2,000 个高语义左端角色和 2,000 个右端角色，在卖家槽与噪声槽上各为 12 个槽 72 次、16 个槽 71 次；左右端的 12 个高计数槽集合必须互不相交，从而合并为 4 个槽 142 次、24 个槽 143 次；
- 对每种处理类型和端点角色，二卖家／三卖家控制者上的端点暴露按全部卖家机会的 16∶12 比例取最近整数；确切计数、余数归属以及与卖家槽／噪声槽的联合交叉表须由组合可行性预检后写入机器政策；
- `logical_item_ordinal` 只能在同一处理类型、端点角色、联合噪声签名和角色合格谓词分层内循环选择；每个分层的合格逻辑商品序号计数只能取该分层总次数除以合格序号数的向下或向上取整。分层定义、循环起点和不足时的失败条件必须冻结；不得笼统承诺跨不同合格集合的全局“最大差 1”；
- 每类登记负例的文本处理剂量固定，不设可按候选或结果改变的强弱档位；
- 任何端点或方向不变量不满足时，不得依靠重新抽世界、换种子或删掉难例通过。

高语义负例的公共语义资产不得把八类商品强制均匀化。每个拆分的 2,000 对语义资产必须依据已钉住公共域中的 12 位匿名类别整数权重，以最大余数法一次确定精确类别计数；当前理论计数依注册顺序为 `559、383、254、117、82、81、76、448`。商品在各自类别内、十种属性以及 56 种不同标题骨架有向组合分别按向下／向上取整均衡；再使用训练与开发各自不同、只读取拆分名和公共资产坐标的 SHA-256（安全散列算法）固定排列。该排列不得读取控制者、卖家槽、噪声槽、标签、私有坐标、文本或模型结果。这样只引入“同一语义对共享类别、商品和属性”这一登记困难负例机制，不额外制造均匀类别相对于真实底座类别分布的捷径。验证器必须重新打开钉住政策，独立重算公共域摘要、整数权重、最大余数计数、条件商品计数、属性计数和骨架对计数。

全部条件必须由同一个联合选择器同时求解，不能先独立配平边、端点、方向、控制者规模和商品合格性后再拼接。选择器使用冻结的变量顺序、目标函数和规范并列解；组合预检必须在任何文本生成前证明每世界存在六对异控制者、十二端点互异的完整角色匹配，并证明全部卖家槽和噪声槽具有足够合格机会。联合问题为 `UNSAT` 时 V9.3 失败关闭，不得重新分箱真实分布、放宽角色配额、换求解起点或选择另一可行解。

精确标题克隆必须复制已经完成代码无关原生渲染的标题。登记负例仍按上位合同从反事实文本近随机硬门的 372 对中排除，但其端点对其他卖家对的画像影响必须保留并接受文本审计。

## 8. 修订后的捷径审计

训练集拟合、开发集过门；审核甲乙在盲预测冻结前只做不需要监督真值的结构审计。资格硬门分为四个对象，描述性结果另列，彼此独立报告，不拼接后隐藏来源：

1. **结构与零出现门**：第 4 至第 7 节全部精确计数、代码零出现、私有坐标非干预、字段白名单、文档碰撞和拆分隔离必须逐项通过。
2. **卖家槽矩阵**：每对的 378 维卖家槽对独热值及最小、最大、差、和；固定二范数逻辑回归与深度 2 浅树分别拟合。
3. **噪声槽与可见结构矩阵**：噪声槽对独热值，以及商品数量、标题／描述／联合缺失掩码、直接登记处理、四种端点角色、端点处理对其他配对的溢出剂量、二／三卖家控制者规模、`logical_item_ordinal`、候选序号／回退类型、类别／模板／服务／交付和所有进入 M0／M3 基础特征但不属于合法作者风格或身份历史的结构字段之对称摘要；同样使用两类固定模型。该矩阵在真值打开前冻结，不含自由文本、作者风格或身份历史。
4. **作者风格错排反事实文本资格硬门**：继续使用 V9.2 的七视图、两模型、14 个硬门模型，固定使用排除六个登记负例后的同一 372 对掩码和 `20/372` 基线。

代码无关的原作者完整表面使用相同七视图、两模型和 372 对掩码，形成 14 个仅描述性作者风格结果；它们不具有否决权，也不得补偿任何硬门失败。若另报完整 378 对全任务描述，必须使用不同的明确名称和 `20/378` 基线。代码遮蔽和代码中和表面不再物化，其原 42 模型定义退出 V9.3，禁止以重复表面或零矩阵冒充描述证据。

V9.3 的文本模型输入固定为原作者完整逐商品／画像和风格错排完整逐商品／画像，共 2 个表面、4 份文件；不再沿用 V9.2 的 4 表面、8 文件计数。代码通道是否关闭只由第 1 类结构门证明。

第 2、3 类合成一个“结构坐标家族”，但四个模型分别训练、分别预测，只在每项统计内取事前规定的最大值，不得拼接两个矩阵。完整 378 对的 AP 基线固定为 `20/378`；单特征最大对称 ROC-AUC 不超过 0.52，家族最大对称 ROC-AUC 不超过 0.53，家族最大 AP 提升不超过 0.01，9,999 次世界重抽样的家族最大 ROC-AUC 与 AP 提升 95% 上界分别不超过 0.53 和 0.015。反事实文本家族继续使用 372 对和 `20/372` 基线，不得混用。

任何方法根数据产生以前，机器政策必须登记第 2、3 类每一列的精确列名、公式、维数、顺序、类型、空集合约定、归一化方式、资格掩码和规范摘要。验证器必须拒绝缺列、多列、重排、隐式类型转换和运行时动态增列。直接登记处理指示本身是预期的弱负标签信号，仍须进入矩阵并如实报告，不能因其属于任务难例而从 378 对结构门删除。

在每世界恰有 6 个登记负例、其余 372 对未登记且采用标准并列秩时，单独使用“是否登记难例”预测负例的设计期对称 ROC-AUC 固定为 `91/179≈0.508380`，AP 提升固定为 `20/372-20/378=5/5859≈0.000853`。这两个数只用于核对登记机制的固有剂量，不是替代正式探针的通过结果；端点画像溢出、角色、控制者规模及交互仍必须由完整结构门评估。

合法信号不设置数据后性能下限，但须通过机制性正控制：除明确删除的代码分支外，身份资产清单、三十三维历史字段、控制者内共享关系和作者风格渲染规则必须与上位合同钉住；保持噪声计划不变并替换作者风格时，只有预登记风格投影可变化，且每个冻结风格族至少产生一个规范非空差异；保持身份层不变并替换噪声计划时，身份历史投影必须逐字节不变。正控制失败即机制实现失败，不得靠增强可预测性修复。

质量证据必须使用机器政策内唯一、有序的观测注册表；每项固定 `observation_id`、对象、拆分、视图、模型、指标、资格硬门／描述性角色和预期出现次数。结果的精确标识集合必须与注册表相等，缺失或多出任何项均失败关闭。证据必须公布失败家族、视图、模型和安全列类别，使后继能够定位根因；不得公布审核甲乙逐行标签、控制者关系、qrels 或可还原真值的预测向量。任一硬门失败即整根失效，不得用另一家族通过抵消。

## 9. 分阶段执行与停止规则

执行顺序固定为：

1. 冻结并测试抽象 500 世界卖家槽／噪声槽双重配平构造器；只输出小型计划和结构统计，不生成文本。构造成功后把训练／开发指示交集与 Hamming 距离精确写回机器政策。
2. 冻结 28 个联合噪声签名、四类角色合格谓词和登记负例联合选择器；完成包含控制者规模、双槽位、方向、端点互异和逻辑商品合格性的完整组合可行性预检。边际恒等式通过不能替代联合可行解。
3. 实现代码无关渲染、三层私有坐标非干预、代码零出现和代码无关碰撞；先完成覆盖最大正式登记作用域的容量证明或全作用域无落盘回放，再运行小规模渲染单元测试，不构建方法根。
4. 完成结构坐标矩阵、反事实文本质量链、聚焦测试和全仓回归；提交并推送干净字节。
5. 由网页端 GPT-5.6 Sol Pro 对精确合同和实现作聚焦科研审查；本地必须核实其事实和建议，不得照收幻觉或偏离到网络、权限、锁和操作系统攻防。
6. 只有新的独立单次许可才可生成 V9.3 方法资格随机权威；随后另行许可一次 500／500／2／2 方法资格根构建，再另行许可一次质量审计。
7. 方法资格根取得完整 `PASS` 只允许申请全新且独立的正式随机权威；该权威按第 4 节一次冻结正式四个 500 世界。正式根自身质量通过后，才可另行授权模型阶段。

普通开发错误直接修复并回归；已获准运行且形成不可恢复科研失败的版本，先记录运行编号、失败阶段、根因、结论边界、关键摘要和不复用承诺，再删除失败载荷、缓存、临时目录和仅服务该失败版本的代码。不得保留大型失败数据等待以后查看。

## 10. 模型与最终指标边界

V9.3 不改变 M0、M1、M2、M3 的定义、共同折、阈值和统计比较。未来最终报告必须把 pooled AP（合并配对平均精确率）与梯形 PR-AUC（精确率—召回率曲线下面积）分别计算和报告，并完整报告 MRR（平均倒数排名）、MAP（查询级平均精确率的算术均值，即 mean average precision）、Recall@1/3/5/10（前若干名召回率）、NDCG@1/3/5/10（前若干名归一化折损累计增益），以及上位科学合同规定的 ROC-AUC、Precision、Recall、F1、Specificity、Balanced Accuracy、MCC、Brier、Log Loss、Recall@FPR=1%、混淆矩阵和配对世界重抽样区间。查询级 AP、MAP、合并配对 AP 三者的聚合层级必须分开命名，不得把 MAP 解释为 accuracy（准确率）的均值。

当前唯一允许动作是合同审查、抽象配平与容量可行性预检、实现、测试、全仓回归、文档和 Git 同步。正式数据行、审核真值读取、模型训练和正式指标继续为零。

## 11. 组合求解开发尝试记录

早期 `design_preflight_v1_20260825`（3 个文件、249,036 字节，回执自摘要 `3e8909be9d513706fc4c87ea9ca7939d4e2f151766784f1b8053ff0718fbd1a1`）虽然分别满足单拆分边际配平，但训练与开发共用同一公开设计常数，不能证明拆分计划独立；已由使用两个独立常数、含精确距离和全局重标拒绝审计的 `design_preflight_v2_20260825` 取代。早期 `registered_negative_preflight_v2_20260825`（3 个文件、1,220,822 字节，回执自摘要 `3b99805596dfa515ed27482b9d988541e43183473c2c0451d661a2f24e46c498`）只做一次训练搜索并派生开发计划，且缺少后来冻结的双坐标方向、角色与控制者规模交叉、文本合格商品选择、独立语义资产分布和训练／开发计划全局重标拒绝门。两者均未生成正式数据或模型，不能作为 V9.3 方法资格证据；在本节保留边界和关键摘要后，其载荷目录于后继干净运行前删除，路径永久不复用。

登记负例联合选择器第一次固定轨迹使用训练公开设计常数 `281320260826`，前 40,000,000 次为线性降温，后 10,000,000 次为近零温精修。它未读取文本、身份真值或模型结果，也没有启动开发轨迹。训练轨迹从初始违约量 34,451 降到第 49,000,000 次的 40，但在固定 50,000,000 次上限内未达到严格零违约，终态为“固定抽象联合选择轨迹未求解”。本次没有发布计划、回执、数据、临时构建目录或训练结果；最终 40 不能解释为近似可行解，也不得进入后续渲染。

该失败只说明单步近零温精修陷入局部解，不证明联合整数合同不可满足。后继求解器不得更换本次种子、放宽配额或筛选数据；允许在同一起点精确重放后，事前固定有限再升温和最终精修阶段，并增加分家族违约量诊断。第一次轨迹及其原输出路径永久不作为可行性证据复用。

第二次固定轨迹继续使用同一训练公开设计常数，采用 40,000,000 次原退火、15,000,000 次有限再升温和 20,000,000 次最终精修。它在 75,000,000 次固定终点仍有 29 点违约：噪声有向边 7、卖家有向边 5、噪声无序边 2、卖家无序边 10、噪声角色与控制者规模交叉表 5，其他六个计数家族均为 0。开发轨迹仍未启动，也未发布任何计划、回执、数据或临时构建目录。该结果证明一次再升温可以离开第一次局部解，但现有至多两端点的单步邻域仍不足以联合闭合两套坐标的边余数；第二次原输出路径同样永久不作为可行性证据复用。后继允许在不改变种子、数据和约束的前提下加入单次提案内的三端点循环和整对交换。

第三次固定轨迹加入跨三个世界同角色位置循环、跨世界随机位置三循环、跨世界整对交换和世界内三循环，保持相同训练公开设计常数，并使用 40,000,000 次退火、15,000,000 次再升温和 30,000,000 次最终精修。85,000,000 次终点仍有 32 点违约：噪声有向边 9、卖家有向边 13、噪声无序边 4、卖家无序边 4、卖家角色与控制者规模交叉表 2，其他六个家族为 0。开发轨迹未启动，无计划、回执、数据或临时构建目录发布。该结果说明随机复合邻域仍不能提供严格闭合保证；不得继续以增加退火次数冒充求解。后继须改用确定性局部整数修复：候选移动先逐世界验证合法，再由整数规划选择世界互斥的移动并同时满足全部冻结计数上下界。候选模型不可行只证明该候选池不足，不得冒充完整联合合同不可满足。

第四次开发轨迹（原拟输出路径 `registered_negative_preflight_v6_20260825`）保持训练公开设计常数，使用 40,000,000 次退火和 15,000,000 次冷精修，随后进入一次性候选整数修复。进入修复前剩余目标值为 45：噪声有向边 7、卖家有向边 23、噪声无序边 5、卖家无序边 8、噪声角色边际 1、噪声角色与控制者规模交叉表 1，其余家族为 0。进程随后结束，未发布输出目录、计划、回执、数据或模型；旧实现没有在候选生成完成和整数求解结束时分别留下持久诊断，因此现有证据不能区分候选池不可行、求解超时或进程异常，禁止补写虚假根因。该轨迹的科研结论仅为“未形成可审计的严格零违例解”，其路径和候选状态永久不复用。

第四次轨迹还暴露出方法问题：旧整数修复要求单批随机候选一步达到所有 5,324 个计数单元的严格可行域，缺少针对每个违例单元的确定性候选覆盖，也没有允许候选池先严格减少剩余违例再重新生成候选。后继修复器必须逐轮独立重算全部计数，使用带显式松弛量的稀疏整数规划按字典序最小化总边界违例与移动数；每轮必须严格改善至少一个完整重算指标，最终仍只接受零违例。求解状态、候选数、选中移动数和每轮前后违例必须即时输出；任何非零终态仍是失败，不得放宽门槛。

第五次开发轨迹（原拟输出路径 `registered_negative_preflight_v7_20260825`）继续使用同一训练公开设计常数，40,000,000 次退火和 15,000,000 次冷精修的终态仍为 45 点违例。新版候选器逐项覆盖了 45 个违例单元，产生 6,923 个定向候选和 4,000 个补偿候选，总计 11,110 个；随机阶段检查 307,920 个合法提案。带松弛量的稀疏二元整数规划在 300 秒上限内只保留了零移动初始解，终态为 HiGHS 状态 13“达到时间上限”，因此按“未选择任何严格改善移动”失败关闭。开发轨迹未启动，输出目录和 `.building` 均不存在；没有计划、数据或模型。临时控制台日志为 13,268 字节，SHA-256（安全散列算法）为 `aa5b49ea5b1e299dfb6c36986762d45a66cc657cf6c549e861338740ec7ad90f`，在本段登记后删除，原路径永久不复用。

第五次失败不证明 5,324 项联合合同不可满足，只证明把 11,110 个二元候选一次交给通用分支定界器仍然过大。后继必须先按规范候选键并列排序，反复选择使完整平方违例目标下降最多的单个世界合法移动；每次应用后清零并从 500 世界独立重建全部计数，禁止沿用旧增量值。只有不存在单步严格下降移动时，才可把按“帮助违例量、目标变化、受影响世界和规范字节”预排序压缩的小候选集交给带松弛整数规划；每轮仍须严格改善，最终只接受零违例。大整数规划的超时初始解不得再冒充候选不可行。

第六次开发轨迹（原拟输出路径 `registered_negative_preflight_v8_20260825`）严格重放相同 55,000,000 次训练轨迹并再次到达 45 点。第 1 轮压缩整数规划把目标降到 38；第 2、3 轮定向单步各降 1；第 4 至 6 轮组合解依次把 `36→28→24→20`。所有应用后都从 500 世界清零重建全部计数，声明目标与完整重算一致。第 7 轮对 2,966 个定向候选和最多 4,000 个补偿候选预排序，仅保留 1,500 个帮助候选和 500 个补偿候选；HiGHS 在 300 秒内只返回零移动初始解，因没有严格改善而失败关闭。开发轨迹未启动，输出目录和 `.building` 均不存在；没有计划、数据或模型。临时控制台日志为 19,236 字节，SHA-256 为 `bb31f63bb10ab8b8ea92172beb5f6844e28150a996908c67df5717c6e54ad32e`，本段登记后删除，路径永久不复用。

第六次轨迹证明定向单步与组合修复能够真实减少违例，但也证明固定压缩到 2,000 个候选可能删掉闭合所需组合，且允许零移动作为初始可行解会浪费通用分支定界器时限。后继改用 OR-Tools 9.15.6755 的 CP-SAT（约束规划—布尔可满足性）求解器：保留至多 20,000 个已逐世界验证的确定性候选，明确加入“本轮总一范数违例至多为上轮减一”，因此零移动不再可行；每世界至多一个候选，全部 5,324 个单元仍有显式上下界松弛，单线程、固定种子、每轮 900 秒。求解器只负责寻找严格改善的世界互斥候选集合；应用后仍必须独立全量重建，最终只接受平方目标和一范数违例同时为零。依赖版本须进入构建回执和后续环境锁。

第七次开发轨迹（原拟输出路径 `registered_negative_preflight_v9_20260826`）严格重放到训练 45 点终态，随后把 6,923 个定向候选和 4,000 个补偿候选共 11,110 个全部交给单线程、固定种子的 CP-SAT，并显式要求总一范数违例至多 44。求解器在 900 秒上限返回 `UNKNOWN`，没有严格改善解，也没有不可满足证明；因此失败关闭。开发轨迹未启动，输出目录和 `.building` 均不存在；无计划、数据或模型。临时日志为 13,222 字节，SHA-256 为 `1550cf144526e5fdb6a587fb6dca01f914c4683d6882fa9643f811e9df8dc101`，登记后删除，路径永久不复用。

第七次失败说明从 45 点直接处理 11,110 个候选仍不合适，也再次暴露“每世界只能选择一个预先生成移动”的候选域限制。曾考虑在 20 点附近合并两个单世界候选作为复合行，但该想法没有运行、没有结果，而且合并两个相对同一基准行的终态并不等同于在原始端点空间中执行完整的两步搜索；因此该方案在进入新实验前撤销，不得作为后继修复或根因证据。

正确的后继是原始变量残差诊断：先严格重放第六次已经验证的粗修复路径，再逐项登记剩余违例的当前值、上下界和贡献世界；随后按允许读取当前抽象计数违例、但不读取文本、真值或机器学习模型结果的固定规则选择受影响世界，直接以这些世界的 12 个角色端点为整数决策变量，同时执行端点互异、异控制者、角色合格性和全部 5,324 个全局计数约束。局部模型无解只能触发按冻结批次扩大世界集合，不能解释为完整合同无解；只有全部 500 世界的原始变量模型给出可复核的 `INFEASIBLE`（不可满足）证明，才能修改合同。若原始变量模型得到严格零违例解，则根因闭合为旧候选生成域不足。该诊断通过独立全量计数复算以前，不得生成开发计划、文本、正式数据或模型。

第八次开发轨迹预先冻结为构造器版本 `2026-08-26-step28-v13-v1-13-construct-registered-negative-plan-v9-3-solver-v8-original-residual` 和全新输出路径 `registered_negative_preflight_v10_20260826`。它仍使用同一公开设计常数和前 55,000,000 次轨迹；大于 20 点时只允许重放第六次已经验证的定向单步与压缩松弛整数粗修复，达到一范数违例不超过 20 后立即退出预生成候选域，进入原始端点模型。

原始端点模型的世界集合按下列规则一次确定为嵌套序列：首先纳入当前对任一超额计数单元有贡献的全部世界；对每个不足单元，再将拆分名、公开设计常数、完整违例摘要、单元坐标和世界序号编码为无空白规范 JSON 并按其 SHA-256 排序，纳入 `max(4, 2×不足量)` 个能够产生该单元的世界；其余世界按可覆盖不足单元数降序、上述独立摘要和世界序号排序。固定批次为 `24、48、96、192、320、500` 个世界；若强制纳入集合更大，则批次扩至该集合大小并跳过重复规模。各批次固定墙钟上限依次为 `300、450、600、900、1200、1800` 秒，单线程、固定种子；某批次 `INFEASIBLE`、`UNKNOWN` 或到时均按上述序列继续，只有得到可行解才停止。

每个入模世界的六个登记负例分别在全部合法有向卖家端点对上取一个值，不限制相对当前行修改多少个端点；同世界十二个端点全局互异。每个合法选项同时向卖家／噪声的无序边、有向边、角色、端点、二／三卖家控制者以及角色与控制者规模交叉表贡献计数，未入模世界的贡献冻结，从而对全部 5,324 个单元直接施加原上下界。求解器的当前行提示只影响搜索顺序，不限制可行域。得到候选解后必须从 500 世界清空并独立重建全部计数，平方违例目标和一范数违例均须为零；否则视为实现错误。500 世界模型只有明确 `INFEASIBLE` 才是完整合同不可满足证据，`UNKNOWN` 和超时没有科研结论。第七次失败专用的候选 CP-SAT 和未运行的两步合并代码均不进入第八次轨迹。

第八次开发轨迹于 2026-08-26 唯一运行。训练拆分严格重放初始目标 34,451、55,000,000 步后的 45 点终态和已经验证的粗修复序列 `45→38→37→36→28→24→20`；每次应用后均从 500 世界清空并重建全部计数，声明值与独立重算一致。20 点残差摘要为 `19dbedf0d57277c88f16d4c94f0a0efe1b337dc898461239edf2a7ab7fa01d79`，强制集合含 170 个超额贡献世界，最小选择集合为 191 个世界，固定选择顺序摘要为 `662b0a49f6beecf20f25760896649366d2d6eb6cb403922324b071a548546d1d`。

原始端点模型依次对 191、192、320、500 个世界运行，四次均返回 `UNKNOWN`，没有可行解也没有不可满足证明。完整 500 世界模型含 2,148,000 个布尔选项和 5,324 个残差计数单元，在 1,807.686382 秒求解墙钟内只有 2,495 个分支、0 个冲突。该证据定位的是表示与传播不足：把每个合法有向端点对独热展开，令求解器的大部分时间消耗在超大模型物化／预处理，而没有形成有效搜索；它不证明联合合同不可满足，也不允许把 20 点近似解发布为计划。

开发拆分未启动；`registered_negative_preflight_v10_20260826`、`.building`、计划、回执、文本、真值和模型均未产生。第八次路径和同一 2,148,000 变量独热模型永久不得复用。后继必须保持种子、数据、5,324 项约束和严格零违例门不变，但使用等价的紧凑分解或具有可审计传播界的求解表示；不得仅延长同一模型时限、放宽配额或删除质量门。

第九次后继预先冻结为构造器版本 `2026-08-26-step28-v13-v1-13-construct-registered-negative-plan-v9-3-solver-v9-pruned-residual` 和新路径 `registered_negative_preflight_v11_20260826`。它精确重放相同 55,000,000 步及粗修复，不复用 V10 的模型、状态或路径。V10 把任一超额单元的全部贡献世界强制入模，使最小批次膨胀到 191；第九次对每个超额单元只按 `max(4, 2×超额量)` 选择当前贡献世界，对每个不足单元按 `max(4, 2×不足量)` 选择能够产生该单元的世界。排序先按能够覆盖的不足单元数降序，再按拆分、公开设计常数、完整违例摘要、单元、用途和世界序号的规范摘要及世界序号确定；各单元选择集合取并集，其余世界沿用同一确定性覆盖排序。

固定嵌套批次改为 `24、48、72、96、128、192`，墙钟依次为 `300、450、600、900、1200、1800` 秒；若前置并集更大则提升到并集大小并跳过重复规模。每批先从全局当前计数减去所选世界的当前贡献，得到每个单元必须由所选世界重新提供的残差上下界。若外部冻结贡献已经超过某单元上界，该批静态不可行并直接扩大；对每个合法端点选项，若其单独贡献已经超过任一残差上界，则因所有贡献均非负，该选项不可能属于任何可行解，可在建变量前安全删除。任一组被删空时同样只解释为该批静态不可行。其余端点合法域、十二端点互异和全部 5,324 项约束不变，候选解仍须经 500 世界清空重建且平方目标与一范数违例同时为零。

第九次不再运行完整 500 世界的同一独热表示，因而其所有批次失败只能说明紧凑局部邻域未闭合，不能证明合同不可满足。聚焦构造器新增残差上界剪枝反例，当前 14 项全部通过；完整 V9.3 合同回归为 29 项，其中 24 项实际通过、5 项因计划未发布而跳过。只有训练和开发均独立取得严格零违例，才允许发布 V11 计划。

V11 唯一运行于 2026-08-26 13:24 启动，并在训练固定重放第 49,000,000 步、目标 52 后由本地科研审查主动终止。终止原因不是优化器结果：世界构造器的完整非干预测试发现，联合噪声签名 V1 把 17 个没有任何非空描述的真实训练侧卖家按最大余数折算成了 1 个 `description_present_mask=00` 噪声槽；上位机器政策同时要求每个合成卖家至少 2 条非空描述，身份槽也只允许进入非空描述。故 V1 与仍有效的训练就绪数据合同机械矛盾，使用它得到的任何登记计划都不能进入世界构建。V11 没有启动粗修复、局部整数规划或开发拆分，没有输出目录、计划、回执、文本、真值或模型；该运行路径永久不得复用，也不对 5,324 项登记负例合同的可满足性作结论。

后继必须保持真实中文训练侧、去标签、卖家等权和联合签名整数化原则不变，但在整数化以前应用上位政策已经冻结的训练就绪条件：每个来源卖家裁剪／填充后的联合签名至少含 1 个非空标题和 2 个非空描述。当前只读复算得到来源 676 个卖家中 648 个合格、28 个排除，合格卖家原始选中商品行 3,354 条；这些数值仍须由新版本构造器、独立重放和规范自哈希正式闭合。联合签名 V1、自摘要 `a98cb5718fdbcd528274789e908d635fe3322ccb620f9e58d23d545a4e4def71`、文件摘要 `6ed27b57ae701303c2f9e8d2d567e858bb139e91c0de3ee7fbaea0f5bbae9cfd` 及依赖它的 V11 运行不得复用。新联合签名发布后，登记负例计划必须使用新的版本、输入摘要和输出路径从头独立求解。

训练就绪联合签名 V2 已在新路径 `joint_noise_signature_preflight_v2_20260826.json` 发布：648 个合格卖家、28 个排除卖家、3,354 条合格原始商品行、85 条排除商品行和 33 类联合签名均由钉住来源独立重放闭合。28 个整数化噪声槽全部至少含 1 个非空标题和 2 个非空描述。规范自摘要为 `714237710d99eb2f31ba44b93bd3bd463e247b636c7805516e93933275af4e7f`，文件摘要为 `05caa6a2b19591fdbf042384b1d31dccfde4a3f1096275ec44671f9f4b3c4d37`，独立派生表摘要为 `472b7e976f9af79ec3651da24231de8d64ce5d9423986ec2d3b6cb97c28f392f`。V2 仍只是去标签聚合预检，不是方法资格数据或质量通过。

下一次登记计划固定为构造器版本 `2026-08-26-step28-v13-v1-13-construct-registered-negative-plan-v9-3-solver-v10-training-ready-noise`、计划版本 `2026-08-26-step28-v13-v1-13-registered-negative-plan-v9-3-training-ready-noise` 和新路径 `registered_negative_preflight_v12_20260826`。它必须从 V2 输入从头独立重放训练和开发，不读取或恢复 V11 状态；5,324 项计数门、端点域、严格零违例和全量独立重建门保持不变。

V12 唯一运行从 V2 从头重放，训练轨迹在 55,000,000 步达到 45 点，随后经粗修复依次降到 `38→37→36→28→24→20`。进入原始端点残差世界选择时，稀疏超额单元 `directed_seller:(0,26,7)` 的当前贡献世界少于旧代码固定要求的 `max(4, 2×超额量)`，构造器错误抛出“贡献世界不足”。这里的 4 和 2 倍只用于扩大候选多样性，不是合同可行性的必要条件；消除超额只需覆盖至少等于超额量的现有贡献世界，后续固定批次本来还会从完整世界顺序补足求解域。因此 V12 是残差选择器的机械执行失败，不是数据或 5,324 项合同不可满足结论。开发拆分、输出目录、`.building`、计划、回执、文本、真值和模型均未产生；V12 路径永久不复用。

后继 V13 固定为构造器版本 `2026-08-26-step28-v13-v1-13-construct-registered-negative-plan-v9-3-solver-v11-capped-residual-contributors` 和新路径 `registered_negative_preflight_v13_20260826`。对每个超额单元仍取 `max(4, 2×超额量)` 作为期望多样性，但实际选择数上限为该单元全部现有贡献世界数，并继续硬性要求所选数不少于超额量；其余世界仍按原冻结完整顺序进入 24／48／72／96／128／192 批次。新增稀疏三贡献世界反例测试，证明该情形选择全部三者且不再虚假失败；约束、种子、初始轨迹、严格零违例门和独立全量重建均不改变。

V13 唯一运行重新得到训练拆分 20 点残差，并成功越过 V12 的稀疏贡献世界错误。随后 71 世界／99,778 个选项／300 秒、72 世界／103,140 个选项／600 秒、96 世界／169,044 个选项／900 秒三批均返回 `UNKNOWN`；96 世界批只有 473,564 个分支和 16 个冲突。128 世界批已经物化 284,424 个选项并进入 1,200 秒求解，但在确认扩张只继续增加高度对称变量而没有改善传播后，由用户明确要求终止。该终止不是可行、不可满足或数据质量结论。

终止后精确核验：进程已退出，开发拆分未启动，`registered_negative_preflight_v13_20260826`、`.building`、计划、回执、文本、真值、正式数据和模型均不存在；没有需要保留或删除的失败载荷。V13 路径、24／48／72／96／128／192 扩张策略及同一独热端点表示永久不复用。后继必须冻结当前合法安排，只围绕非零残差枚举可独立重放的局部交换及有限复合交换，以互斥世界约束选择交换组合；任何候选解仍须清空并重建全部 5,324 个单元，平方违例与一范数违例均为零才能发布。

后继 V14 固定为构造器版本 `2026-08-26-step28-v13-v1-13-construct-registered-negative-plan-v9-3-solver-v12-residual-exchange-columns` 和新路径 `registered_negative_preflight_v14_20260826`。达到一范数违例不超过 20 后，当前 500 世界安排全部冻结；候选列只来自对当前非零残差的完整确定性定向变换、世界内同角色交换、世界间同位置交换和同一登记对位置的完整双端交换。前者不得按单元截断或抽样，后三类保持相应全局角色／端点边际；定向候选造成的边界外推另以两层确定性补偿闭包覆盖。每列保存完整行替换和全部计数增量，同一世界至多选择一列；整数规划先最小化全部 5,324 项的一范数违例，再最小化触及世界和端点数。每轮应用后必须清空计数并从 500 世界独立重建；只有严格改善才可进入下一轮，零违例且完整目标也为零才可发布。候选安全上限只允许失败关闭，禁止截断后继续求解。

V14 唯一运行于 2026-08-26 17:48:16 启动。训练拆分精确重放 55,000,000 步到 45 点，再经既有粗修复 `45→38→37→36→28→24→20`；每轮声明值均与全量计数重建一致。进入 V14 交换列枚举后，在任何新整数求解调用以前，两层补偿闭包的确定性候选数已超过 250,000 条安全上限，程序按合同拒绝截断并以 `RegisteredNegativeConstructionError` 退出。越界发生在补偿深度枚举内部，早于世界间同位置和完整登记对交换枚举，因此不是求解器 `UNKNOWN`、不可满足证明或数据质量结论。

本次构造器为 101,475 字节／`175d560691b57ecc9082177d904cf19a3a09c02df01965047020372bdc4d09e8`；聚焦测试为 `28b52ca0259c2c3dadd99a5c5f7451011cac4041cbb5522357a40e0137c9d16f`。开发拆分未启动，V14 最终目录和 `.building` 均不存在；计划、回执、文本、真值、正式数据、模型和质量结果均为零，字节码缓存为零。V14 路径、构造器版本和无压缩两层补偿闭包永久不复用。后继不得提高候选上限或截断后继续；必须先按完整计数增量与触及世界结构做等价压缩，或使用按残差方向定价的增量列生成，使未选候选不必同时物化。

失败边界记录后，V14 交换列求解器、补偿闭包、三项专用求解测试和运行路径引用已从活实现删除；构造器保留首入口终止守卫，防止失败版本再次运行。方法政策的登记计划根改为明确的未分配哨兵，冻结脚本同样无法误用 V14 路径。清理后聚焦回归 26 项：21 项通过、5 项因后继计划未分配而跳过，0 失败；政策自哈希 `c71b43c9c95a6213320f9c8815f5e09717827aa693caacca6c96b480502c98ca` 独立验证通过，V14 路径、哨兵路径、缓存和残留进程均不存在。

后继 V15 固定为构造器版本 `2026-08-26-step28-v13-v1-13-construct-registered-negative-plan-v9-3-solver-v13-incremental-priced-residual-columns` 和新路径 `registered_negative_preflight_v15_20260826`。它继续从训练就绪联合签名 V2 和两份 V2 配平计划干净重放，不读取 V14 状态。两份配平计划的实际文件摘要分别为 `c4a058fb97e351b033d2f4c595985251cf790b1693123bdec01fc64bcc18a4c2`、`d29197a5c052e6a450ea838291f740fbc39e434dd754d2bd3c91dd2bf5ba4087`；此前交接中多出的不同摘要是文档记录错误，不是文件漂移。联合签名 V2 文件摘要仍为 `05caa6a2b19591fdbf042384b1d31dccfde4a3f1096275ec44671f9f4b3c4d37`。V15 入口同时固定路径和文件摘要，任一不符均在搜索前失败关闭。

V15 达到一范数违例不超过 20 后，不再一次性展开两层补偿闭包。它只对当前实际非零单元枚举定向合法变换；同一触及世界集合且完整 5,324 单元增量相同的列只保留规范字节最小者，除此之外不截断。稀疏主问题对每个世界至多选一列，并用动态次级代价保证严格按“全部 5,324 单元总松弛量、改动代价”字典序优化。若获得严格改善，才把选中行应用到真实状态；若只能得到中性种子，则只在内存投影它，查明该种子实际新增或加重的边界违例，仅为这些单元生成补偿列，并逐列回到原状态复算完整增量。投影本身永不提交。

重复中性种子用精确禁忌约束排除，但允许旧种子与新补偿列组成超集；最多定价 48 轮，候选池超过 40,000 条时失败关闭且不截断。任何求解器超时、无 incumbent、预算耗尽或非零终态都不构成合同不可满足结论。每次实际改善后仍清空 11 组计数数组，从 500 世界重建全部 5,324 单元，并要求主问题松弛量与重建一范数完全相等；最终平方目标和一范数均为零才能发布训练计划，再从独立开发初态重复同一流程。

当前 V15 只完成实现与原语级验证，尚未运行正式轨迹。16 项构造器原语测试通过，覆盖动态字典序代价、世界互斥、精确中性种子禁忌、完整增量等价去重、输入路径／字节钉住，以及“直接修正产生旁路违例、按实际旁路增量定价后两列联合闭合”的合成反例。正式输出目录、`.building`、计划、回执、文本、真值、正式数据、质量结果和模型仍为零；完成相邻回归和运行前静态核验以前不得启动 V15。

V15 唯一运行已经机械失败关闭。固定训练轨迹精确到达第 55,000,000 步的 45 点；V15 改过的粗修复路径依次为 `45→39→37→36→35→34→27→22→21→20`，每次声明的一范数和平方目标均与 500 世界全量重建一致。20 点时完整枚举 26,369 条非等价直接列，没有截断。第 1 次定价主问题在 120 秒上限返回一个单世界 incumbent，只作内存投影得到 22 点并实际制造 3 个旁路目标；针对它枚举 1,378 个提案、1,149 个合法提案和 1,041 个等价压缩后列，向主池新增 1,022 列。第 2 次主问题仍返回同一种子，未增加新列，因此该精确种子被禁忌。

第 3 次主问题在同一约 27,391 列池和已禁忌种子下，到时前只返回一个投影违例大于 24 的较差 incumbent。实现没有把允许的 `20+4` 邻域写成主问题硬约束，而是在返回后把越界探索种子抛成整轮致命异常 `Residual pricing seed exceeded its frozen slack neighborhood`。这个种子从未提交，正确控制流应当只接受硬约束内的 incumbent，或者恢复 20 点基态并跳过越界种子；因此本次是求解接线错误，不是合同不可满足、数据质量或近似计划结论。越界后的精确投影值没有在抛错前持久化，禁止猜测。

失败构造器为 103,839 字节／`7035f062feada014f90831e82061fe52f1f3f02bab02edfb73ae6f575e24779b`，测试文件为 37,714 字节／`bee08013b58df9ccf456007130ab2997b07fc32a5caa4a03e20f0b2362ec58f4`。开发拆分未启动，V15 最终目录和 `.building` 均不存在；计划、回执、文本、真值、正式数据、质量结果和模型均为零，缓存和残留进程为零。V15 版本、路径、运行字节和“先强制非空、再事后检查邻域”的控制流永久不复用。后继必须在优化模型内部硬性限制探索解总松弛，并把严格改善搜索与中性种子选择分离；任何越界解不得投影或终止整轮。

后继 V16 固定为构造器版本 `2026-08-26-step28-v13-v1-13-construct-registered-negative-plan-v9-3-solver-v14-strict-master-deterministic-safe-seed-pricing` 和新路径 `registered_negative_preflight_v16_20260826`。输入继续精确钉住两份配平计划和联合签名 V2 的既有路径与文件摘要，不读取 V15 运行状态。V15 曾让残差用的动态松弛代价连带改变了大于 20 点阶段的粗修复选择；V16 对粗修复重新显式使用已冻结的 10,000 松弛代价，动态次级代价只用于不超过 20 点的残差主问题。

V16 把两类动作彻底分开。严格主问题直接加入“全部 5,324 单元总松弛不超过当前值减一”的硬约束，不再强制返回非空探索解；只有满足该硬约束的完整候选组合才能提交。若严格主问题在时限内没有合格 incumbent，则从初始直接列中按规范行键确定性选择安全种子：种子预测总松弛只能保持当前值或最多增加 4，实际变坏单元不超过 64 个，每轮最多 4 个、最多 12 轮。越界种子直接跳过，不投影，也不终止整轮。

每个安全种子只在内存事务中投影。投影后从 500 世界清空重建全部计数，要求预测总松弛、平方目标增量和逐单元旁路签名同时与实际值一致；随后只为实际变坏单元生成补偿列。投影结束无条件恢复基线，补偿列再相对基线逐行重算，并要求完整 5,324 单元增量、终态行和平方目标增量一致。同世界集合且完整增量相同的列只保留规范最小表示；全部新非等价列只有在整池不超过 40,000 时才原子加入，禁止截断或部分加入。任何实际提交仍须独立重建并严格降低一范数；最终训练和开发均只接受平方目标与一范数同时为零。

V16 当前只完成实现和运行前验证，尚未运行正式轨迹。17 项构造器原语测试通过；相邻 V9.3 回归共 60 项，其中 55 项通过、5 项因正式计划尚未发布而预期跳过、0 失败。反例覆盖硬松弛可行／不可行、越界种子跳过、安全种子旁路补偿闭合、总松弛相同但逐单元旁路签名漂移时拒绝并恢复基线、等价压缩、世界互斥以及正式路径／输入字节钉住。最终字节上的全仓回归共 828 项，用时 1,600.202 秒：814 项实际通过、14 项按历史或未发布计划边界跳过、0 失败、0 错误。构造器为 117,191 字节／`716844ed787f6010a957f5008428cd1a706759b6ae9bcabaf5d7ed7c4db8d08f`，聚焦测试为 41,619 字节／`cf2cd93add6b5a4502b979163ba8cb6d48149335807793907bc0c535645acbb0`。回归后缓存、随机测试目录、`.building` 和残留 Python 进程均为零；V16 输出目录、计划、回执、文本、真值、正式数据、质量结果和模型仍为零。上述运行前门已闭合，下一动作只允许从钉住 V2 输入唯一运行 V16 抽象构造；仍不得生成文本、打开真值或训练模型。

V16 随后唯一运行并在训练残差阶段机械失败关闭。固定轨迹到第 55,000,000 步为 45 点，显式恢复的粗修复松弛代价使路径重新与冻结历史一致：`45→38→37→36→28→24→20`；每次声明值均与 500 世界清空重建一致。20 点时完整直接列经严格等价压缩后为 19,236 条；带“总松弛至多 19”硬约束的严格主问题在 120 秒内没有 incumbent，因此没有不合格解被投影或提交。

第 1 轮按规范顺序选择 4 个安全种子，预测松弛分别为 `20、20、20、21`，实际旁路目标单元数分别为 `1、1、2、2`。四次事务投影的总松弛、平方目标和逐单元旁路签名均通过独立重建；补偿列回到基线重算后共新增 19,674 个非等价列，并对 873 个既有等价类换成更小规范表示，主池达到 38,910。扩充后的严格主问题再次在 120 秒内无 incumbent。第 2 轮在任何部分加入以前发现全部新非等价列会使主池超过冻结 40,000 上限，遂以 `Residual pricing candidate pool would exceed its frozen safety boundary; no columns were truncated or partially added` 正确失败关闭。第 2 轮将达到的精确池大小没有在异常前持久化，禁止猜测。

本轮证明 V16 已修复 V15 的越界 incumbent 误杀，但“每轮同时为 4 个安全种子完整加入所有旁路补偿列”仍然过宽：首轮即消耗 19,674 个新等价类，只剩 1,090 个容量，第二轮无法原子加入。它不证明 5,324 项合同不可满足，也不允许提高 40,000 上限、截断候选或发布 20 点近似计划。开发拆分未启动；V16 最终目录、`.building`、计划、回执、文本、真值、正式数据、质量结果和模型均为零，缓存和残留进程为零。V16 版本、路径和四种子全量补列控制流永久不复用。后继只能在不改合同与上限的前提下进一步收窄定价单位，例如一次只处理一个确定性安全种子或延迟生成主问题实际需要的补偿列；方案须重新冻结后才可实现和运行。

## 12. V17 直接零违例主问题运行前合同（2026-08-26）

V17 不再求“当前 20 点至少下降 1”的中间目标，也不再保留总松弛变量。进入一范数违例不超过 20 的残差阶段后，主问题只含候选列二元变量，并把全部 5,324 个冻结计数单元直接写成 `下界－当前值 ≤ 候选总增量 ≤ 上界－当前值`；另对每个世界施加至多选择一个候选的互斥约束。因此任何可接受 incumbent 必须把 `20` 一次闭合到 `0`，`20→19→…→0` 不属于本版搜索路径。候选代价只用于多个零违例解之间选择较小改动，不能补偿任何计数违例。

初始直接列仍按“相同世界集合＋全部 5,324 单元相同增量”完整压缩，不截断。如果初始零违例主问题在固定 120 秒内没有 incumbent，只允许每次投影一个按规范顺序选出的安全种子；投影必须完整重建 500 世界计数，旁路单元逐项一致，并在退出投影时恢复原基线。该种子诱导的全部非等价补偿列是一个原子批次：整批加入后总池不超过 40,000 才可提交，否则整批拒绝并尝试下一种子，禁止截断或部分加入。最多尝试 48 个种子；耗尽只说明冻结候选／定价预算未找到零解，不得宣称合同不可满足。

选中列提交后必须再次清空并独立重建全部计数；一范数违例和平方目标同时精确为 0 才能继续开发拆分。训练和开发均通过后才能发布抽象计划。V17 唯一路径为 `registered_negative_preflight_v17_20260826`；V16 专用逐级严格主问题和四种子批处理活代码已被后继实现取代，不得复用。当前仍不得生成自然文本、读取真值、构建方法资格数据或训练 M0／M1／M2／M3。

运行前聚焦 V9.3 合同回归共 32 项：27 项实际通过、5 项因正式计划尚未发布而跳过、0 失败、0 错误。反例覆盖直接零违例可行／不可行、世界互斥、安全种子旁路补偿、逐单元签名漂移恢复，以及完整补偿批次超出池上限时整批拒绝且基线不变。最终字节全仓回归共 828 项，用时 1,592.381 秒：814 项实际通过、14 项具有明确历史或未发布计划边界的跳过、0 失败、0 错误。构造器为 120,085 字节／`4e0e5355bdf98768eafc8b78354095bb13dd265e53bc6caf101b79f4f78e94ba`，聚焦测试为 42,600 字节／`85c58af9ead3cea22805a9d0bc2295f455854bbc6a61a3ba30c3160e4d0184d5`。回归后字节码缓存、测试临时目录、V17 最终目录和 `.building` 均为零；V17 抽象构造获得一次干净运行资格。

V17 唯一运行随后在训练残差阶段失败关闭。固定轨迹与粗修复再次由 500 世界全量重建得到 `45→38→37→36→28→24→20`。20 点时，19,236 条初始直接候选的无松弛零违例主问题被求解器明确判为不可行；该结论只适用于这组直接候选，不是完整联合合同不可满足。随后 48 个确定性安全种子均完成事务投影检查。第 1、2、3、4、17、20 个种子的完整补偿批次分别新增 `3,545、888、2,624、12,617、614、311` 个非等价列，累计新增 20,599 个，池依次达到 `22,781、23,669、26,293、38,910、39,524、39,835`；六次加入分别发生 30、8、195、653、105、100 个规范表示替换，合计 1,091 个。每次扩充后的零违例主问题均在 120 秒内无 incumbent，状态为到时未知，不是不可行证明。

其余 42 个种子批次均大于当时剩余容量，全部在加入前原子拒绝，没有截断、部分加入或基线污染。最终 39,835 条池只剩 165 条容量，48 个种子预算耗尽，构造器以 `Single-seed residual pricing exhausted its frozen budget without an exact-zero solution; this is not a contract infeasibility conclusion` 退出。开发拆分未启动；V17 最终目录、`.building`、计划、回执、文本、真值、方法资格数据、质量结果和模型均为零，缓存和残留进程为零。

根因已经收敛为表示与状态管理问题：V17 修正了“直接求 0”的目标，却仍要求把每个种子的全部补偿等价类永久累积到固定 40,000 池中，因此再次触及容量墙；它没有证明 20 点状态不存在零闭合。另一个明确的效率错误是，程序只在训练和开发均成功后写文件，故已经独立重建通过的 20 点抽象安排仍只存在于退出进程内存，未形成可复用的小型检查点，迫使每版从 55,000,000 次轨迹重放。后继必须先用全新路径发布并独立验证一个无文本、无真值、无模型、版本无关的 20 点抽象检查点，再研究真正延迟生成、可淘汰非活动列或其他不要求全批永久物化的零闭合主问题。V17 版本、路径、全批永久累积控制流和运行字节永久不复用。

## 13. 训练残差检查点合同（2026-08-27）

为消除每次研究 20→0 都重放 55,000,000 次轨迹的无效成本，下一次唯一允许的长运行不是新残差求解，而是发布训练拆分的版本无关抽象检查点。新路径固定为 `train_residual_checkpoint_v1_20260827`，只读取既有训练配平计划和训练就绪联合噪声签名 V2 的钉住路径与文件摘要。构造仍完整执行冻结退火和粗修复，并在首次经 500 世界全量重建确认一范数违例与平方目标均为 20 时停止；不得调用 V17 残差定价、生成文本、读取真值或训练模型。

检查点只允许包含训练拆分 500×12 个逻辑卖家槽整数、两项输入谱系、构造器／检查点发布器文件摘要、搜索回执、全部 5,324 个计数单元的独立重建汇总和 20 个非零单元的安全结构记录。卖家槽取值域必须为 0 至 27，每个世界仍须满足十二端点互异、异控制者、角色合格性和六对结构合法性。重新打开文件后必须在新的 `JointSearch` 实例中覆盖安排、清空全部计数并重建；形状、合法世界数、逐家族平方目标、一范数违例、非零单元列表和规范自哈希全部逐字节一致才可发布。检查点明确不是登记负例计划、方法资格数据或训练资格证据，但属于后续方法不可替代且体积很小的稳定输入，因此即使后续零闭合求解失败也应保留，不按失败大载荷删除。

终态 V17 的正式入口已经恢复为后继未分配守卫；其无松弛直接求解器、48 种子全批累积方法、五项专用常量和三项专用反例测试已从活代码删除。上游确定性轨迹、粗修复、5,324 项计数重建和候选合法性属于跨版本验证原语，继续由检查点构造复用。只有检查点实现、聚焦测试、全仓回归、最终文件摘要和运行前零残留门全部闭合后，才允许唯一运行该检查点构造。

## 14. 严格零违例路线关闭与 V9.3-R1 修订（2026-08-27）

在 V10 至 V17 已连续证明多种显式表示和候选控制流不能在冻结预算内形成严格零违例计划后，用户明确要求停止长期卡在 `20→0`，可先把完整问题交由网页端 GPT-5.6 Sol Pro 作一次算法审查；若仍不能给出低成本、可保证成功的办法，则如实记录限制并继续科研。网页端提出的世界块分解与精确定价方案在理论上可与原始域等价，但仍需新建分支定价器、穷举反例、训练检查点以及训练／开发两次求解，单次训练预算即 14,400 秒且不保证闭合。本地未采纳该方案，避免再次把主线押在高工程量求解器上。

原严格路线现永久关闭。训练已观察到的 20 点仍不是合格计划，不构成理论配平通过、残差无害或合同不可满足结论。后继由新文件 `STEP28_V13_V1_13_V9_3_R1_BOUNDED_RESIDUAL_STRUCTURE_GATE_AMENDMENT_20260827.zh.md` 管理：固定旧轨迹的有界残差终态，完整公开全部 5,324 项实际值与带符号偏差，并在任何文本和方法根以前运行原合同已经冻结的卖家槽与噪声槽／可见结构探针。只有机械合法性和全部结构硬门一次通过，才能表述为“未检出超过冻结阈值的可学习结构捷径”；任一失败立即关闭该后继版本。旧合同、V10 至 V17 失败记录和旧状态语义不修改、不复用。
