# Step28-v13 v1.13 V9 质量审计第二次执行数据失效记录

日期：2026-08-21

状态：`DATASET_INVALIDATED_MODEL_VIEW_PROFILE_COMMITMENT_DRIFT`

## 1. 运行身份与不可复用授权

本次运行标识为 `step28-v13-v1.13-v9-quality-audit-attempt-2-20260821`。执行字节来自提交 `8b00c172560959ffce665418933b42b3d7292470`、树 `0136cb703d7ca9185da224a13e54ad4209211a72`。网页端复审位于 `https://chatgpt.com/c/6a869b6e-31e0-83eb-b0c9-000a9eb212fb`，完整正文为 20,383 个 UTF-8 字节，摘要 `73a4289d1656e74153515ed9b99928d511545a553699aaf056a139f55606e451`，最后一行是“允许运行一次冻结质量审计”。

第二次一次性回执编号为 `cc8ba6f28c35c00c9d5d3ae3f8e6a240e3ceb330568a81b373a2189fdd2613d8`。待用文件为 4,486 字节，文件摘要为 `d218116e102ce1685866d9ce62239017e5b9257a9934cfab07d1dc90a253982a`；入口已在读取设计数据前把它移至第二次尝试专用的已消费路径。该许可已经用尽，禁止重试或复用。

## 2. 终端结果

唯一无参数入口在 Python 3.10.11、NumPy 2.2.6、scikit-learn 1.7.2 下以退出码 0 发布终端回执。退出码只表示终态成功落盘，不表示数据通过。

结果位于 `reports/step28_v13_v1_13_quality_audit/v9_design_preflight_attempt2_20260821/quality_audit_receipt.json`，大小 5,788 字节，文件摘要为 `9a2bb8fd88e87bd9af621a0f1c684aa8c06fd29b77d80621c41fa57c5dbd9641`，规范自哈希为 `90a9b6839ec5ccc219fda9f63c93d888a451d966cc1a20d415e8a4c219d53928`。终态为：

- `status=DATASET_INVALIDATED`；
- `failure_stage=loaded_model_view_structure_binding`；
- `exception_type=QualityAuditRunnerError`；
- 异常消息摘要 `ab87a86b96b41444936df70ea74d47e189494043a5235f429fb1d74d03268b73`；
- 该摘要精确对应 `Persisted model-view structure hash drift`；
- `cleanup_required=true`，输入根在作出决定时仍保留。

## 3. 精确根因

独立只读重放了四个拆分的六个实际模型视图，并逐世界复算结构回执。共发现 3,012 个不一致，严格等于 `1,004 个世界 × 3 种卖家画像表面`：

| 拆分 | 世界数 | 完整画像 | 代码遮蔽画像 | 代码中和画像 |
|---|---:|---:|---:|---:|
| train | 500 | 500 | 500 | 500 |
| development | 500 | 500 | 500 | 500 |
| audit_a | 2 | 2 | 2 | 2 |
| audit_b | 2 | 2 | 2 | 2 |

三种商品视图的逐世界摘要全部闭合，问题只发生在卖家画像。

构建路径在写盘前调用 `_project_model_seller_profile`，把内部卖家画像缩减为冻结模型允许读取的五个文本字段及限定数值统计；但 `quality_channel_materializer_v9` 在该投影以前就对较宽的内部画像对象计算 `full_profile_sha256`、`masked_profile_sha256` 和 `neutral_profile_sha256`。质量审计按合同从实际写盘的六个模型视图重新分组并计算摘要，因此每个世界的三种画像摘要必然与登记值不同。

这是构建产物自身的可审计承诺错误，不是第一次尝试的授权能力死锁，也不是审计器对同一对象采用了不同哈希算法。现有证据没有证明模型可见文本被运行后篡改，但 V9 根无法证明其登记的结构回执对应模型实际读取字节，所以必须按预登记硬门判为数据失效。

## 4. 科研结论边界

V9 设计根规范自哈希 `2d453eee6a44ea57fedfe8dd05b28c72b92ed3496f219db1b3d727de9d7969cd` 永久失去正式生成和训练资格。不得原地改写 3,012 个摘要、替换清单或把当前根改称通过。

失败发生在监督真值能力构造以前：训练和开发 pair label 未打开，审核甲／乙真值读取为零。正式种子、正式 500×4、M0／M1／M2／M3、模型训练、模型指标以及行级标签／预测输出均为零。当前结果不能评价中文合成身份机制的统计质量；它只证明 V9 写盘对象与结构登记对象不一致。

## 5. 修复与清理纪律

后继版本必须让构建器和结构回执共享唯一的“模型实际写盘卖家画像投影”实现，并在写盘后从实际六个模型视图逐世界重放全部六项摘要。测试必须至少证明：

1. 内部画像含额外字段时，回执只绑定冻结模型投影；
2. 三种卖家画像写盘前后逐世界摘要完全一致；
3. 修改任一实际写盘字段会触发结构绑定失败；
4. 仅修改被投影排除的内部审计字段不会改变模型视图摘要；
5. 商品视图、标签、控制者、Identity33、随机权威、质量阈值和探针定义均不因修复改变。

新版本必须使用新政策字节、新提交／树、新输出根、新网页复审和新的单次构建许可。修复不得利用已看到的质量数值调整数据、标签、世界、阈值或模型。

按项目失败清理纪律，保留本次 5,788 字节终端回执、已消费回执、本文和不可逆摘要；在本文进入 Git 记录后删除 V9 的 1.46 GB 失效数据根以及无复用价值的失败缓存／临时载荷。V9 失败根、第一次与第二次执行字节均不得再次运行。

删除前已把失效根压缩成只读等价承诺 `reports/step28_v13_v1_13_scientific_builder/v9_failed_terminal_lineage_20260821/equivalence_commitment.json`。该文件为 23,733 字节，文件摘要 `65930d2474f9e6bbe821918e13e2ce77eebd1c74e692b3c5991605c6ea740613`，规范自哈希 `8b03ac621efbd028605d6d938a1015af7984210bd880391bea5d6fac6cf84b07`；它逐文件承诺 68 个禁止变化文件，并为四个结构审计文件保存非画像字段流、旧画像摘要流、世界顺序和原清单摘要。只读反事实覆盖全部 1,004 个世界：旧画像摘要恰好 3,012 个不匹配，按实际落盘投影重算后为 0；四个非画像字段流和四个旧画像摘要流均与紧凑承诺一致。待清理根共有 77 个文件、1,464,155,768 字节。
